<odoo>
    <data>
        <!-- Server Action – routes to correct theme via print_player_cards() -->
        <record id="action_print_player_card" model="ir.actions.server">
            <field name="name">Print Player Cards</field>
            <field name="model_id" ref="model_auction_team_player"/>
            <field name="binding_model_id" ref="model_auction_team_player"/>
            <field name="groups_id" eval="[(4, ref('auction_module.group_auction_player_card_print'))]"/>
            <field name="state">code</field>
            <field name="code">records.print_player_cards()</field>
        </record>
    </data>
</odoo>