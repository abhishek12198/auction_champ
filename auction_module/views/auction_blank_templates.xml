<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="player_template_blank"  name="Auction Player Template">
        <html lang="en">
            <script type="text/javascript">
                    odoo.define('auction.auction_player', function (require) {
                        'use strict';

                        var core = require('web.core');
                        var Dialog = require('web.Dialog');

                        var _t = core._t;

                        function redirectToURL(url) {
                            window.location.href = url;
                        }

                        var AuctionPlayer = {
                            showModal: function (ev) {
                                ev.preventDefault();
                                var self = this;
                                Dialog.confirm(this, _t("Are you sure you want to mark this player as SOLD?"), {
                                    confirm_callback: function () {
                                        // Code to mark the player as SOLD
                                        // You can call a controller method here to handle this action
                                    },
                                });
                            },
                        };

                        core.action_registry.add('auction_player', AuctionPlayer);
                    });
            </script>
            <head>
                <meta charset="UTF-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
                <title>Auction Player Template</title>
                <style>
                    body {
                        font-family: Arial, sans-serif;
                        margin: 0;
                        padding: 0;
                        box-sizing: border-box;
                        background-image: url('/auction_module/static/src/background/bpl_season_1.jpeg');
                        background-repeat: no-repeat;
                        background-size: cover;
                        background-position: center;
                        height : auto;

                    }
                    .player-card {
                        width: 1000px;
                        height: 1000px;
                        border-radius: 5px;
                        padding: 20px;
                        margin: 20px auto;
                        text-align: center;
                    }
                    .player-image {
                        width: 500px;
                        height: auto;
                        margin-bottom: 10px;

                    }
                    .button {
                        display: inline-block;
                        padding: 10px 20px;
                        margin: 5px;
                        border: none;
                        border-radius: 5px;
                        cursor: pointer;
                    }
                    .sold {
                        background-color: #4CAF50;
                        color: white;
                    }
                    .unsold {
                        background-color: #f44336;
                        color: white;
                    }
                    .reset {
                        background-color: #1C6FB4;
                        color: white;
                    }
                    .responsive-img {
                            max-width: 100%;
                            height: auto;
                        }
                    p {
                        font-family: Arial, sans-serif;
                    }
                    .large-title {
                        font-size: 50px;
                        font-weight: bold;
                        font-family: "Lucida Console", "Courier New", monospace;
                        color: #E0F2F6;
                    }
                    /* Applying font size */
                    .large-text {
                        font-size: 40px;
                        color: #E0F2F6;
                    }

                    /* Applying font weight */
                    .bold-text {
                        font-weight: bold;
                    }

                    /* Applying font style (italic) */
                    .italic-text {
                        font-style: italic;
                    }
                    h2 {
                        font-family: Arial, sans-serif;
                    }

                </style>
            </head>
            <body>
                <div class="header">

                    <div class="logo_right" style="float: right;">
                            <!--<img t-att-src="image_data_uri(env['auction.tournament'].search([('active', '=', True)], limit=1).logo)" style="max-height:120pt;max-width:90%" alt="Team Image"/>-->
                           <!-- <img t-att-src="'/path/to/your/right_logo.png'" style="width: 100px; height: auto;"/>-->
                        </div>

                    </div>
                <div style="text-align: center; margin-top: 300px;" >
                    <h1 style="font-size: 48px; color: #DE0707;">Welcome to <span t-esc="env['auction.tournament'].sudo().search([('active', '=', True)], limit=1).name"/>-<span t-esc="env['auction.tournament'].sudo().search([('active', '=', True)], limit=1).description"/> auction!</h1>

                    <h2 t-if="tournament.venue" style="font-size: 40px; color: #DE0707;">Venue: <span t-esc="env['auction.tournament'].search([('active', '=', True)], limit=1).venue"/> </h2>
                    <h1 style="font-size: 48px; color: #DE0707;">Auction will start soon!</h1>
                </div>

            </body>
            <div style="page-break-after: always;"></div>
        </html>
</template>

<template id="auction_completed_message" name="Auction Completed">
    <t t-name="module_name.template_auction_completed">
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&amp;display=swap');

            /* Overall Page Background */
            body {
                background: linear-gradient(to bottom, #e0f7fa, #b2ebf2); /* Light blue gradient background */
                margin: 0;
                padding: 0;
                font-family: 'Roboto', sans-serif;
            }

            .message-container {
                width: 80%;
                margin: 0 auto;
                padding: 30px;
                text-align: center;
                border-radius: 12px;
                background: linear-gradient(to right, #ff7e5f, #feb47b); /* Vibrant gradient background */
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
                color: #fff;
                position: relative; /* Ensure logo can be positioned absolutely */
            }
            .tournament-logo {
                position: absolute;
                top: 10px;
                right: 10px;
                max-height: 80px;
            }
            .message-header {
                font-size: 36px;
                font-weight: 700;
                margin-bottom: 20px;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            }
            .message-body {
                font-size: 24px;
                font-weight: 400;
                line-height: 1.5;
                margin-bottom: 30px;
            }
            .thank-you-note {
                font-size: 20px;
                font-weight: 400;
                color: #ffffff;
                background: rgba(0, 0, 0, 0.2);
                padding: 10px 20px;
                border-radius: 8px;
                display: inline-block;
            }
        </style>

        <div class="message-container">
            <!-- Tournament Logo -->
            <img t-att-src="image_data_uri(env['auction.tournament'].search([('active', '=', True)], limit=1).logo)" alt="Tournament Logo" class="tournament-logo"/>

            <div class="message-header">The Auction Completed</div>
            <div class="message-body">
                Thanking you all. All the best for the matches!
            </div>
            <div class="thank-you-note">Thank you for participating!</div>
        </div>
    </t>
</template>

<!-- welcome_message_template moved to auction_welcome_template.xml -->



<!-- ══════════════════════════════════════════════════════════════════════
     PUBLIC LIVE AUCTION BOARD  (/auction/live-board)
     IPL-grade public page — themed, auto-refreshing, responsive
     ══════════════════════════════════════════════════════════════════════ -->
<template id="public_live_board_template" name="Public Live Auction Board">
<t t-set="theme_val" t-value="theme or 'vanilla'"/>
<html t-att-data-theme="theme_val"
      t-att-data-sport="(tournament.tournament_type if tournament else 'cricket') or 'cricket'"
      t-att-data-live-data-url="'/' + (db_name or '') + '/' + (tournament_slug or '') + '/auction/live-board/data'">
<head>
<meta charset="UTF-8"/>
  <link rel="shortcut icon" t-att-href="'/web/image/res.company/%d/favicon' % res_company.id" type="image/x-icon"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Live Auction Board</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="crossorigin"/>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bebas+Neue&amp;family=Oswald:wght@400;500;600;700&amp;family=Rajdhani:wght@400;500;600;700&amp;family=Teko:wght@500;600&amp;display=swap"/>
<style>
/* ── THEME VARIABLES ─────────────────────────────────────────────── */
:root {
  --lb-bg: #070c18;
  --lb-header-from: #0b1120;
  --lb-header-to: #162048;
  --lb-accent: #3b72e8;
  --lb-accent2: #5d93ff;
  --lb-glow: rgba(59,114,232,0.35);
  --lb-live-dot: #ef4444;
  --lb-sold-color: #22c55e;
  --lb-card: rgba(255,255,255,0.045);
  --lb-card-border: rgba(255,255,255,0.08);
  --lb-text: #e8edf8;
  --lb-sub: #8896b3;
  --lb-panel: #0d1526;
  --lb-sep: rgba(255,255,255,0.06);
  --lb-bid-from: #0d1e45;
  --lb-bid-to: #162d6e;
  --lb-sold-from: #052e16;
  --lb-sold-to: #14532d;
}
[data-theme="cherry"] {
  --lb-bg: #110206;
  --lb-header-from: #130408;
  --lb-header-to: #5a0a0a;
  --lb-accent: #e02626;
  --lb-accent2: #f87171;
  --lb-glow: rgba(220,38,38,0.38);
  --lb-live-dot: #f97316;
  --lb-card: rgba(255,255,255,0.04);
  --lb-card-border: rgba(220,38,38,0.15);
  --lb-text: #fef0f0;
  --lb-sub: #f0948a;
  --lb-panel: #180306;
  --lb-sep: rgba(220,38,38,0.12);
  --lb-bid-from: #3b0a0a;
  --lb-bid-to: #7f1d1d;
}
[data-theme="butterscotch"] {
  --lb-bg: #0e0800;
  --lb-header-from: #120900;
  --lb-header-to: #5c2d00;
  --lb-accent: #d97706;
  --lb-accent2: #fbbf24;
  --lb-glow: rgba(217,119,6,0.38);
  --lb-live-dot: #ef4444;
  --lb-card: rgba(255,255,255,0.04);
  --lb-card-border: rgba(217,119,6,0.18);
  --lb-text: #fef3c7;
  --lb-sub: #d4a843;
  --lb-panel: #150b00;
  --lb-sep: rgba(217,119,6,0.12);
  --lb-bid-from: #3b1e00;
  --lb-bid-to: #78350f;
}
[data-theme="strawberry"] {
  --lb-bg: #100009;
  --lb-header-from: #13000b;
  --lb-header-to: #5c0830;
  --lb-accent: #db2777;
  --lb-accent2: #f472b6;
  --lb-glow: rgba(219,39,119,0.38);
  --lb-live-dot: #f97316;
  --lb-card: rgba(255,255,255,0.04);
  --lb-card-border: rgba(219,39,119,0.18);
  --lb-text: #fce7f3;
  --lb-sub: #e879a0;
  --lb-panel: #16000d;
  --lb-sep: rgba(219,39,119,0.12);
  --lb-bid-from: #3b0021;
  --lb-bid-to: #831843;
}

[data-theme="blackberry"] {
  --lb-bg: #050a14;
  --lb-header-from: #030812;
  --lb-header-to: #0d1830;
  --lb-accent: #3B82F6;
  --lb-accent2: #93C5FD;
  --lb-glow: rgba(59,130,246,0.40);
  --lb-live-dot: #93C5FD;
  --lb-card: rgba(59,130,246,0.05);
  --lb-card-border: rgba(59,130,246,0.18);
  --lb-text: #ffffff;
  --lb-sub: #93c5fd;
  --lb-panel: #0a1220;
  --lb-sep: rgba(59,130,246,0.14);
  --lb-bid-from: #0a1628;
  --lb-bid-to: #1e3a8a;
  --lb-sold-from: #0a1628;
  --lb-sold-to: #1e3a8a;
}

[data-theme="pistah"] {
  --lb-bg: #060e08;
  --lb-header-from: #030a04;
  --lb-header-to: #0d1f11;
  --lb-accent: #6BBF4E;
  --lb-accent2: #A8E063;
  --lb-glow: rgba(107,191,78,0.38);
  --lb-live-dot: #A8E063;
  --lb-card: rgba(107,191,78,0.04);
  --lb-card-border: rgba(107,191,78,0.15);
  --lb-text: #e8f5e9;
  --lb-sub: #90c87a;
  --lb-panel: #0b1a0e;
  --lb-sep: rgba(107,191,78,0.12);
  --lb-bid-from: #0d2e10;
  --lb-bid-to: #1b5e20;
  --lb-sold-from: #0d2e10;
  --lb-sold-to: #1b5e20;
}

[data-theme="lemon"] {
  --lb-bg: #12100a;
  --lb-header-from: #1a1608;
  --lb-header-to: #3a3208;
  --lb-accent: #E8C200;
  --lb-accent2: #F5D84A;
  --lb-glow: rgba(232,194,0,0.38);
  --lb-live-dot: #F5D84A;
  --lb-card: rgba(255,255,255,0.04);
  --lb-card-border: rgba(232,194,0,0.18);
  --lb-text: #fffdf0;
  --lb-sub: #c9b86a;
  --lb-panel: #18140a;
  --lb-sep: rgba(232,194,0,0.12);
  --lb-bid-from: #3a3208;
  --lb-bid-to: #8a7000;
  --lb-sold-from: #3a3208;
  --lb-sold-to: #8a7000;
}

/* ── RESET &amp; BASE ──────────────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
body {
  background: var(--lb-bg);
  color: var(--lb-text);
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  min-height: 100vh;
  overflow-x: hidden;
}
img { display: block; }

/* ── HEADER ──────────────────────────────────────────────────────── */
.lb-header {
  background: linear-gradient(135deg, var(--lb-header-from) 0%, var(--lb-header-to) 100%);
  border-bottom: 1px solid var(--lb-card-border);
  padding: 14px 28px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  position: sticky; top: 0; z-index: 100;
  box-shadow: 0 4px 24px rgba(0,0,0,0.4);
}
.lb-hdr-left {
  display: flex; align-items: center; gap: 14px; min-width: 0;
}
.lb-t-logo {
  width: 52px; height: 52px;
  border-radius: 10px; overflow: hidden; flex-shrink: 0;
  border: 2px solid var(--lb-card-border);
  background: rgba(255,255,255,0.06);
  display: flex; align-items: center; justify-content: center;
}
.lb-t-logo img { width: 100%; height: 100%; object-fit: contain; }
.lb-t-name { font-size: 1.15rem; font-weight: 800; color: var(--lb-text); letter-spacing: .3px; }
.lb-t-desc { font-size: .72rem; color: var(--lb-sub); margin-top: 2px; }
.lb-hdr-center { flex: 1; display: flex; justify-content: center; }
.lb-live-badge {
  display: flex; align-items: center; gap: 8px;
  background: rgba(239,68,68,0.15);
  border: 1.5px solid rgba(239,68,68,0.4);
  border-radius: 30px;
  padding: 6px 16px;
  font-size: .8rem; font-weight: 800; letter-spacing: 2px;
  text-transform: uppercase;
  color: #ff5757;
}
.lb-live-dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--lb-live-dot);
  animation: blink 1.2s ease-in-out infinite;
}
@keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.2} }
.lb-hdr-right {
  display: flex; align-items: center; gap: 18px; flex-shrink: 0;
}
.lb-clock { font-size: .88rem; font-weight: 600; color: var(--lb-sub); font-variant-numeric: tabular-nums; }

/* ── MAIN GRID ────────────────────────────────────────────────────── */
.lb-main {
  display: grid;
  grid-template-columns: 360px 1fr;
  gap: 20px;
  padding: 20px 24px;
  max-width: 1400px;
  margin: 0 auto;
}

/* ── PLAYER PANEL ─────────────────────────────────────────────────── */
.lb-player-panel {
  display: flex; flex-direction: column; gap: 0;
  background: var(--lb-card);
  border: 1px solid var(--lb-card-border);
  border-radius: 20px;
  overflow: hidden;
}
.lb-photo-wrap {
  position: relative;
  width: 100%;
  aspect-ratio: 3/4;
  background: linear-gradient(180deg, var(--lb-panel) 0%, rgba(0,0,0,0.7) 100%);
  overflow: hidden;
}
#lb-player-img {
  width: 100%; height: 100%; object-fit: contain; object-position: center;
  transition: opacity .25s ease;
}
.lb-photo-placeholder {
  width: 100%; height: 100%;
  display: flex; align-items: center; justify-content: center;
  font-size: 6rem; opacity: .18;
  flex-direction: column;
  gap: 12px;
  box-sizing: border-box;
  padding: 16px;
  text-align: center;
}
.lb-photo-placeholder.lb-photo-loading {
  opacity: 1;
  font-size: 1rem;
  color: rgba(255,255,255,.88);
  gap: 18px;
  background:
    radial-gradient(ellipse 70% 55% at 50% 42%, rgba(0,217,255,.12), transparent 65%),
    radial-gradient(ellipse 50% 40% at 50% 70%, rgba(109,40,217,.14), transparent 70%),
    rgba(4,8,20,.55);
  backdrop-filter: blur(2px);
  -webkit-backdrop-filter: blur(2px);
}
.lb-photo-loader {
  position: relative;
  width: 72px;
  height: 72px;
  flex-shrink: 0;
}
.lb-photo-loader-ring {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 2px solid transparent;
  border-top-color: #00d9ff;
  border-right-color: rgba(0,217,255,.35);
  box-shadow: 0 0 18px rgba(0,217,255,.25);
  animation: lbPhotoOrbit 1.1s cubic-bezier(.5,.15,.5,.85) infinite;
}
.lb-photo-loader-ring--outer {
  inset: -6px;
  border-top-color: #f4c542;
  border-right-color: rgba(244,197,66,.3);
  border-width: 1.5px;
  box-shadow: 0 0 14px rgba(244,197,66,.2);
  animation-duration: 1.7s;
  animation-direction: reverse;
  opacity: .85;
}
.lb-photo-loader-core {
  position: absolute;
  inset: 18px;
  border-radius: 50%;
  background:
    radial-gradient(circle at 35% 30%, rgba(255,255,255,.35), transparent 45%),
    linear-gradient(145deg, rgba(0,217,255,.35), rgba(109,40,217,.45));
  box-shadow:
    0 0 20px rgba(0,217,255,.35),
    inset 0 1px 0 rgba(255,255,255,.25);
  animation: lbPhotoPulse 1.4s ease-in-out infinite;
}
.lb-photo-loader-core::after {
  content: '';
  position: absolute;
  inset: 6px;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,.18);
}
.lb-photo-loading-copy {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
}
.lb-photo-loading-txt {
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .16em;
  text-transform: uppercase;
  opacity: .92;
  max-width: 14rem;
  line-height: 1.4;
  text-shadow: 0 2px 12px rgba(0,0,0,.45);
}
.lb-photo-loading-dots {
  display: inline-flex;
  gap: 6px;
  align-items: center;
}
.lb-photo-loading-dots span {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: #00d9ff;
  box-shadow: 0 0 8px rgba(0,217,255,.65);
  animation: lbPhotoDot 1.2s ease-in-out infinite;
  opacity: .35;
}
.lb-photo-loading-dots span:nth-child(2) {
  animation-delay: .15s;
  background: #a78bfa;
  box-shadow: 0 0 8px rgba(167,139,250,.55);
}
.lb-photo-loading-dots span:nth-child(3) {
  animation-delay: .3s;
  background: #f4c542;
  box-shadow: 0 0 8px rgba(244,197,66,.55);
}
@keyframes lbPhotoOrbit {
  to { transform: rotate(360deg); }
}
@keyframes lbPhotoPulse {
  0%, 100% { transform: scale(.92); opacity: .75; }
  50% { transform: scale(1); opacity: 1; }
}
@keyframes lbPhotoDot {
  0%, 80%, 100% { transform: scale(.7); opacity: .3; }
  40% { transform: scale(1.15); opacity: 1; }
}
html[data-theme] .lb-photo-placeholder.lb-photo-loading {
  background:
    radial-gradient(ellipse 70% 55% at 50% 42%, rgba(0,217,255,.16), transparent 65%),
    radial-gradient(ellipse 50% 40% at 50% 70%, rgba(109,40,217,.18), transparent 70%),
    rgba(6,10,22,.62);
}
html[data-theme] .lb-photo-loader-ring {
  border-top-color: var(--pr-cyan, #00d9ff);
  border-right-color: color-mix(in srgb, var(--pr-cyan, #00d9ff) 35%, transparent);
  box-shadow: 0 0 18px color-mix(in srgb, var(--pr-cyan, #00d9ff) 35%, transparent);
}
html[data-theme] .lb-photo-loader-ring--outer {
  border-top-color: var(--pr-gold, #f4c542);
  border-right-color: color-mix(in srgb, var(--pr-gold, #f4c542) 30%, transparent);
  box-shadow: 0 0 14px color-mix(in srgb, var(--pr-gold, #f4c542) 25%, transparent);
}
html[data-theme] .lb-photo-loading-txt {
  color: rgba(255,255,255,.9);
  letter-spacing: .18em;
}
/* Mystery locked — large lock above PLAYER LOCKED (same as projector) */
.lb-mystery-lock{
  position:absolute;inset:0;z-index:8;pointer-events:none;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:10px;padding:16px;text-align:center;color:#fff;
  background:rgba(0,0,0,.42)}
.lb-mystery-lock-ico{
  width:clamp(56px,8vw,100px);height:clamp(56px,8vw,100px);
  display:flex;align-items:center;justify-content:center;margin-bottom:4px;
  filter:drop-shadow(0 4px 14px rgba(0,0,0,.55))}
.lb-mystery-lock-ico svg{width:100%;height:100%;display:block}
.lb-mystery-lock-title{
  font-size:clamp(.95rem,2.2vw,1.5rem);font-weight:800;letter-spacing:3px;
  text-shadow:0 2px 10px rgba(0,0,0,.55)}
.lb-mystery-lock-sub{opacity:.9;font-size:clamp(.75rem,1.3vw,.95rem);font-weight:600}
.lb-sl-overlay {
  position: absolute; top: 12px; left: 12px;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(6px);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 8px; padding: 4px 10px;
  font-size: .72rem; font-weight: 800; color: #fff; letter-spacing: .5px;
}
.lb-state-overlay {
  position: absolute; top: 12px; right: 12px;
  border-radius: 8px; padding: 5px 12px;
  font-size: .68rem; font-weight: 900; letter-spacing: 1.5px; text-transform: uppercase;
}
.lb-state-auction { background: rgba(245,158,11,0.9); color: #fff; animation: state-pulse 2s ease-in-out infinite; }
.lb-state-sold     { background: rgba(22,163,74,0.9); color: #fff; }
.lb-state-unsold   { background: rgba(220,38,38,0.9); color: #fff; }
@keyframes state-pulse { 0%,100%{opacity:1} 50%{opacity:.7} }

/* ── SOLD / UNSOLD STAMP OVERLAY ─────────────────────────────────── */
.lb-stamp-overlay {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 10;
  animation: stampIn .35s cubic-bezier(.34,1.56,.64,1) both;
}
@keyframes stampIn {
  from { transform: scale(1.6) rotate(-8deg); opacity: 0; }
  to   { transform: scale(1)   rotate(-8deg); opacity: 1; }
}
/* dark tint behind stamp */
.lb-stamp-overlay::before {
  content: '';
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.55);
}
.lb-stamp-box {
  position: relative;
  transform: rotate(-8deg);
  text-align: center;
  padding: 14px 28px;
  border-radius: 10px;
  border-width: 5px;
  border-style: solid;
  box-shadow: 0 0 0 2px rgba(255,255,255,0.15);
}
/* SOLD stamp */
.lb-stamp-sold .lb-stamp-box {
  background: rgba(15,50,15,0.88);
  border-color: #22c55e;
}
.lb-stamp-sold .lb-stamp-hammer {
  font-size: 2rem; line-height: 1; margin-bottom: 4px;
  animation: hammerSwing .5s cubic-bezier(.36,.07,.19,.97) both;
  transform-origin: bottom right;
}
@keyframes hammerSwing {
  0%   { transform: rotate(-45deg); }
  60%  { transform: rotate(12deg); }
  80%  { transform: rotate(-8deg); }
  100% { transform: rotate(0deg); }
}
.lb-stamp-sold .lb-stamp-word {
  font-size: 2.6rem; font-weight: 900; letter-spacing: 8px;
  color: #22c55e; text-transform: uppercase;
  text-shadow: 0 0 18px rgba(34,197,94,0.7);
  line-height: 1;
}
.lb-stamp-sold .lb-stamp-pts {
  font-size: 1.05rem; font-weight: 800; color: #86efac;
  margin-top: 5px; letter-spacing: 1px;
}
.lb-stamp-sold .lb-stamp-team {
  display: flex; align-items: center; justify-content: center; gap: 9px;
  margin-top: 8px;
}
.lb-stamp-sold .lb-stamp-team-logo {
  width: 32px; height: 32px; border-radius: 6px; overflow: hidden;
  background: rgba(255,255,255,0.1);
  display: flex; align-items: center; justify-content: center;
}
.lb-stamp-sold .lb-stamp-team-logo img { width: 100%; height: 100%; object-fit: contain; }
.lb-stamp-sold .lb-stamp-team-name {
  font-size: .88rem; font-weight: 800; color: #fff; letter-spacing: .5px;
}
/* UNSOLD stamp */
.lb-stamp-unsold .lb-stamp-box {
  background: rgba(50,10,10,0.88);
  border-color: #ef4444;
}
.lb-stamp-unsold .lb-stamp-word {
  font-size: 2.4rem; font-weight: 900; letter-spacing: 6px;
  color: #ef4444; text-transform: uppercase;
  text-shadow: 0 0 18px rgba(239,68,68,0.7);
  line-height: 1;
}
.lb-stamp-unsold .lb-stamp-sub {
  font-size: .76rem; font-weight: 700; color: #fca5a5;
  margin-top: 6px; letter-spacing: 1.5px; text-transform: uppercase;
}
.lb-photo-gradient {
  position: absolute; bottom: 0; left: 0; right: 0;
  height: 120px;
  background: linear-gradient(to top, var(--lb-panel), transparent);
}
.lb-photo-name {
  position: absolute; bottom: 14px; left: 14px; right: 14px;
}
.lb-player-name-big {
  font-size: 1.3rem; font-weight: 900; color: #fff;
  text-shadow: 0 2px 8px rgba(0,0,0,0.7);
  line-height: 1.15;
}
.lb-player-sub { font-size: .72rem; color: rgba(255,255,255,0.65); margin-top: 3px; }

.lb-player-meta {
  padding: 14px 16px 16px;
  display: flex; flex-direction: column; gap: 10px;
}
.lb-badges-row { display: flex; gap: 7px; flex-wrap: wrap; }
.lb-badge {
  font-size: .62rem; font-weight: 800; padding: 3px 10px; border-radius: 20px;
  text-transform: uppercase; letter-spacing: .6px;
}
.lb-b-tier   { color: #fff; /* bg set inline */ }
.lb-b-role   { background: var(--lb-accent); color: #fff; }
.lb-b-icon   { background: linear-gradient(135deg,#d97706,#fbbf24); color: #fff; }
.lb-b-dom    { background: #0891b2; color: #fff; }
.lb-b-for    { background: #059669; color: #fff; }
.lb-b-ico    { background: #7c3aed; color: #fff; }
.lb-stats-row {
  display: flex; gap: 8px; flex-wrap: wrap;
}
.lb-stat-chip {
  flex: 1; background: rgba(255,255,255,0.05); border: 1px solid var(--lb-sep);
  border-radius: 10px; padding: 8px 6px; text-align: center;
}
.lb-stat-lbl { font-size: .52rem; font-weight: 700; letter-spacing: .8px; text-transform: uppercase; color: var(--lb-sub); margin-bottom: 3px; }
.lb-stat-val { font-size: .8rem; font-weight: 700; color: var(--lb-text); }
.lb-tag-chip {
  display: inline-flex; align-items: center; gap: 4px;
  background: rgba(255,255,255,0.06); border: 1px solid var(--lb-sep);
  border-radius: 999px; padding: 5px 11px;
  font-size: .62rem; font-weight: 700; letter-spacing: .5px;
  text-transform: uppercase; color: var(--lb-text);
}
.lb-base-row {
  display: flex; align-items: center; justify-content: space-between;
  background: rgba(255,255,255,0.04); border-radius: 10px; padding: 10px 14px;
  border: 1px solid var(--lb-sep);
}
.lb-base-lbl { font-size: .65rem; font-weight: 700; letter-spacing: .8px; text-transform: uppercase; color: var(--lb-sub); }
.lb-base-val { font-size: 1rem; font-weight: 900; color: var(--lb-accent2); }

/* ── BID PANEL ───────────────────────────────────────────────────── */
.lb-bid-panel {
  display: flex; flex-direction: column; gap: 16px;
}

/* Current bid / sold card */
.lb-status-card {
  border-radius: 20px; overflow: hidden;
  border: 1px solid var(--lb-card-border);
  min-height: 200px;
  display: flex; flex-direction: column; justify-content: center;
}
.lb-status-auction {
  background: linear-gradient(135deg, var(--lb-bid-from) 0%, var(--lb-bid-to) 100%);
  padding: 28px;
}
.lb-status-sold {
  background: linear-gradient(135deg, var(--lb-sold-from) 0%, var(--lb-sold-to) 100%);
  padding: 28px;
}
.lb-status-waiting {
  background: var(--lb-card);
  padding: 28px; display: flex; align-items: center; justify-content: center; flex-direction: column; gap: 12px;
}
.lb-status-label {
  font-size: .65rem; font-weight: 900; letter-spacing: 2.5px; text-transform: uppercase;
  opacity: .7; margin-bottom: 6px;
}
.lb-sold-trophy { font-size: 3rem; text-align: center; margin-bottom: 6px; }
.lb-sold-amount {
  font-size: 2.6rem; font-weight: 900; color: var(--lb-sold-color);
  text-align: center; letter-spacing: -1px;
  text-shadow: 0 0 30px rgba(34,197,94,0.4);
}
.lb-sold-pts { font-size: .9rem; font-weight: 600; opacity: .7; }
.lb-sold-team {
  display: flex; align-items: center; justify-content: center; gap: 14px;
  margin-top: 12px;
}
.lb-sold-team-logo {
  width: 56px; height: 56px; border-radius: 12px; overflow: hidden;
  border: 2px solid rgba(255,255,255,0.2);
  background: rgba(255,255,255,0.08);
  display: flex; align-items: center; justify-content: center;
}
.lb-sold-team-logo img { width: 100%; height: 100%; object-fit: contain; }
.lb-sold-team-name { font-size: 1.3rem; font-weight: 900; color: #fff; }
.lb-sold-team-mgr { font-size: .7rem; color: rgba(255,255,255,.55); margin-top: 3px; }
.lb-auction-pulse {
  font-size: .8rem; color: var(--lb-sub); text-align: center;
  display: flex; align-items: center; justify-content: center; gap: 10px;
}
.lb-auction-spinner {
  width: 14px; height: 14px; border: 2px solid var(--lb-card-border);
  border-top-color: var(--lb-accent); border-radius: 50%;
  animation: spin .8s linear infinite;
}
@keyframes spin { to{transform:rotate(360deg)} }

/* ── HISTORY CARD ────────────────────────────────────────────────── */
.lb-history-card {
  background: var(--lb-card);
  border: 1px solid var(--lb-card-border);
  border-radius: 20px;
  padding: 18px 18px 12px;
  flex: 1;
}
.lb-section-title {
  font-size: .62rem; font-weight: 900; letter-spacing: 2px; text-transform: uppercase;
  color: var(--lb-accent); margin-bottom: 14px;
  display: flex; align-items: center; gap: 8px;
}
.lb-section-title::after {
  content: ''; flex: 1; height: 1px; background: var(--lb-sep);
}
.lb-history-item {
  display: flex; align-items: center; gap: 12px;
  padding: 10px 0;
  border-bottom: 1px solid var(--lb-sep);
}
.lb-history-item:last-child { border-bottom: none; }
.lb-history-avatar {
  width: 38px; height: 38px; border-radius: 8px; overflow: hidden; flex-shrink: 0;
  background: rgba(255,255,255,0.08);
  display: flex; align-items: center; justify-content: center;
}
.lb-history-avatar img { width: 100%; height: 100%; object-fit: cover; }
.lb-history-avatar-ph { font-size: 1rem; opacity: .5; }
.lb-history-msg { flex: 1; font-size: .75rem; color: var(--lb-text); line-height: 1.4; }
.lb-history-time { font-size: .65rem; color: var(--lb-sub); flex-shrink: 0; }
.lb-history-empty { text-align: center; color: var(--lb-sub); font-size: .8rem; padding: 20px 0; }

/* ── TEAMS STRIP ─────────────────────────────────────────────────── */
.lb-teams-wrap {
  padding: 0 24px 24px;
  max-width: 1400px;
  margin: 0 auto;
}
.lb-teams-card {
  background: var(--lb-card);
  border: 1px solid var(--lb-card-border);
  border-radius: 20px;
  padding: 18px 20px;
}
.lb-teams-grid {
  display: flex; flex-wrap: wrap; gap: 12px; margin-top: 4px;
}
.lb-team-chip {
  display: flex; align-items: center; gap: 10px;
  background: rgba(255,255,255,0.04);
  border: 1px solid var(--lb-sep);
  border-radius: 12px;
  padding: 8px 14px 8px 8px;
  min-width: 160px;
}
.lb-team-chip-logo {
  width: 36px; height: 36px; border-radius: 8px; overflow: hidden; flex-shrink: 0;
  background: rgba(255,255,255,0.06);
  display: flex; align-items: center; justify-content: center;
}
.lb-team-chip-logo img { width: 100%; height: 100%; object-fit: contain; }
.lb-team-chip-logo-ph { font-size: 1.1rem; opacity: .5; }
.lb-team-chip-name { font-size: .8rem; font-weight: 700; color: var(--lb-text); }
.lb-team-chip-pts  { font-size: .62rem; color: var(--lb-sub); margin-top: 1px; }

/* ── NO AUCTION STATE ────────────────────────────────────────────── */
.lb-waiting-full {
  min-height: 50vh;
  display: flex; align-items: center; justify-content: center; flex-direction: column; gap: 16px;
  color: var(--lb-sub); text-align: center; padding: 40px;
}
.lb-waiting-icon { font-size: 4rem; opacity: .3; }
.lb-waiting-text { font-size: 1.1rem; font-weight: 600; }

/* ── MVP / TOP BUYS CARD ─────────────────────────────────────────── */
.lb-mvp-card {
  background: var(--lb-card);
  border: 1px solid var(--lb-card-border);
  border-radius: 20px;
  padding: 18px 18px 12px;
  flex-shrink: 0;
}
.lb-mvp-item {
  display: flex; align-items: center; gap: 10px;
  padding: 7px 6px;
  border-bottom: 1px solid var(--lb-sep);
  border-radius: 8px;
}
.lb-mvp-item:last-child { border-bottom: none; }
.lb-mvp-item-1 {
  background: linear-gradient(90deg, rgba(251,191,36,0.10) 0%, transparent 80%);
}
.lb-mvp-rank {
  flex-shrink: 0; width: 22px; text-align: center;
  font-size: .85rem; font-weight: 900; line-height: 1;
}
.lb-mvp-rank-gold   { color: #fbbf24; }
.lb-mvp-rank-silver { color: #94a3b8; }
.lb-mvp-rank-bronze { color: #b45309; }
.lb-mvp-rank-num    { color: var(--lb-sub); font-size: .7rem; }
.lb-mvp-avatar {
  position: relative;
  width: 34px; height: 34px; border-radius: 8px; overflow: visible; flex-shrink: 0;
}
.lb-mvp-avatar-inner {
  width: 34px; height: 34px; border-radius: 8px; overflow: hidden;
  background: rgba(255,255,255,0.08);
  display: flex; align-items: center; justify-content: center;
}
.lb-mvp-avatar-inner img { width: 100%; height: 100%; object-fit: cover; object-position: top center; }
.lb-mvp-avatar-ph { font-size: .95rem; opacity: .5; }
.lb-mvp-crown {
  position: absolute; top: -9px; left: 50%; transform: translateX(-50%);
  font-size: .65rem; line-height: 1; pointer-events: none;
}
.lb-mvp-info { flex: 1; min-width: 0; }
.lb-mvp-name { font-size: .76rem; font-weight: 800; color: var(--lb-text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; text-transform: uppercase; }
.lb-mvp-sub  { font-size: .6rem; color: var(--lb-sub); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-top: 1px; }
.lb-mvp-right { flex-shrink: 0; text-align: right; }
.lb-mvp-pts   { font-size: .82rem; font-weight: 900; color: var(--lb-accent2); }
.lb-mvp-team  { display: flex; align-items: center; justify-content: flex-end; gap: 4px; margin-top: 2px; }
.lb-mvp-team-logo {
  width: 14px; height: 14px; border-radius: 3px; overflow: hidden; flex-shrink: 0;
  background: rgba(255,255,255,0.08);
  display: flex; align-items: center; justify-content: center;
}
.lb-mvp-team-logo img { width: 100%; height: 100%; object-fit: contain; }
.lb-mvp-team-name { font-size: .58rem; color: var(--lb-sub); text-transform: uppercase; }
.lb-mvp-empty { text-align: center; color: var(--lb-sub); font-size: .8rem; padding: 16px 0; }

/* ── FOOTER BAR ──────────────────────────────────────────────────── */
.lb-footer {
  text-align: center;
  padding: 10px 24px 18px;
  font-size: .65rem; color: var(--lb-sub);
  border-top: 1px solid var(--lb-sep);
}

/* ── BREAK TIME OVERLAY (IPL-style) ─────────────────────────────── */
#lb-break-overlay {
  display: none; position: fixed; inset: 0; z-index: 800;
  flex-direction: column; align-items: center; justify-content: center;
  text-align: center; overflow: hidden;
  background: radial-gradient(ellipse 150% 90% at 50% -5%, #0e2060 0%, #060c1e 45%, #020408 100%);
}
#lb-break-overlay.active { display: flex; animation: brk-fade-in .7s ease; }
@keyframes brk-fade-in { from{opacity:0} to{opacity:1} }

.lb-brk-glow {
  position: absolute; top: -25%; left: 50%; transform: translateX(-50%);
  width: min(1000px, 140vw); height: min(600px, 80vh); border-radius: 50%;
  background: radial-gradient(ellipse, rgba(255,193,7,.10) 0%, transparent 65%);
  animation: brk-glow-pulse 4s ease-in-out infinite; pointer-events: none;
}
@keyframes brk-glow-pulse { 0%,100%{opacity:.5;transform:translateX(-50%) scale(1)} 50%{opacity:1;transform:translateX(-50%) scale(1.14)} }

.lb-brk-scan {
  position: absolute; inset: 0; pointer-events: none;
  background: repeating-linear-gradient(0deg,transparent,transparent 3px,rgba(255,255,255,.012) 3px,rgba(255,255,255,.012) 4px);
}
.lb-brk-bar {
  position: absolute; left: 0; right: 0; height: 4px;
  background: linear-gradient(90deg,transparent,#FFD700 20%,#FFC200 50%,#FFD700 80%,transparent);
  box-shadow: 0 0 20px rgba(255,193,7,.7);
}
.lb-brk-bar-top { top: 0; }
.lb-brk-bar-bot { bottom: 0; }

.lb-brk-content {
  position: relative; z-index: 1;
  display: flex; flex-direction: column; align-items: center;
  gap: 10px; padding: 16px 20px; width: 100%; max-width: min(1100px, 96vw);
  max-height: calc(100vh - 24px); overflow-y: auto; box-sizing: border-box;
}
#lb-brk-logo img {
  width: 64px; height: 64px; object-fit: contain; border-radius: 12px;
  border: 2px solid rgba(255,193,7,.3); background: rgba(255,255,255,.04);
  padding: 6px; filter: drop-shadow(0 0 14px rgba(255,193,7,.4));
}
.lb-brk-logo-ph { font-size: 2.2rem; opacity: .3; }

.lb-brk-badge {
  display: inline-flex; align-items: center; gap: 9px;
  background: rgba(255,200,0,.1); border: 1.5px solid rgba(255,200,0,.4);
  border-radius: 30px; padding: 6px 18px;
  font-size: .72rem; font-weight: 900; letter-spacing: 3.5px;
  text-transform: uppercase; color: #FFD700;
}
.lb-brk-dot { width: 7px; height: 7px; border-radius: 50%; background: #FFD700; animation: blink 1.4s ease-in-out infinite; }

.lb-brk-headline { display: flex; flex-direction: column; align-items: center; line-height: 1; gap: 2px; }
.lb-brk-hl1 { font-size: clamp(1.5rem,4.5vw,2.8rem); font-weight: 900; letter-spacing: 5px; text-transform: uppercase; color: rgba(255,255,255,.65); }
.lb-brk-hl2 { font-size: clamp(2.2rem,7vw,4.8rem); font-weight: 900; letter-spacing: 4px; text-transform: uppercase; color: #FFD700; text-shadow: 0 0 30px rgba(255,200,0,.65),0 0 70px rgba(255,200,0,.25); }

.lb-brk-divider { width: 100px; height: 2px; border-radius: 2px; background: linear-gradient(90deg,transparent,#FFD700,transparent); }
.lb-brk-sub { font-size: clamp(.65rem,1.8vw,.82rem); color: rgba(255,255,255,.42); letter-spacing: 3px; text-transform: uppercase; }

.lb-brk-rings { position: relative; width: 76px; height: 76px; display: flex; align-items: center; justify-content: center; margin: 4px 0; }
.lb-brk-ring { position: absolute; border-radius: 50%; border: 2px solid rgba(255,200,0,.45); animation: brk-ring-out 3s ease-out infinite; }
.lb-brk-ring:nth-child(1) { width: 40px; height: 40px; animation-delay: 0s; }
.lb-brk-ring:nth-child(2) { width: 40px; height: 40px; animation-delay: 1s; }
.lb-brk-ring:nth-child(3) { width: 40px; height: 40px; animation-delay: 2s; }
@keyframes brk-ring-out { 0%{transform:scale(1);opacity:.85} 100%{transform:scale(3.4);opacity:0} }
.lb-brk-icon { font-size: 1.5rem; z-index: 1; }

.lb-brk-timer { font-size: .6rem; color: rgba(255,255,255,.28); letter-spacing: 2px; text-transform: uppercase; }

/* ── Advertiser pair slider (break screen) — 2 at a time, every 2s ── */
.lb-brk-ads { width: 100%; max-width: min(960px, 94vw); }
.lb-brk-ads-label {
  font-size: .52rem; font-weight: 800; letter-spacing: 2.5px;
  text-transform: uppercase; color: rgba(255,193,7,.45);
  margin-bottom: 10px; display: flex; align-items: center; gap: 10px;
}
.lb-brk-ads-label::before,.lb-brk-ads-label::after {
  content: ''; flex: 1; height: 1px;
  background: linear-gradient(90deg,transparent,rgba(255,193,7,.22),transparent);
}
.lb-brk-ads-viewport {
  width: 100%; overflow: hidden;
  background: transparent; border: none; border-radius: 0;
}
.lb-brk-ads-track {
  display: flex; align-items: stretch;
  transition: transform .55s cubic-bezier(.22,.9,.28,1);
  will-change: transform;
}
.lb-brk-ad-pair {
  flex: 0 0 100%; width: 100%; min-width: 100%;
  display: grid; grid-template-columns: 1fr 1fr; gap: clamp(16px,3vw,36px);
  align-items: center; justify-items: center;
  padding: 8px 4px; box-sizing: border-box; min-height: 150px;
}
.lb-brk-ad-pair-solo { grid-template-columns: 1fr; }
.lb-brk-ad-pair-solo .lb-brk-ad-cell { max-width: 480px; }
.lb-brk-ad-cell {
  width: 100%; max-width: 420px; height: clamp(110px,16vh,160px);
  display: flex; align-items: center; justify-content: center;
  padding: 0; border: none; border-radius: 0; background: transparent;
}
.lb-brk-ad-cell img {
  max-width: 100%; max-height: 100%; width: auto; height: auto;
  object-fit: contain; border-radius: 0;
  filter: drop-shadow(0 6px 18px rgba(0,0,0,.45));
}
.lb-brk-ad-name { font-size: 1.05rem; font-weight: 700; color: rgba(255,255,255,.7); text-align: center; }
.lb-brk-ad-dots { display: flex; gap: 6px; justify-content: center; flex-wrap: wrap; margin-top: 12px; }
.lb-brk-ad-dot { width: 6px; height: 6px; border-radius: 50%; background: rgba(255,193,7,.25); transition: background .3s, transform .3s; }
.lb-brk-ad-dot.active { background: #FFD700; transform: scale(1.25); }

/* ── Sponsor strip — FIXED viewport footer ──────────────────────── */
#lb-sponsor-strip.lb-sponsor-strip,
.lb-sponsor-strip {
  position: fixed !important;
  left: 0 !important; right: 0 !important; bottom: 0 !important;
  z-index: 250 !important;
  width: 100% !important;
  max-width: none !important;
  margin: 0 !important;
  flex-shrink: 0;
  border-top: 2px solid var(--lb-accent2);
  background: linear-gradient(180deg, var(--lb-panel) 0%, var(--lb-bg) 100%);
  box-shadow: 0 -8px 32px rgba(0,0,0,.55), 0 -1px 0 rgba(255,255,255,.06) inset;
  padding: .65rem 1.25rem .75rem;
  overflow: hidden;
}
.lb-spon-head { text-align: center; margin-bottom: .4rem; }
.lb-spon-label {
  font-size: .9rem; letter-spacing: .28em; text-transform: uppercase;
  font-weight: 900; color: var(--lb-accent2);
  text-shadow: 0 0 18px var(--lb-glow);
}
.lb-spon-row { display: block; width: 100%; max-width: 1400px; margin: 0 auto; }
.lb-spon-viewport { width: 100%; overflow: hidden; }
.lb-spon-track {
  display: flex; align-items: center; justify-content: center; flex-wrap: wrap;
  gap: 1.6rem 2.2rem; width: 100%; padding: .4rem 0; margin: 0;
}
.lb-spon-item { flex-shrink: 0; }
.lb-spon-card {
  height: clamp(52px, 7vw, 80px);
  display: flex; align-items: center; justify-content: center; padding: 0 .5rem;
}
.lb-spon-logo {
  height: 100%; width: auto; max-width: 180px; object-fit: contain;
  filter: drop-shadow(0 2px 8px rgba(0,0,0,.35));
}
.lb-spon-name { font-size: .95rem; font-weight: 700; color: var(--lb-sub); }
.lb-spon-empty {
  width: 100%; text-align: center; font-size: .9rem; color: var(--lb-sub); padding: .5rem 1rem;
}
<!-- Keep page content clear of the fixed sponsor bar (only when sponsors exist) -->
body.lb-has-sponsors { padding-bottom: 140px; }

@media (max-width: 600px) {
  .lb-brk-content { gap: 9px; padding: 16px; }
  .lb-brk-hl1 { letter-spacing: 3px; }
  .lb-brk-hl2 { letter-spacing: 2px; }
  .lb-brk-ad-pair { min-height: 120px; padding: 6px 2px; gap: 12px; }
  .lb-brk-ad-cell { height: clamp(90px,18vh,130px); max-width: none; }
  .lb-brk-ad-cell img { max-height: 100%; }

  /* Compact sponsor bar: 5 logos per row so it does not hide the board */
  .lb-sponsor-strip {
    padding: .28rem .45rem .32rem;
  }
  .lb-spon-head {
    margin-bottom: .12rem;
  }
  .lb-spon-label {
    font-size: .58rem;
    letter-spacing: .16em;
  }
  .lb-spon-track {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    align-items: center;
    justify-items: center;
    gap: .2rem .28rem;
    width: 100%;
    padding: .1rem 0;
    flex-wrap: unset;
  }
  .lb-spon-item {
    flex-shrink: unset;
    width: 100%;
    min-width: 0;
  }
  .lb-spon-card {
    height: 28px;
    width: 100%;
    padding: 0;
  }
  .lb-spon-logo {
    height: 100%;
    width: 100%;
    max-width: 100%;
    object-fit: contain;
  }
  .lb-spon-name {
    font-size: .55rem;
    line-height: 1.1;
    text-align: center;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    max-width: 100%;
  }
  body.lb-has-sponsors { padding-bottom: 58px; }
}

/* ── TEAM CHIP — pointer cursor ─────────────────────────────────── */
.lb-team-chip { position: relative; cursor: pointer; }

/* ── FLOATING SQUAD POPUP (desktop hover) ───────────────────────── */
#lb-team-floating-popup {
  position: fixed; z-index: 300; display: none;
  background: var(--lb-panel);
  border: 1px solid var(--lb-card-border);
  border-radius: 14px;
  padding: 12px 14px;
  min-width: 260px; max-width: 340px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.65);
  pointer-events: none;
}
.lb-tp-title {
  font-size: .62rem; font-weight: 800; text-transform: uppercase;
  letter-spacing: 1px; color: var(--lb-accent);
  padding-bottom: 6px; margin-bottom: 6px;
  border-bottom: 1px solid var(--lb-sep);
}
.lb-tp-empty { font-size: .72rem; color: var(--lb-sub); text-align: center; padding: 8px 0; }
.lb-tp-table { width: 100%; border-collapse: collapse; }
.lb-tp-table th {
  font-size: .56rem; font-weight: 800; color: var(--lb-sub);
  text-align: left; padding: 2px 6px 4px;
  letter-spacing: .8px; text-transform: uppercase;
  border-bottom: 1px solid var(--lb-sep);
}
.lb-tp-table td {
  padding: 5px 6px; font-size: .71rem; color: var(--lb-text);
  border-bottom: 1px solid rgba(255,255,255,0.04);
  vertical-align: middle;
}
.lb-tp-table tr:last-child td { border-bottom: none; }
.lb-tp-photo {
  width: 26px; height: 26px; border-radius: 5px; overflow: hidden;
  background: rgba(255,255,255,0.07);
  display: inline-flex; align-items: center; justify-content: center;
  vertical-align: middle; flex-shrink: 0;
}
.lb-tp-photo img { width: 100%; height: 100%; object-fit: cover; object-position: top; }
.lb-tp-ph { font-size: .72rem; opacity: .4; }
.lb-tp-pts { font-weight: 900; color: var(--lb-accent2); white-space: nowrap; }

/* ── TEAM PLAYERS MODAL (mobile / tap) ──────────────────────────── */
#lb-team-modal-overlay {
  display: none; position: fixed; inset: 0; z-index: 500;
  background: rgba(0,0,0,0.75); backdrop-filter: blur(4px);
  align-items: flex-end; justify-content: center; padding: 0;
}
#lb-team-modal-overlay.open { display: flex; }
.lb-tm-sheet {
  background: var(--lb-panel);
  border: 1px solid var(--lb-card-border);
  border-radius: 20px 20px 0 0;
  width: 100%; max-width: 520px;
  max-height: 80vh; overflow: hidden;
  display: flex; flex-direction: column;
}
.lb-tm-header {
  display: flex; align-items: center; gap: 12px;
  padding: 14px 16px 12px;
  border-bottom: 1px solid var(--lb-sep); flex-shrink: 0;
  background: linear-gradient(90deg, rgba(255,255,255,0.03) 0%, transparent 100%);
}
.lb-tm-hlogo {
  width: 38px; height: 38px; border-radius: 9px; overflow: hidden;
  background: rgba(255,255,255,0.06);
  display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  border: 1px solid var(--lb-card-border);
}
.lb-tm-hlogo img { width: 100%; height: 100%; object-fit: contain; }
.lb-tm-hname { font-size: .95rem; font-weight: 800; color: var(--lb-text); line-height: 1.2; }
.lb-tm-hpts  { font-size: .62rem; color: var(--lb-sub); margin-top: 2px; }
.lb-tm-hclose {
  margin-left: auto; width: 28px; height: 28px; border-radius: 50%;
  background: rgba(255,255,255,0.08); border: 1px solid var(--lb-sep);
  cursor: pointer; color: var(--lb-sub); font-size: .85rem;
  display: flex; align-items: center; justify-content: center; flex-shrink: 0;
}
.lb-tm-hclose:hover { background: rgba(255,255,255,0.18); color: var(--lb-text); }
.lb-tm-body { overflow-y: auto; flex: 1; }
.lb-tm-row {
  display: flex; align-items: center; gap: 12px;
  padding: 9px 16px; border-bottom: 1px solid var(--lb-sep);
}
.lb-tm-row:last-child { border-bottom: none; }
.lb-tm-rphoto {
  width: 42px; height: 42px; border-radius: 9px; overflow: hidden;
  background: rgba(255,255,255,0.06); flex-shrink: 0;
  display: flex; align-items: center; justify-content: center;
  border: 1px solid var(--lb-sep);
}
.lb-tm-rphoto img { width: 100%; height: 100%; object-fit: cover; object-position: top; }
.lb-tm-rph { font-size: 1.1rem; opacity: .35; }
.lb-tm-rinfo { flex: 1; min-width: 0; }
.lb-tm-rname { font-size: .83rem; font-weight: 700; color: var(--lb-text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.lb-tm-rrole { font-size: .6rem; color: var(--lb-sub); margin-top: 1px; display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }
.lb-role-tags { display: inline-flex; align-items: center; gap: 4px; vertical-align: middle; }
.lb-role-tag {
  display: inline-flex; align-items: center; justify-content: center;
  min-width: 28px; height: 18px; padding: 0 6px;
  border-radius: 4px; font-size: .55rem; font-weight: 800;
  letter-spacing: .5px; color: #fff; background: var(--lb-accent);
  line-height: 1; text-transform: uppercase;
}
.lb-role-tag.bowl { background: #c45c26; }
.lb-role-tag.bat  { background: #1a7a4c; }
.lb-role-tag.wk   { background: #6A1B9A; }
.lb-pos-badge {
  display: inline-flex; align-items: center; justify-content: center;
  min-width: 26px; height: 18px; padding: 0 5px;
  border-radius: 4px; font-size: .58rem; font-weight: 800;
  letter-spacing: .4px; color: #fff; background: var(--lb-accent);
  line-height: 1;
}
.lb-tm-rpts  { font-size: .88rem; font-weight: 900; color: var(--lb-accent2); flex-shrink: 0; }
.lb-tm-empty { text-align: center; color: var(--lb-sub); font-size: .82rem; padding: 28px 16px; }

/* ── RESPONSIVE ──────────────────────────────────────────────────── */
@media (max-width: 900px) {
  .lb-main { grid-template-columns: 1fr; padding: 14px 14px; }
  .lb-player-panel { max-width: 100%; }
  .lb-photo-wrap { aspect-ratio: 4/3; }
  .lb-header { padding: 12px 16px; }
  .lb-t-name { font-size: .95rem; }
}
@media (max-width: 600px) {
  .lb-header {
    flex-wrap: nowrap;
    align-items: center;
    gap: 6px;
    padding: 6px 10px;
    min-height: 0;
  }
  .lb-hdr-left {
    gap: 8px;
    flex: 1 1 auto;
    min-width: 0;
  }
  /* Keep tournament identity readable */
  .lb-t-logo {
    width: 40px;
    height: 40px;
    border-radius: 8px;
  }
  .lb-t-name {
    font-size: .95rem;
    line-height: 1.15;
  }
  .lb-t-desc {
    font-size: .62rem;
    margin-top: 1px;
    line-height: 1.2;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 46vw;
  }
  /* Shrink secondary header chrome */
  .lb-hdr-center {
    order: 0;
    width: auto;
    flex: 0 0 auto;
    flex-direction: row;
    align-items: center;
    justify-content: flex-end;
    gap: 4px;
  }
  .lb-live-badge {
    gap: 4px;
    padding: 2px 7px;
    font-size: .52rem;
    letter-spacing: .8px;
    border-radius: 4px;
    border-width: 1px;
  }
  .lb-live-dot {
    width: 5px;
    height: 5px;
  }
  .lb-hdr-status {
    display: none;
  }
  .lb-hdr-right {
    gap: 6px;
  }
  .lb-powered-by {
    font-size: .36rem;
    letter-spacing: .8px;
  }
  .lb-powered-logo {
    height: 14px;
    margin-top: 0;
  }
  .lb-clock {
    font-size: .62rem;
    letter-spacing: .5px;
  }
  .lb-sold-amount { font-size: 2rem; }
  .lb-teams-wrap { padding: 0 14px 14px; }
}

/* ═══════════════════════════════════════════════════════════════════
   PREMIUM BROADCAST REVAMP — visual-only override layer.
   Layered AFTER the base rules so it wins the cascade without editing
   any existing selector, ID, class, markup structure or JS behaviour.
   Uses the existing themed CSS variables (--lb-accent, --lb-glow, …).
   ═══════════════════════════════════════════════════════════════════ */

/* Stadium lighting — brighter, warmer, blended (less black) */
body::before {
  content: ''; position: fixed; inset: 0; z-index: -2; pointer-events: none;
  background:
    radial-gradient(120% 85% at 50% -10%, rgba(255,255,255,0.16) 0%, transparent 52%),
    radial-gradient(95% 65% at 6% 2%, var(--lb-glow) 0%, transparent 50%),
    radial-gradient(95% 65% at 94% 2%, var(--lb-glow) 0%, transparent 50%),
    radial-gradient(150% 130% at 50% 118%, rgba(255,255,255,0.08) 0%, transparent 58%),
    var(--lb-bg);
}
body::after {
  content: ''; position: fixed; inset: 0; z-index: -1; pointer-events: none;
  background: radial-gradient(135% 125% at 50% 34%, transparent 78%, rgba(0,0,0,0.14) 100%);
}
/* Warmer, lighter base wash — per theme (all 5) */
[data-theme="vanilla"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(180,205,255,0.26) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(93,147,255,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(59,114,232,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(150,180,255,0.12) 0%, transparent 58%),
    linear-gradient(160deg, #22345c 0%, #16213f 48%, #1f2e52 100%);
}
[data-theme="cherry"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(255,170,170,0.24) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(248,113,113,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(220,38,38,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(255,150,150,0.12) 0%, transparent 58%),
    linear-gradient(160deg, #3d1519 0%, #280d11 48%, #391317 100%);
}
[data-theme="butterscotch"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(255,214,140,0.28) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(251,191,36,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(217,119,6,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(255,190,120,0.14) 0%, transparent 58%),
    linear-gradient(160deg, #3d2a10 0%, #2a1c0a 48%, #382613 100%);
}
[data-theme="strawberry"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(255,170,215,0.24) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(244,114,182,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(219,39,119,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(255,150,200,0.12) 0%, transparent 58%),
    linear-gradient(160deg, #3d1228 0%, #280b1a 48%, #391026 100%);
}

[data-theme="blackberry"] body::before {
  background:
    radial-gradient(ellipse 80% 55% at 20% 0%, rgba(59,130,246,.14), transparent 55%),
    radial-gradient(ellipse 60% 45% at 100% 100%, rgba(30,58,138,.16), transparent 50%),
    var(--lb-bg);
}

[data-theme="pistah"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(190,240,160,0.24) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(168,224,99,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(107,191,78,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(170,224,120,0.12) 0%, transparent 58%),
    linear-gradient(160deg, #1d3a29 0%, #122a1c 48%, #1a3527 100%);
}

[data-theme="lemon"] body::before {
  background:
    radial-gradient(120% 90% at 50% -12%, rgba(255,230,120,0.26) 0%, transparent 55%),
    radial-gradient(95% 65% at 6% 2%, rgba(245,216,74,0.22) 0%, transparent 52%),
    radial-gradient(95% 65% at 94% 2%, rgba(232,194,0,0.20) 0%, transparent 52%),
    radial-gradient(150% 130% at 50% 120%, rgba(255,220,100,0.12) 0%, transparent 58%),
    linear-gradient(160deg, #3a3208 0%, #1a1608 48%, #2a2208 100%);
}

/* ── Premium sports typography ─────────────────────────────────────── */
.lb-t-name, .lb-section-title, .lb-status-label, .lb-live-badge,
.lb-hdr-status, .lb-state-overlay {
  font-family: 'Oswald','Segoe UI',sans-serif;
}
.lb-player-name-big, .lb-sold-amount, .lb-base-val, .lb-mvp-pts,
.lb-stamp-word, .lb-sold-team-name, .lb-clock {
  font-family: 'Teko','Oswald','Segoe UI',sans-serif;
}
.lb-t-name { font-size: 1.3rem; letter-spacing: .4px; }
.lb-player-name-big { font-weight: 600; letter-spacing: .5px; }

/* ── Header — premium glass bar ────────────────────────────────────── */
.lb-header {
  padding: 15px 34px;
  box-shadow: 0 6px 30px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.08);
  backdrop-filter: blur(8px);
}
[data-theme="vanilla"] .lb-header {
  background: linear-gradient(135deg, #101a38 0%, #24356e 55%, #2e4a8a 100%);
  border-bottom: 1px solid rgba(93,147,255,0.28);
}
[data-theme="cherry"] .lb-header {
  background: linear-gradient(135deg, #2a0a0c 0%, #6b1414 55%, #8a1f1f 100%);
  border-bottom: 1px solid rgba(248,113,113,0.28);
}
[data-theme="butterscotch"] .lb-header {
  background: linear-gradient(135deg, #2a1a06 0%, #6b3907 55%, #8a4d0a 100%);
  border-bottom: 1px solid rgba(251,191,36,0.28);
}
[data-theme="strawberry"] .lb-header {
  background: linear-gradient(135deg, #2a0818 0%, #6b1240 55%, #8a1a52 100%);
  border-bottom: 1px solid rgba(244,114,182,0.28);
}

[data-theme="blackberry"] .lb-header {
  background: linear-gradient(90deg, var(--lb-header-from), var(--lb-header-to));
  border-bottom: 1px solid rgba(59,130,246,.28);
  box-shadow: 0 0 24px rgba(59,130,246,.18);
}

[data-theme="pistah"] .lb-header {
  background: linear-gradient(135deg, #0c2010 0%, #1c4a22 55%, #26602e 100%);
  border-bottom: 1px solid rgba(168,224,99,0.28);
}

[data-theme="lemon"] .lb-header {
  background: linear-gradient(135deg, #2a2208 0%, #6b5a08 55%, #8a7000 100%);
  border-bottom: 1px solid rgba(245,216,74,0.28);
}
.lb-t-logo {
  width: 56px; height: 56px; border-radius: 14px;
  box-shadow: 0 0 0 1px var(--lb-card-border), 0 8px 22px rgba(0,0,0,0.45);
}
.lb-hdr-center { flex-direction: column; align-items: center; gap: 6px; }
.lb-live-badge {
  align-self: center; flex: 0 0 auto;
  background: linear-gradient(135deg, rgba(239,68,68,0.30), rgba(239,68,68,0.12));
  box-shadow: 0 0 22px rgba(239,68,68,0.28), inset 0 0 0 1px rgba(239,68,68,0.45);
  backdrop-filter: blur(4px);
}
.lb-hdr-status {
  font-size: .58rem; font-weight: 600; letter-spacing: 3px;
  text-transform: uppercase; color: var(--lb-sub);
}
.lb-powered { display: flex; flex-direction: column; align-items: flex-end; line-height: 1.05; }
.lb-powered-by {
  font-size: .48rem; font-weight: 700; letter-spacing: 2px;
  text-transform: uppercase; color: var(--lb-sub);
}
.lb-powered-logo {
  height: 30px; width: auto; margin-top: 2px;
  filter: drop-shadow(0 2px 6px rgba(0,0,0,0.45));
}

/* ── Cards — lighter glassmorphism + soft depth (all themes) ───────── */
.lb-player-panel, .lb-history-card, .lb-mvp-card, .lb-teams-card, .lb-status-card {
  background: linear-gradient(180deg, rgba(255,255,255,0.16), rgba(255,255,255,0.07));
  border: 1px solid rgba(255,255,255,0.18);
  box-shadow: 0 12px 40px rgba(0,0,0,0.28), inset 0 1px 0 rgba(255,255,255,0.14);
  backdrop-filter: blur(10px);
}
.lb-player-panel {
  align-self: start; position: sticky; top: 92px;
  box-shadow: 0 22px 60px rgba(0,0,0,0.38), 0 0 0 1px rgba(255,255,255,0.10),
              inset 0 1px 0 rgba(255,255,255,0.12);
}

/* ── Current Bid / Status — the hero number ────────────────────────── */
.lb-status-card {
  box-shadow: 0 18px 55px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.07);
}
.lb-status-auction {
  position: relative; isolation: isolate;
  background:
    linear-gradient(180deg, rgba(255,255,255,0.12), rgba(255,255,255,0.02)),
    linear-gradient(135deg, var(--lb-bid-from) 0%, var(--lb-bid-to) 100%);
}
.lb-status-sold {
  background:
    linear-gradient(180deg, rgba(255,255,255,0.12), rgba(255,255,255,0.02)),
    linear-gradient(135deg, var(--lb-sold-from) 0%, var(--lb-sold-to) 100%);
}
.lb-status-auction::before {
  content: ''; position: absolute; inset: 0; z-index: 0; pointer-events: none;
  background: radial-gradient(80% 100% at 50% 0%, var(--lb-glow) 0%, transparent 62%);
  opacity: .55;
}
.lb-status-auction > * { position: relative; z-index: 1; }
.lb-sold-amount {
  font-size: 3.4rem; text-shadow: 0 0 26px var(--lb-glow); letter-spacing: 1px;
  transition: all .3s ease;
}
.lb-status-label { letter-spacing: 3px; }

/* Accent numbers glow */
.lb-base-val, .lb-mvp-pts, .lb-tp-pts, .lb-tm-rpts {
  text-shadow: 0 0 16px var(--lb-glow);
}

/* Section titles — gold accent */
.lb-section-title { letter-spacing: 2.4px; color: var(--lb-accent2); }

/* ── Team chips — subtle hover lift ────────────────────────────────── */
.lb-team-chip {
  transition: transform .2s ease, box-shadow .2s ease, border-color .2s ease;
}
.lb-team-chip:hover {
  transform: translateY(-3px);
  border-color: var(--lb-accent);
  box-shadow: 0 10px 26px rgba(0,0,0,0.4), 0 0 22px var(--lb-glow);
}

/* ── Teams relocated to right column — vertical list ───────────────── */
.lb-teams-card { margin-top: 0; }
.lb-teams-grid { flex-direction: column; flex-wrap: nowrap; gap: 8px; }
.lb-team-chip {
  min-width: 0; width: 100%;
  background: rgba(255,255,255,0.07);
  border-color: rgba(255,255,255,0.12);
}
.lb-team-chip > div:last-child { flex: 1; min-width: 0; }
.lb-team-chip-name { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

/* ── Responsive — TV / projector / ultra-wide / tablet scaling ─────── */
@media (min-width: 1600px) {
  .lb-main { max-width: 1840px; grid-template-columns: 440px 1fr; gap: 28px; padding: 28px 40px; }
  .lb-teams-wrap { max-width: 1840px; }
  .lb-spon-row { max-width: 1840px; }
  .lb-t-name { font-size: 1.65rem; }
  .lb-sold-amount { font-size: 4.2rem; }
}
@media (min-width: 2200px) {
  body { font-size: 20px; }
  .lb-main { max-width: 2200px; grid-template-columns: 540px 1fr; }
  .lb-teams-wrap { max-width: 2200px; }
  .lb-spon-row { max-width: 2200px; }
  .lb-sold-amount { font-size: 5rem; }
}
@media (max-width: 600px) {
  .lb-header { padding: 6px 10px; }
  .lb-hdr-center {
    order: 0;
    width: auto;
    align-items: center;
    flex-direction: row;
  }
  .lb-powered { align-items: flex-end; }
  .lb-powered-logo { height: 14px; }
  .lb-powered-by { font-size: .36rem; }
  .lb-clock { font-size: .62rem; }
  .lb-live-badge { padding: 2px 7px; font-size: .52rem; }
  .lb-hdr-status { display: none; }
}
</style>

<!-- ══════════════════════════════════════════════════════════════════════
     PREMIUM BROADCAST THEME  (UI-only override layer — IPL / Big Bash style)
     Overrides the base live-board styles above. No JS / DOM ids changed.
     ══════════════════════════════════════════════════════════════════════ -->
<style>
/* ── Premium palette applied to every theme (unifies all tournaments) ───── */
html[data-theme]{
  --lb-bg:#05070f;
  --lb-header-from:#081026;
  --lb-header-to:#0c1a3d;
  --lb-accent:#00D9FF;      /* cyan   */
  --lb-accent2:#F4C542;     /* gold   */
  --lb-glow:rgba(0,217,255,0.35);
  --lb-live-dot:#FF2E63;    /* live red */
  --lb-sold-color:#22C55E;  /* green  */
  --lb-card:rgba(255,255,255,0.045);
  --lb-card-border:rgba(255,255,255,0.10);
  --lb-text:#F1F5FF;
  --lb-sub:#A8B3CF;
  --lb-panel:#0a1228;
  --lb-sep:rgba(255,255,255,0.08);
  --lb-bid-from:#12163a;
  --lb-bid-to:#241452;      /* deep purple */
  --lb-sold-from:#06301b;
  --lb-sold-to:#0f5132;
  /* premium raw tokens */
  --pr-navy:#081B33; --pr-ink:#111827; --pr-gold:#F4C542; --pr-cyan:#00D9FF;
  --pr-purple:#6D28D9; --pr-red:#FF2E63; --pr-green:#22C55E; --pr-muted:#A8B3CF;
}

/* ── Cinematic stadium background ───────────────────────────────────────── */
html[data-theme] body{
  font-family:'Rajdhani','Oswald',system-ui,sans-serif;
  color:var(--lb-text);
  background:
    radial-gradient(1200px 620px at 12% -8%, rgba(0,217,255,0.14), transparent 60%),
    radial-gradient(1100px 560px at 88% 6%, rgba(109,40,217,0.20), transparent 60%),
    radial-gradient(900px 700px at 50% 118%, rgba(244,197,66,0.06), transparent 60%),
    linear-gradient(160deg,#04060e 0%,#070c1c 45%,#0a0620 100%);
  background-attachment:fixed;
}
html[data-theme] body::before{
  content:"";position:fixed;inset:0;z-index:0;pointer-events:none;
  background:
    repeating-linear-gradient(0deg,rgba(255,255,255,0.015) 0 1px,transparent 1px 3px);
  opacity:.5;mix-blend-mode:overlay;
}
html[data-theme] body > *{position:relative;z-index:1;}
/* Sponsor bar must stay viewport-fixed as a true footer (overrides body > * relative) */
html[data-theme] body > #lb-sponsor-strip,
html[data-theme] body > .lb-sponsor-strip{
  position:fixed !important;
  left:0 !important;right:0 !important;bottom:0 !important;
  width:100% !important;max-width:none !important;
  z-index:250 !important;
  margin:0 !important;
}

/* ── Typography ─────────────────────────────────────────────────────────── */
.lb-t-name,.lb-hdr-status,.lb-live-badge,.lb-section-title,
.lb-player-name-big,.lb-status-label,.lb-base-lbl,.lb-stat-lbl,
.lb-badge,.lb-team-chip-name,.lb-sold-team-name{
  font-family:'Oswald','Rajdhani',sans-serif;
}
.lb-player-name-big,.lb-sold-amount,.lb-stamp-word{
  font-family:'Bebas Neue','Oswald',sans-serif !important;
  letter-spacing:1.5px;
}

/* ── HEADER — premium broadcast banner ──────────────────────────────────── */
html[data-theme] .lb-header{
  background:linear-gradient(100deg,var(--lb-header-from),var(--lb-header-to));
  border-bottom:1px solid rgba(0,217,255,0.20);
  box-shadow:0 8px 34px rgba(0,0,0,0.55),inset 0 -1px 0 rgba(0,217,255,0.15);
  backdrop-filter:blur(6px);
  padding:14px 30px;position:relative;overflow:hidden;
}
html[data-theme] .lb-header::after{
  content:"";position:absolute;left:0;bottom:0;height:2px;width:100%;
  background:linear-gradient(90deg,transparent,var(--pr-cyan),var(--pr-gold),var(--pr-purple),transparent);
  opacity:.85;animation:lbHdrSweep 6s linear infinite;background-size:200% 100%;
}
@keyframes lbHdrSweep{0%{background-position:0 0}100%{background-position:200% 0}}
html[data-theme] .lb-t-logo{
  border-radius:14px;border:1px solid rgba(0,217,255,0.30);
  box-shadow:0 0 22px rgba(0,217,255,0.25);background:rgba(0,0,0,0.30);
}
html[data-theme] .lb-t-name{
  font-weight:700;letter-spacing:.6px;text-transform:uppercase;
  background:linear-gradient(90deg,#fff,#cfe8ff);-webkit-background-clip:text;background-clip:text;
  -webkit-text-fill-color:transparent;
}
html[data-theme] .lb-t-desc{color:var(--pr-gold);letter-spacing:1.4px;text-transform:uppercase;font-size:.62rem;}
html[data-theme] .lb-hdr-status{
  color:var(--pr-muted);letter-spacing:2px;text-transform:uppercase;font-size:.66rem;
}
html[data-theme] .lb-live-badge{
  background:linear-gradient(90deg,#ff2e63,#b3123f);
  box-shadow:0 0 22px rgba(255,46,99,0.55);border:1px solid rgba(255,255,255,0.25);
  letter-spacing:3px;font-weight:700;border-radius:8px;
}
html[data-theme] .lb-live-dot{background:#fff;box-shadow:0 0 10px #fff;animation:lbLiveBlink 1s steps(2,jump-none) infinite;}
@keyframes lbLiveBlink{50%{opacity:.15;transform:scale(.7)}}
html[data-theme] .lb-clock{
  font-family:'Oswald',monospace;color:var(--pr-cyan);letter-spacing:2px;
  text-shadow:0 0 12px rgba(0,217,255,0.45);
}
html[data-theme] .lb-powered-by{color:var(--pr-muted);}

/* ── MAIN — 3-column broadcast grid ─────────────────────────────────────── */
html[data-theme] .lb-main{
  grid-template-columns:minmax(300px,360px) minmax(0,1fr) minmax(300px,380px);
  gap:20px;max-width:1560px;padding:22px 26px;align-items:start;
}
.lb-center-col,.lb-side-col{display:flex;flex-direction:column;gap:18px;min-width:0;}
html[data-theme] .lb-center-col{justify-content:flex-start;min-height:calc(100vh - 210px);}

/* ── Tournament stats block (center column) ─────────────────────────────── */
html[data-theme] .lb-stats-card{
  background:linear-gradient(160deg,rgba(255,255,255,0.06),rgba(255,255,255,0.02));
  border:1px solid rgba(255,255,255,0.10);border-radius:16px;
  backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);
  box-shadow:0 18px 46px rgba(0,0,0,0.50),inset 0 1px 0 rgba(255,255,255,0.06);
  padding:20px 22px;
}
html[data-theme] .lb-stats-grid{
  display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:16px;
}
html[data-theme] .lb-stat-tile{
  position:relative;overflow:hidden;border-radius:14px;padding:26px 18px;
  background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.08);
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;
  transition:transform .2s,box-shadow .2s;
}
html[data-theme] .lb-stat-tile:hover{transform:translateY(-3px);box-shadow:0 12px 26px rgba(0,0,0,0.4);}
html[data-theme] .lb-stat-tile::before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;}
html[data-theme] .lb-stat-tile-num{
  font-family:'Bebas Neue','Oswald',sans-serif;font-size:3rem;line-height:1;letter-spacing:1px;
  font-variant-numeric:tabular-nums;
}
html[data-theme] .lb-stat-tile-lbl{
  font-family:'Oswald',sans-serif;font-size:.74rem;font-weight:600;letter-spacing:2.2px;
  text-transform:uppercase;color:var(--pr-muted);
}
html[data-theme] .lb-stat-inauction .lb-stat-tile-num{color:var(--pr-cyan);text-shadow:0 0 18px rgba(0,217,255,0.40);}
html[data-theme] .lb-stat-inauction::before{background:var(--pr-cyan);box-shadow:0 0 14px rgba(0,217,255,0.5);}
html[data-theme] .lb-stat-sold .lb-stat-tile-num{color:var(--pr-green);text-shadow:0 0 18px rgba(34,197,94,0.40);}
html[data-theme] .lb-stat-sold::before{background:var(--pr-green);box-shadow:0 0 14px rgba(34,197,94,0.5);}
html[data-theme] .lb-stat-unsold .lb-stat-tile-num{color:var(--pr-red);text-shadow:0 0 18px rgba(255,46,99,0.40);}
html[data-theme] .lb-stat-unsold::before{background:var(--pr-red);box-shadow:0 0 14px rgba(255,46,99,0.5);}
html[data-theme] .lb-stat-total .lb-stat-tile-num{color:var(--pr-gold);text-shadow:0 0 18px rgba(244,197,66,0.40);}
html[data-theme] .lb-stat-total::before{background:var(--pr-gold);box-shadow:0 0 14px rgba(244,197,66,0.5);}

/* ── Glassmorphism card treatment ───────────────────────────────────────── */
html[data-theme] .lb-player-panel,
html[data-theme] .lb-status-card,
html[data-theme] .lb-history-card,
html[data-theme] .lb-mvp-card,
html[data-theme] .lb-teams-card{
  background:linear-gradient(160deg,rgba(255,255,255,0.06),rgba(255,255,255,0.02));
  border:1px solid rgba(255,255,255,0.10);
  border-radius:16px;
  backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);
  box-shadow:0 18px 46px rgba(0,0,0,0.50),inset 0 1px 0 rgba(255,255,255,0.06);
}

/* Player panel: glowing animated gradient border + cinematic overlay */
html[data-theme] .lb-player-panel{position:relative;top:0;align-self:start;overflow:hidden;}
html[data-theme] .lb-player-panel::before{
  content:"";position:absolute;inset:0;border-radius:16px;padding:1.5px;
  background:linear-gradient(140deg,var(--pr-cyan),var(--pr-purple),var(--pr-gold));
  -webkit-mask:linear-gradient(#000 0 0) content-box,linear-gradient(#000 0 0);
  -webkit-mask-composite:xor;mask-composite:exclude;
  opacity:.7;animation:lbBorderGlow 5s ease-in-out infinite;pointer-events:none;z-index:3;
}
@keyframes lbBorderGlow{0%,100%{opacity:.35}50%{opacity:.85}}
html[data-theme] .lb-photo-wrap{
  background:
    radial-gradient(120% 90% at 50% 0%, rgba(0,217,255,0.18), transparent 55%),
    radial-gradient(120% 90% at 50% 100%, rgba(109,40,217,0.30), transparent 60%),
    #060a16;
}
html[data-theme] .lb-photo-wrap::after{
  content:"";position:absolute;inset:0;pointer-events:none;
  background:
    linear-gradient(115deg,transparent 40%,rgba(0,217,255,0.08) 50%,transparent 60%),
    radial-gradient(60% 40% at 50% 0%,rgba(255,255,255,0.10),transparent 70%);
}
html[data-theme] .lb-player-name-big{
  font-size:2.5rem;line-height:.98;
  text-shadow:0 3px 20px rgba(0,0,0,0.85),0 0 24px rgba(0,217,255,0.25);
}
html[data-theme] .lb-player-sub{color:var(--pr-gold);letter-spacing:1.6px;text-transform:uppercase;}

/* SL and state overlays */
html[data-theme] .lb-sl-overlay{
  background:linear-gradient(90deg,var(--pr-gold),#d99a1a);color:#111;font-weight:700;
  box-shadow:0 4px 18px rgba(244,197,66,0.45);
}
html[data-theme] .lb-state-overlay{backdrop-filter:blur(4px);letter-spacing:2px;}
html[data-theme] .lb-state-auction{background:linear-gradient(90deg,#00D9FF,#0284c7);color:#031018;box-shadow:0 0 18px rgba(0,217,255,0.5);}

/* Badges and chips */
html[data-theme] .lb-badge{border-radius:8px;letter-spacing:.6px;text-transform:uppercase;font-weight:600;box-shadow:0 4px 14px rgba(0,0,0,0.35);}
html[data-theme] .lb-b-icon{background:linear-gradient(90deg,var(--pr-gold),#e0a52a)!important;color:#1a1200!important;}
html[data-theme] .lb-tag-chip{
  background:rgba(0,217,255,0.10);border:1px solid rgba(0,217,255,0.28);color:#bff2ff;
  border-radius:20px;letter-spacing:.4px;
}
html[data-theme] .lb-stat-chip{
  background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.09);border-radius:12px;
  min-height:46px;display:flex;flex-direction:column;justify-content:center;
}
.lb-stat-break{flex-basis:100%;width:100%;height:0;margin:0;padding:0;border:0;}
html[data-theme] .lb-stat-lbl{color:var(--pr-cyan);letter-spacing:1.4px;text-transform:uppercase;}
html[data-theme] .lb-base-row{
  background:linear-gradient(90deg,rgba(244,197,66,0.14),rgba(244,197,66,0.04));
  border:1px solid rgba(244,197,66,0.30);border-radius:12px;
}
html[data-theme] .lb-base-lbl{color:var(--pr-gold);letter-spacing:2px;text-transform:uppercase;}
html[data-theme] .lb-base-val{color:#fff;font-family:'Oswald',sans-serif;font-weight:700;text-shadow:0 0 14px rgba(244,197,66,0.35);}

/* ── Section titles ─────────────────────────────────────────────────────── */
html[data-theme] .lb-section-title{
  color:var(--pr-gold);letter-spacing:2.6px;text-transform:uppercase;font-weight:600;
  display:flex;align-items:center;gap:9px;
}
html[data-theme] .lb-section-title::after{
  content:"";flex:1;height:1px;
  background:linear-gradient(90deg,rgba(244,197,66,0.5),transparent);
}
html[data-theme] .lb-section-title i{color:var(--pr-cyan);}

/* ── Status / Bid card (CENTER hero) ────────────────────────────────────── */
html[data-theme] .lb-status-card{padding:34px 30px;min-height:220px;position:relative;overflow:hidden;display:flex;flex-direction:column;justify-content:center;}
html[data-theme] .lb-status-card::before{
  content:"";position:absolute;right:-40px;bottom:-40px;width:260px;height:260px;pointer-events:none;
  background:radial-gradient(circle,rgba(0,217,255,0.10),transparent 70%);
}
html[data-theme] .lb-status-auction{background:none;}
html[data-theme] .lb-status-label{color:var(--pr-cyan);letter-spacing:1.8px;text-transform:uppercase;font-weight:600;font-size:1rem;}
html[data-theme] .lb-status-sold{
  background:linear-gradient(135deg,var(--lb-sold-from),var(--lb-sold-to));
  border-radius:12px;padding:18px;border:1px solid rgba(34,197,94,0.35);
  box-shadow:0 0 30px rgba(34,197,94,0.25);animation:lbPopIn .5s ease;
}
html[data-theme] .lb-sold-amount{color:#eaffef;text-shadow:0 0 22px rgba(34,197,94,0.55);}
html[data-theme] .lb-auction-spinner{border-top-color:var(--pr-cyan)!important;}
@keyframes lbPopIn{0%{transform:scale(.85);opacity:0}100%{transform:scale(1);opacity:1}}

/* ── History (Recent Transactions) ─────────────────────────────────────── */
html[data-theme] .lb-history-item{
  border-radius:12px;border:1px solid rgba(255,255,255,0.07);
  background:rgba(255,255,255,0.03);transition:transform .2s,background .2s;
}
html[data-theme] .lb-history-item:first-child{
  border-color:rgba(0,217,255,0.4);box-shadow:0 0 18px rgba(0,217,255,0.20);
  background:rgba(0,217,255,0.06);animation:lbSlideIn .5s ease;
}
@keyframes lbSlideIn{from{opacity:0;transform:translateY(-8px)}to{opacity:1;transform:none}}
html[data-theme] .lb-history-time{color:var(--pr-muted);}

/* ── MVP / Top Buys ─────────────────────────────────────────────────────── */
html[data-theme] .lb-mvp-item{
  border-radius:12px;border:1px solid rgba(255,255,255,0.07);background:rgba(255,255,255,0.03);
}
html[data-theme] .lb-mvp-item-1{
  border-color:rgba(244,197,66,0.45);
  background:linear-gradient(90deg,rgba(244,197,66,0.14),rgba(244,197,66,0.02));
  box-shadow:0 0 22px rgba(244,197,66,0.22);
}
html[data-theme] .lb-mvp-pts{color:var(--pr-gold);font-family:'Oswald',sans-serif;font-weight:700;}
html[data-theme] .lb-mvp-name{color:#fff;}
html[data-theme] .lb-mvp-crown{filter:drop-shadow(0 0 6px rgba(244,197,66,0.7));}

/* ── Teams — full-width horizontal broadcast strip ──────────────────────── */
html[data-theme] .lb-teams-card{
  max-width:1560px;margin:4px auto 20px;padding:16px 22px;
}
html[data-theme] .lb-teams-grid{
  display:flex;flex-wrap:wrap;flex-direction:row;gap:12px;
}
html[data-theme] .lb-team-chip{
  flex:1 1 210px;max-width:280px;
  border-radius:14px;border:1px solid rgba(255,255,255,0.09);
  background:linear-gradient(150deg,rgba(255,255,255,0.06),rgba(255,255,255,0.02));
  transition:transform .2s,box-shadow .2s,border-color .2s;
}
html[data-theme] .lb-team-chip:hover,html[data-theme] .lb-team-chip:focus{
  transform:translateY(-4px);border-color:rgba(0,217,255,0.45);
  box-shadow:0 12px 30px rgba(0,0,0,0.5),0 0 20px rgba(0,217,255,0.25);outline:none;
}
html[data-theme] .lb-team-chip-name{color:#fff;font-weight:600;}
html[data-theme] .lb-team-chip-pts{color:var(--pr-cyan);}

/* ── Footer ticker ──────────────────────────────────────────────────────── */
html[data-theme] .lb-footer{
  padding:0;border-top:1px solid rgba(0,217,255,0.18);
  background:linear-gradient(90deg,#05070f,#0a1024);overflow:hidden;
}
.lb-ticker{overflow:hidden;white-space:nowrap;}
.lb-ticker-track{display:inline-flex;align-items:center;gap:0;padding:11px 0;animation:lbTicker 34s linear infinite;}
.lb-ticker-item{
  display:inline-flex;align-items:center;gap:10px;padding:0 34px;
  font-family:'Oswald',sans-serif;font-size:.74rem;font-weight:600;letter-spacing:2.6px;
  text-transform:uppercase;color:#cdd7ee;border-right:1px solid rgba(255,255,255,0.10);
}
.lb-ticker-dot{width:8px;height:8px;border-radius:50%;background:var(--pr-red);box-shadow:0 0 10px var(--pr-red);}
@keyframes lbTicker{0%{transform:translateX(0)}100%{transform:translateX(-50%)}}

/* ── Sponsor strip (fixed bottom highlight) ──────────────────────────── */
html[data-theme] .lb-sponsor-strip,
html[data-theme] body > #lb-sponsor-strip{
  position:fixed !important;
  left:0 !important;right:0 !important;bottom:0 !important;
  width:100% !important;max-width:none !important;
  z-index:250 !important;
  background:linear-gradient(180deg,rgba(12,18,48,0.98),rgba(5,7,15,1));
  border-top:2px solid var(--pr-gold);
  box-shadow:0 -10px 36px rgba(0,0,0,.65),0 0 24px rgba(255,215,0,.12);
}
html[data-theme] .lb-spon-label{
  color:var(--pr-gold);letter-spacing:.28em;
  text-shadow:0 0 20px rgba(255,215,0,.35);
}

/* ── Entrance animation for panels ──────────────────────────────────────── */
html[data-theme] .lb-player-panel,html[data-theme] .lb-center-col > *,
html[data-theme] .lb-side-col > *,html[data-theme] .lb-teams-card{
  animation:lbFadeUp .55s cubic-bezier(.16,.84,.44,1) both;
}
@keyframes lbFadeUp{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:none}}

/* ── Responsive rearrange (no horizontal scroll, graceful reflow) ────────── */
@media (min-width:2000px){
  html[data-theme] .lb-main{max-width:2200px;grid-template-columns:minmax(360px,460px) minmax(0,1fr) minmax(360px,480px);gap:28px;padding:30px 44px;}
  html[data-theme] .lb-teams-card{max-width:2200px;}
  html[data-theme] .lb-player-name-big{font-size:3rem;}
}
@media (max-width:1180px){
  html[data-theme] .lb-main{grid-template-columns:minmax(0,1fr) minmax(0,1fr);}
  html[data-theme] .lb-player-panel{grid-column:1 / -1;}
}
@media (max-width:820px){
  html[data-theme] .lb-main{grid-template-columns:1fr;padding:16px 14px;gap:16px;}
  html[data-theme] .lb-player-panel{grid-column:auto;}
  html[data-theme] .lb-header{flex-wrap:nowrap;gap:8px;padding:8px 12px;}
  html[data-theme] .lb-hdr-center{order:0;width:auto;}
  html[data-theme] .lb-team-chip{flex:1 1 100%;max-width:none;}
  html[data-theme] .lb-player-name-big{font-size:2.1rem;}
}
@media (max-width:600px){
  html[data-theme] .lb-header{
    flex-wrap:nowrap;
    align-items:center;
    gap:6px;
    padding:6px 10px;
    box-shadow:0 4px 16px rgba(0,0,0,.4);
  }
  html[data-theme] .lb-t-logo{
    width:40px;
    height:40px;
    border-radius:8px;
    box-shadow:0 0 10px rgba(0,217,255,.18);
  }
  html[data-theme] .lb-t-name{
    font-size:.95rem;
    letter-spacing:.3px;
  }
  html[data-theme] .lb-t-desc{
    font-size:.56rem;
    letter-spacing:.8px;
  }
  html[data-theme] .lb-hdr-center{
    order:0;
    width:auto;
    flex-direction:row;
    align-items:center;
    gap:4px;
  }
  html[data-theme] .lb-live-badge{
    padding:2px 7px;
    font-size:.5rem;
    letter-spacing:.8px;
    border-radius:4px;
    box-shadow:0 0 10px rgba(255,46,99,.35);
  }
  html[data-theme] .lb-live-dot{width:5px;height:5px;}
  html[data-theme] .lb-hdr-status{display:none;}
  html[data-theme] .lb-hdr-right{gap:6px;}
  html[data-theme] .lb-powered-by{
    font-size:.34rem;
    letter-spacing:.6px;
  }
  html[data-theme] .lb-powered-logo{height:14px;margin-top:0;}
  html[data-theme] .lb-clock{
    font-size:.62rem;
    letter-spacing:.5px;
    text-shadow:none;
  }
  html[data-theme] .lb-sponsor-strip,
  html[data-theme] body > #lb-sponsor-strip{
    padding:.28rem .45rem .32rem;
    box-shadow:0 -4px 16px rgba(0,0,0,.45);
  }
  html[data-theme] .lb-spon-label{
    font-size:.58rem;
    letter-spacing:.16em;
  }
  html[data-theme] .lb-spon-track{
    display:grid;
    grid-template-columns:repeat(5,minmax(0,1fr));
    gap:.2rem .28rem;
    padding:.1rem 0;
  }
  html[data-theme] .lb-spon-card{height:28px;padding:0;}
  html[data-theme] body.lb-has-sponsors{padding-bottom:58px;}
}
</style>
</head>
<body>
<t t-call="auction_module.point_unit_bootstrap">
  <t t-set="pu_tournament" t-value="tournament"/>
</t>


<!-- HEADER -->
<header class="lb-header">
  <div class="lb-hdr-left">
    <t t-if="tournament and tournament.logo">
      <div class="lb-t-logo">
        <img t-att-src="'/%s/auction/public/image/auction.tournament/%d/logo' % (db_name, tournament.id)" alt="Logo"/>
      </div>
    </t>
    <t t-else="">
      <div class="lb-t-logo" style="font-size:1.4rem;">🏆</div>
    </t>
    <div>
      <div class="lb-t-name" id="hdr-t-name">
        <t t-if="tournament"><t t-esc="tournament.name"/></t>
        <t t-else="">Live Auction Board</t>
      </div>
      <div class="lb-t-desc" id="hdr-t-desc">
        <t t-if="tournament and tournament.description"><t t-esc="tournament.description"/></t>
      </div>
    </div>
  </div>
  <div class="lb-hdr-center">
    <div class="lb-live-badge"><span class="lb-live-dot"></span> LIVE</div>
    <div class="lb-hdr-status">Live Player Auction</div>
  </div>
  <div class="lb-hdr-right">
    <div class="lb-powered">
      <span class="lb-powered-by">Powered by</span>
      <img class="lb-powered-logo" src="/auction_module/static/src/assets/images/logo.svg" alt="AuctionChamp"/>
    </div>
    <div class="lb-clock" id="lb-clock">--:--:--</div>
  </div>
</header>

<!-- MAIN CONTENT (populated by JS) -->
<main class="lb-main" id="lb-main">
  <!-- Left: Player Panel -->
  <section class="lb-player-panel" id="lb-player-panel">
    <div class="lb-photo-wrap">
      <div class="lb-photo-placeholder" id="lb-photo-ph">👤</div>
      <img id="lb-player-img" src="" alt="Player" style="display:none;position:absolute;top:0;left:0;"/>
      <div class="lb-mystery-lock" id="lb-mystery-lock" style="display:none" aria-hidden="true">
        <div class="lb-mystery-lock-ico">
          <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M7 11V8a5 5 0 0 1 10 0v3" stroke="#fff" stroke-width="2.2" stroke-linecap="round"/>
            <rect x="4.5" y="11" width="15" height="10.5" rx="2.2" fill="rgba(255,255,255,.12)" stroke="#fff" stroke-width="2"/>
            <circle cx="12" cy="15.2" r="1.55" fill="#fff"/>
            <path d="M12 16.7v2.1" stroke="#fff" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="lb-mystery-lock-title">PLAYER LOCKED</div>
        <div class="lb-mystery-lock-sub">Requires Winning Bid</div>
      </div>
      <div class="lb-sl-overlay" id="lb-sl-overlay" style="display:none;">#<span id="lb-sl-num">--</span></div>
      <div class="lb-state-overlay lb-state-auction" id="lb-state-overlay" style="display:none;">LIVE</div>
      <!-- SOLD STAMP -->
      <div class="lb-stamp-overlay lb-stamp-sold" id="lb-sold-stamp" style="display:none;">
        <div class="lb-stamp-box">
          <div class="lb-stamp-hammer">🔨</div>
          <div class="lb-stamp-word">SOLD</div>
          <div class="lb-stamp-pts" id="stamp-pts"></div>
          <div class="lb-stamp-team">
            <div class="lb-stamp-team-logo" id="stamp-team-logo-wrap">
              <img id="stamp-team-logo" src="" alt=""/>
            </div>
            <div class="lb-stamp-team-name" id="stamp-team-name"></div>
          </div>
        </div>
      </div>
      <!-- UNSOLD STAMP -->
      <div class="lb-stamp-overlay lb-stamp-unsold" id="lb-unsold-stamp" style="display:none;">
        <div class="lb-stamp-box">
          <div class="lb-stamp-word">UNSOLD</div>
          <!--<div class="lb-stamp-sub">Goes back to pool</div>-->
        </div>
      </div>
      <div class="lb-photo-gradient"></div>
      <div class="lb-photo-name">
        <div class="lb-player-name-big" id="lb-pname">Waiting for next player…</div>
        <div class="lb-player-sub" id="lb-psub"></div>
      </div>
    </div>
    <div class="lb-player-meta">
      <div class="lb-badges-row" id="lb-badges-row"></div>
      <div class="lb-stats-row" id="lb-stats-row" style="display:none;">
        <div class="lb-stat-chip" id="lb-bat-chip">
          <div class="lb-stat-lbl">Batting</div>
          <div class="lb-stat-val" id="lb-bat-val">—</div>
        </div>
        <div class="lb-stat-chip" id="lb-bowl-chip">
          <div class="lb-stat-lbl">Bowling</div>
          <div class="lb-stat-val" id="lb-bowl-val">—</div>
        </div>
      </div>
      <div class="lb-base-row" id="lb-base-row" style="display:none;">
        <div class="lb-base-lbl">Base Price</div>
        <div class="lb-base-val" id="lb-base-val">—</div>
      </div>
    </div>
  </section>

  <!-- Center: Bid status -->
  <section class="lb-center-col" id="lb-center-col">
    <!-- Status card -->
    <div class="lb-status-card" id="lb-status-card">
      <div class="lb-status-waiting">
        <div class="lb-auction-spinner"></div>
        <div class="lb-auction-pulse">Waiting for auction to begin…</div>
      </div>
    </div>

    <!-- Tournament stats -->
    <div class="lb-stats-card" id="lb-stats-card" style="display:none;">
      <div class="lb-section-title"><i class="fa fa-chart-simple"></i> Auction Statistics</div>
      <div class="lb-stats-grid">
        <div class="lb-stat-tile lb-stat-inauction">
          <div class="lb-stat-tile-num" id="lb-stat-inauction">0</div>
          <div class="lb-stat-tile-lbl">In Auction</div>
        </div>
        <div class="lb-stat-tile lb-stat-sold">
          <div class="lb-stat-tile-num" id="lb-stat-sold">0</div>
          <div class="lb-stat-tile-lbl">Sold</div>
        </div>
        <div class="lb-stat-tile lb-stat-unsold">
          <div class="lb-stat-tile-num" id="lb-stat-unsold">0</div>
          <div class="lb-stat-tile-lbl">Unsold</div>
        </div>
        <div class="lb-stat-tile lb-stat-total">
          <div class="lb-stat-tile-num" id="lb-stat-total">0</div>
          <div class="lb-stat-tile-lbl">Total Players</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Right: History + MVP -->
  <section class="lb-side-col" id="lb-side-col">
    <!-- History card -->
    <div class="lb-history-card">
      <div class="lb-section-title"><i class="fa fa-history"></i> Recent Transactions</div>
      <div id="lb-history-list">
        <div class="lb-history-empty">No transactions yet</div>
      </div>
    </div>

    <!-- MVP / Top Buys card -->
    <div class="lb-mvp-card">
      <div class="lb-section-title"><i class="fa fa-trophy"></i> Top Buys &amp; MVP Board</div>
      <div id="lb-mvp-list">
        <div class="lb-mvp-empty">No sold players yet</div>
      </div>
    </div>

  </section>
</main>

<!-- Participating teams — full-width broadcast strip -->
<section class="lb-teams-card" id="lb-teams-card" style="display:none;">
  <div class="lb-section-title"><i class="fa fa-shield-halved"></i> Participating Teams</div>
  <div class="lb-teams-grid" id="lb-teams-grid"></div>
</section>

<div class="lb-footer" id="lb-footer">
  <div class="lb-ticker">
    <div class="lb-ticker-track">
      <span class="lb-ticker-item"><span class="lb-ticker-dot"></span><t t-if="tournament"><t t-esc="tournament.name"/></t><t t-else="">Live Auction Board</t></span>
      <span class="lb-ticker-item">LIVE AUCTION IN PROGRESS</span>
      <span class="lb-ticker-item">Bids update in real time</span>
      <span class="lb-ticker-item">Next player announced automatically</span>
      <span class="lb-ticker-item">Powered by AuctionChamp</span>
      <span class="lb-ticker-item"><span class="lb-ticker-dot"></span><t t-if="tournament"><t t-esc="tournament.name"/></t><t t-else="">Live Auction Board</t></span>
      <span class="lb-ticker-item">LIVE AUCTION IN PROGRESS</span>
      <span class="lb-ticker-item">Bids update in real time</span>
      <span class="lb-ticker-item">Next player announced automatically</span>
      <span class="lb-ticker-item">Powered by AuctionChamp</span>
    </div>
  </div>
</div>

<script><![CDATA[
(function(){
  let lastPlayerId = null;
  let lastState    = null;
  let lastMysteryRevealed = null;
  var _teamsData   = [];
  var _sport = (document.documentElement.getAttribute('data-sport') || 'cricket').toLowerCase();

  function cricketRoleTags(role) {
    var lo = (role || '').toLowerCase().replace(/[-_\s]+/g, '');
    if (!lo) return '';
    var isWK = lo.indexOf('wicket') >= 0 || lo === 'wk' || lo.indexOf('keeper') >= 0;
    var isAR = lo.indexOf('allround') >= 0;
    var isBat = lo.indexOf('bat') >= 0;
    var isBowl = lo.indexOf('bowl') >= 0;
    var tags = [];
    if (isAR || (isBat && isBowl)) {
      tags.push('<span class="lb-role-tag bat">BAT</span>');
      tags.push('<span class="lb-role-tag bowl">BOWL</span>');
    } else if (isWK && isBat) {
      tags.push('<span class="lb-role-tag wk">WK</span>');
      tags.push('<span class="lb-role-tag bat">BAT</span>');
    } else if (isWK) {
      tags.push('<span class="lb-role-tag wk">WK</span>');
    } else if (isBat) {
      tags.push('<span class="lb-role-tag bat">BAT</span>');
    } else if (isBowl) {
      tags.push('<span class="lb-role-tag bowl">BOWL</span>');
    }
    if (!tags.length) return '';
    return '<span class="lb-role-tags">' + tags.join('') + '</span>';
  }

  function footballPosBadge(p) {
    var code = (p.position_code || '').trim().toUpperCase();
    var name = (p.position_name || '').trim();
    if (!code && name) {
      code = name.replace(/[-_/]+/g, ' ').split(/\s+/).map(function(w) {
        return w.charAt(0);
      }).join('').toUpperCase().slice(0, 3);
    }
    if (!code) return '';
    var title = name || code;
    return '<span class="lb-pos-badge" title="' + lbEsc(title) + '">' + lbEsc(code) + '</span>';
  }

  function playerRoleMetaHtml(p) {
    if (_sport === 'football') {
      return footballPosBadge(p) || (p.role ? '<span class="lb-pos-badge">' + lbEsc(p.role) + '</span>' : '');
    }
    var tags = cricketRoleTags(p.role);
    if (tags) return tags;
    return p.role ? lbEsc(p.role) : '';
  }

  // ── Break time + advertiser state ─────────────────────────────
  var _breakActive   = false;
  var _breakStartTs  = null;
  var _breakClockId  = null;
  var _adList        = [];
  var _adSig         = '';
  var _adBrkTimerId  = null;
  var _adPairIndex   = 0;
  var _adPairCount   = 0;

  function startBreakClock() {
    if (_breakClockId) return;
    _breakClockId = setInterval(function() {
      if (!_breakStartTs) return;
      var e = Math.floor((Date.now() - _breakStartTs) / 1000);
      var m = Math.floor(e / 60), s = e % 60;
      var el = document.getElementById('lb-brk-timer');
      if (el) el.textContent = '\u25cf Break in progress \u2014 ' + (m > 0 ? m + 'm ' : '') + String(s).padStart(2,'0') + 's';
    }, 1000);
  }
  function stopBreakClock() {
    if (_breakClockId) { clearInterval(_breakClockId); _breakClockId = null; }
  }

  function adsSignature(ads) {
    return (ads || []).map(function(a) {
      return String(a.id || '') + '|' + String(a.image_url || '') + '|' + String(a.name || '');
    }).join('||');
  }

  function adCellHtml(ad) {
    if (!ad) return '<div class="lb-brk-ad-cell" aria-hidden="true"></div>';
    var inner = ad.image_url
      ? '<img src="' + ad.image_url + '" alt="' + (ad.name || '') + '" onerror="this.style.display=\'none\'">'
      : '<span class="lb-brk-ad-name">' + (ad.name || '') + '</span>';
    return '<div class="lb-brk-ad-cell">' + inner + '</div>';
  }

  function goToAdPair(idx) {
    if (_adPairCount < 1) return;
    _adPairIndex = ((idx % _adPairCount) + _adPairCount) % _adPairCount;
    var track = document.getElementById('lb-brk-ads-track');
    var vp = document.getElementById('lb-brk-ads-viewport');
    var w = vp ? vp.clientWidth : 0;
    if (track && w) {
      track.style.transform = 'translateX(-' + (_adPairIndex * w) + 'px)';
    }
    var dots = document.querySelectorAll('#lb-brk-ad-dots .lb-brk-ad-dot');
    for (var i = 0; i < dots.length; i++) {
      dots[i].classList.toggle('active', i === _adPairIndex);
    }
  }

  function advanceAdPair() {
    if (_adPairCount < 2) return;
    goToAdPair(_adPairIndex + 1);
  }

  function startAdPairTimer() {
    if (_adBrkTimerId) { clearInterval(_adBrkTimerId); _adBrkTimerId = null; }
    if (_adPairCount > 1) {
      _adBrkTimerId = setInterval(advanceAdPair, 2000);
    }
  }

  function buildAdPairs(list) {
    var n = (list || []).length;
    if (!n) return [];
    if (n === 1) return [{ a: list[0], b: null, solo: true }];
    var pairs = [];
    for (var i = 0; i < n; i += 2) {
      if (i + 1 < n) {
        pairs.push({ a: list[i], b: list[i + 1], solo: false });
      } else {
        /* Odd leftover: pair last with first (round-robin), never show a lone slot */
        pairs.push({ a: list[i], b: list[0], solo: false });
      }
    }
    return pairs;
  }

  function updateAds(ads) {
    var next = ads || [];
    var sig = adsSignature(next);
    var changed = sig !== _adSig;
    _adList = next;
    _adSig = sig;

    if (changed) {
      var brkAds  = document.getElementById('lb-brk-ads');
      var brkTrack = document.getElementById('lb-brk-ads-track');
      var brkDots  = document.getElementById('lb-brk-ad-dots');
      if (brkTrack) {
        var pairs = buildAdPairs(_adList);
        _adPairCount = pairs.length;
        brkTrack.innerHTML = pairs.map(function(pair) {
          if (pair.solo) {
            return '<div class="lb-brk-ad-pair lb-brk-ad-pair-solo">' + adCellHtml(pair.a) + '</div>';
          }
          return '<div class="lb-brk-ad-pair">' + adCellHtml(pair.a) + adCellHtml(pair.b) + '</div>';
        }).join('');
        _adPairIndex = 0;
        // Apply after layout so viewport width is correct
        requestAnimationFrame(function() { goToAdPair(0); });
      }
      if (brkDots) {
        brkDots.innerHTML = '';
        for (var d = 0; d < _adPairCount; d++) {
          brkDots.innerHTML += '<div class="lb-brk-ad-dot' + (d === 0 ? ' active' : '') + '"></div>';
        }
        brkDots.style.display = _adPairCount > 1 ? '' : 'none';
      }
      if (brkAds) brkAds.style.display = _adList.length ? '' : 'none';
      startAdPairTimer();
    }

    // Regular sponsor strip — hide entirely when no advertisers configured
    var strip = document.getElementById('lb-sponsor-strip');
    var stripTrack = document.getElementById('lb-sponsor-track');
    var hasAds = _adList.length > 0;
    if (changed && stripTrack && hasAds) {
      stripTrack.innerHTML = _adList.map(function(ad) {
        var inner = ad.image_url
          ? '<img class="lb-spon-logo" src="' + ad.image_url + '" alt="' + (ad.name||'') + '" title="' + (ad.name||'') + '" onerror="this.style.display=\'none\'">'
          : '<span class="lb-spon-name">' + (ad.name||'') + '</span>';
        return '<div class="lb-spon-item"><div class="lb-spon-card">' + inner + '</div></div>';
      }).join('');
    }
    if (strip) strip.style.display = hasAds ? '' : 'none';
    document.body.classList.toggle('lb-has-sponsors', hasAds);
  }

  window.addEventListener('resize', function() {
    if (_adPairCount) goToAdPair(_adPairIndex);
  });

  function handleBreakTime(isBreak, tournamentData) {
    var overlay = document.getElementById('lb-break-overlay');
    if (isBreak) {
      if (!_breakActive) {
        _breakActive  = true;
        _breakStartTs = Date.now();
        // Set tournament logo once
        var logoEl = document.getElementById('lb-brk-logo');
        if (logoEl && tournamentData && tournamentData.logo_url && !logoEl.dataset.set) {
          logoEl.innerHTML = '<img src="' + tournamentData.logo_url + '" alt="" onerror="this.innerHTML=\'<span class=lb-brk-logo-ph>&#127942;</span>\'">';
          logoEl.dataset.set = '1';
        }
        overlay.classList.add('active');
        startBreakClock();
        requestAnimationFrame(function() {
          if (_adPairCount) goToAdPair(_adPairIndex);
          startAdPairTimer();
        });
      }
    } else {
      if (_breakActive) {
        _breakActive  = false;
        _breakStartTs = null;
        overlay.classList.remove('active');
        stopBreakClock();
        if (_adBrkTimerId) { clearInterval(_adBrkTimerId); _adBrkTimerId = null; }
        var timerEl = document.getElementById('lb-brk-timer');
        if (timerEl) timerEl.textContent = '\u25cf Break in progress';
      }
    }
  }

  function fmtPts(n) {
    var sym = (window.pointUnitSymbol && window.pointUnitSymbol()) || 'pts';
    var pos = (window.AuctionPointUnit && window.AuctionPointUnit.cfg().position) || 'after';
    var space = !(window.AuctionPointUnit && window.AuctionPointUnit.cfg().with_space === false);
    var sep = space ? ' ' : '';
    function wrap(core) {
      return pos === 'before' ? (sym + sep + core) : (core + sep + sym);
    }
    if (!n) return wrap('0');
    if (n >= 100000) return wrap((n/100000).toFixed(2).replace(/\.?0+$/,'') + 'L');
    if (n >= 1000)   return wrap((n/1000).toFixed(1).replace(/\.?0+$/,'') + 'K');
    return wrap(String(n));
  }

  function lbEsc(s) {
    return String(s == null ? '' : s)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;')
      .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
  }

  function lbStat(label, val) {
    return '<div class="lb-stat-chip"><div class="lb-stat-lbl">' + lbEsc(label) +
           '</div><div class="lb-stat-val">' + lbEsc(val) + '</div></div>';
  }

  function lbChips(items) {
    if (!items || !items.length) return '';
    let html = '';
    for (let i = 0; i < items.length; i++) {
      const it = items[i] || {};
      const ic = it.icon ? (lbEsc(it.icon) + ' ') : '';
      html += '<span class="lb-tag-chip">' + ic + lbEsc(it.name || '') + '</span>';
    }
    return html;
  }

  function safeImg(url) {
    return url || '';
  }

  // ── Clock ──────────────────────────────────────────────────────
  function updateClock() {
    const now = new Date();
    document.getElementById('lb-clock').textContent =
      now.toLocaleTimeString('en-IN', {hour:'2-digit', minute:'2-digit', second:'2-digit'});
  }
  var _clockTimer = setInterval(updateClock, 1000);
  updateClock();
  window.addEventListener('beforeunload', function() { clearInterval(_clockTimer); });

  // ── Player photo (preload so old sold face never lingers) ──────
  var _lbPhotoLoadToken = 0;
  var _lbLastPhotoUrl = null;

  function lbShowPhotoLoader() {
    var img = document.getElementById('lb-player-img');
    var ph = document.getElementById('lb-photo-ph');
    if (!img || !ph) return;
    img.style.display = 'none';
    img.style.opacity = '0';
    img.removeAttribute('src');
    _lbLastPhotoUrl = null;
    ph.classList.add('lb-photo-loading');
    ph.style.display = 'flex';
    ph.innerHTML = ''
      + '<div class="lb-photo-loader" aria-hidden="true">'
      +   '<span class="lb-photo-loader-ring lb-photo-loader-ring--outer"></span>'
      +   '<span class="lb-photo-loader-ring"></span>'
      +   '<span class="lb-photo-loader-core"></span>'
      + '</div>'
      + '<div class="lb-photo-loading-copy">'
      +   '<span class="lb-photo-loading-txt">Loading player photo</span>'
      +   '<span class="lb-photo-loading-dots"><span></span><span></span><span></span></span>'
      + '</div>';
  }

  function lbApplyPlayerPhoto(p) {
    var img = document.getElementById('lb-player-img');
    var ph = document.getElementById('lb-photo-ph');
    if (!img || !ph) return;
    var mysteryHidden = !!(p && p.is_mystery && !p.mystery_revealed);

    if (!p || !p.photo_url) {
      _lbPhotoLoadToken += 1;
      _lbLastPhotoUrl = null;
      img.style.display = 'none';
      img.style.opacity = '0';
      img.style.filter = '';
      img.removeAttribute('src');
      ph.classList.remove('lb-photo-loading');
      ph.style.display = 'flex';
      ph.textContent = mysteryHidden ? '?' : '👤';
      return;
    }

    var url = p.photo_url;
    // Same photo already visible — only refresh mystery filter
    if (url === _lbLastPhotoUrl && img.getAttribute('src') === url && img.style.display !== 'none') {
      img.style.filter = mysteryHidden ? 'grayscale(1) brightness(0.72)' : '';
      img.style.opacity = '1';
      ph.classList.remove('lb-photo-loading');
      ph.style.display = 'none';
      return;
    }

    var token = ++_lbPhotoLoadToken;
    lbShowPhotoLoader();

    var pre = new Image();
    pre.onload = function () {
      if (token !== _lbPhotoLoadToken) return;
      img.src = url;
      _lbLastPhotoUrl = url;
      img.style.filter = mysteryHidden ? 'grayscale(1) brightness(0.72)' : '';
      img.style.display = 'block';
      img.style.opacity = '1';
      ph.classList.remove('lb-photo-loading');
      ph.style.display = 'none';
    };
    pre.onerror = function () {
      if (token !== _lbPhotoLoadToken) return;
      _lbLastPhotoUrl = null;
      img.style.display = 'none';
      img.style.opacity = '0';
      img.style.filter = '';
      img.removeAttribute('src');
      ph.classList.remove('lb-photo-loading');
      ph.style.display = 'flex';
      ph.textContent = mysteryHidden ? '?' : '👤';
    };
    // Cached images fire onload immediately → "spot" swap with no loader flash
    pre.src = url;
  }

  // ── Render Player Panel ────────────────────────────────────────
  function renderPlayer(p, soldInfo) {
    if (!p) {
      document.getElementById('lb-pname').textContent = 'Waiting for next player…';
      document.getElementById('lb-psub').textContent = '';
      lbApplyPlayerPhoto(null);
      document.getElementById('lb-sl-overlay').style.display = 'none';
      document.getElementById('lb-state-overlay').style.display = 'none';
      document.getElementById('lb-sold-stamp').style.display   = 'none';
      document.getElementById('lb-unsold-stamp').style.display = 'none';
      var lockClear = document.getElementById('lb-mystery-lock');
      if (lockClear) lockClear.style.display = 'none';
      document.getElementById('lb-badges-row').innerHTML = '';
      document.getElementById('lb-stats-row').style.display = 'none';
      document.getElementById('lb-base-row').style.display = 'none';
      return;
    }

    // Photo — preload before reveal so previous sold player never stays up
    lbApplyPlayerPhoto(p);

    const mysteryHidden = !!(p.is_mystery && !p.mystery_revealed);
    const mysteryLock = document.getElementById('lb-mystery-lock');

    // Name + sub — Mystery Player? only; no role ??? / small ?
    document.getElementById('lb-pname').textContent = mysteryHidden
      ? 'Mystery Player?'
      : (p.name || '—');
    const subRole = (p.tournament_type === 'football')
      ? (p.dominant_position || p.role || '')
      : (p.role || '');
    document.getElementById('lb-psub').textContent  =
      mysteryHidden ? '' : [subRole, p.tier_name].filter(Boolean).join(' · ');

    // SL badge — hidden for unrevealed mystery players
    const slEl = document.getElementById('lb-sl-overlay');
    if (p.sl_no && !mysteryHidden) { slEl.style.display = ''; document.getElementById('lb-sl-num').textContent = p.sl_no; }
    else           { slEl.style.display = 'none'; }

    // State overlay
    const stEl = document.getElementById('lb-state-overlay');
    stEl.style.display = '';
    stEl.className = 'lb-state-overlay lb-state-' + (p.state || 'auction');
    stEl.textContent = p.state === 'sold' ? 'SOLD' : p.state === 'unsold' ? 'UNSOLD' : 'LIVE';

    // Sold / Unsold stamp
    const soldStamp   = document.getElementById('lb-sold-stamp');
    const unsoldStamp = document.getElementById('lb-unsold-stamp');
    if (p.state === 'sold' && soldInfo) {
      const wasVisible = soldStamp.style.display !== 'none';
      soldStamp.style.display  = '';
      unsoldStamp.style.display = 'none';
      if (!wasVisible) {
        // reset animation only on first show
        soldStamp.style.animation = 'none';
        soldStamp.offsetHeight; // reflow
        soldStamp.style.animation = '';
      }
      document.getElementById('stamp-pts').textContent = fmtPts(soldInfo.amount);
      document.getElementById('stamp-team-name').textContent = soldInfo.team_name || '';
      const logoImg  = document.getElementById('stamp-team-logo');
      const logoWrap = document.getElementById('stamp-team-logo-wrap');
      if (soldInfo.team_logo_url) {
        logoImg.src = soldInfo.team_logo_url;
        logoWrap.style.display = '';
      } else {
        logoWrap.style.display = 'none';
      }
      stEl.style.display = 'none'; // stamp replaces the badge
    } else if (p.state === 'unsold') {
      const wasVisible = unsoldStamp.style.display !== 'none';
      unsoldStamp.style.display = '';
      soldStamp.style.display   = 'none';
      if (!wasVisible) {
        // reset animation only on first show
        unsoldStamp.style.animation = 'none';
        unsoldStamp.offsetHeight;
        unsoldStamp.style.animation = '';
      }
      stEl.style.display = 'none'; // stamp replaces the badge
    } else {
      soldStamp.style.display   = 'none';
      unsoldStamp.style.display = 'none';
    }
    if (mysteryLock) mysteryLock.style.display = mysteryHidden ? 'flex' : 'none';

    // Badges
    const isFootball = (p.tournament_type === 'football');
    let badgesHtml = '';
    if (p.tier_name && !mysteryHidden) {
      badgesHtml += `<span class="lb-badge lb-b-tier" style="background:${p.tier_color || '#2252b5'}">${p.tier_name}</span>`;
    }
    if (!mysteryHidden) {
      if (isFootball) {
        if (p.dominant_position) {
          badgesHtml += `<span class="lb-badge lb-b-role">${lbEsc(p.dominant_position)}</span>`;
        }
      } else if (p.role) {
        badgesHtml += `<span class="lb-badge lb-b-role">${p.role}</span>`;
      }
    }
    if (p.icon_player && !mysteryHidden) {
      badgesHtml += `<span class="lb-badge lb-b-icon">⭐ Icon</span>`;
    }
    document.getElementById('lb-badges-row').innerHTML = badgesHtml;

    // Stats row — hide identity attributes while mystery is locked
    const statsRow = document.getElementById('lb-stats-row');
    if (mysteryHidden) {
      statsRow.style.display = 'none';
      statsRow.innerHTML = '';
    } else if (isFootball) {
      let r1 = '';
      if (p.preferred_foot) r1 += lbStat('Foot', p.preferred_foot);
      if (p.use_other_attributes && p.other_attributes && p.other_attributes.length) {
        for (let oi = 0; oi < p.other_attributes.length; oi++) {
          const oa = p.other_attributes[oi];
          if (oa && oa.label) r1 += lbStat(oa.label, oa.value || '—');
        }
      } else {
        if (p.age) r1 += lbStat('Age', String(p.age));
        if (p.secondary_positions && p.secondary_positions.length)
          r1 += lbStat('Secondary', p.secondary_positions.join(', '));
      }
      let r2 = '';
      if (p.p_category)  r2 += lbStat('Category', p.p_category);
      if (p.blood_group) r2 += lbStat('Blood', p.blood_group);
      let sh = r1;
      if (r1 && r2) sh += '<div class="lb-stat-break"></div>';
      sh += r2;
      if (!(p.use_other_attributes && p.other_attributes && p.other_attributes.length)) {
        const fChips = lbChips(p.playing_styles) + lbChips(p.strengths);
        if (fChips) sh += (sh ? '<div class="lb-stat-break"></div>' : '') + fChips;
      }
      if (sh) {
        statsRow.style.display = 'flex';
        statsRow.innerHTML = sh;
      } else {
        statsRow.style.display = 'none';
      }
    } else if (p.batting_style || p.bowling_style) {
      statsRow.style.display = 'flex';
      statsRow.innerHTML =
        '<div class="lb-stat-chip"><div class="lb-stat-lbl">Batting</div>' +
        '<div class="lb-stat-val">' + lbEsc(p.batting_style || '—') + '</div></div>' +
        '<div class="lb-stat-chip"><div class="lb-stat-lbl">Bowling</div>' +
        '<div class="lb-stat-val">' + lbEsc(p.bowling_style || '—') + '</div></div>';
    } else {
      statsRow.style.display = 'none';
    }

    // Base price
    if (p.base_price) {
      document.getElementById('lb-base-row').style.display = '';
      document.getElementById('lb-base-val').textContent = fmtPts(p.base_price);
    } else {
      document.getElementById('lb-base-row').style.display = 'none';
    }
  }

  // ── Render Status Card ─────────────────────────────────────────
  function renderStatus(currentPlayer, soldInfo) {
    const card = document.getElementById('lb-status-card');

    if (!currentPlayer) {
      card.innerHTML = `<div class="lb-status-waiting">
        <div class="lb-auction-spinner"></div>
        <div class="lb-auction-pulse">Waiting for auction to begin…</div>
      </div>`;
      return;
    }

    if (currentPlayer.state === 'sold' && soldInfo) {
      const logoHtml = soldInfo.team_logo_url
        ? `<img src="${soldInfo.team_logo_url}" alt="logo" onerror="this.parentElement.innerHTML='🏆'">`
        : '🏆';
      card.innerHTML = `<div class="lb-status-sold">
        <div class="lb-status-label">🏆 Sold!</div>
        <div class="lb-sold-amount">${fmtPts(soldInfo.amount)}</div>
        <div class="lb-sold-team">
          <div class="lb-sold-team-logo">${logoHtml}</div>
          <div>
            <div class="lb-sold-team-name">${soldInfo.team_name}</div>
          </div>
        </div>
      </div>`;
    } else if (currentPlayer.state === 'auction') {
      const bid = (currentPlayer.current_bid > 0) ? currentPlayer.current_bid : 0;
      const bidTeam = currentPlayer.current_bid_team || null;
      let bidSection;
      if (bid > 0) {
        const logoHtml = bidTeam && bidTeam.logo_url
          ? `<img src="${bidTeam.logo_url}" alt="" onerror="this.parentElement.innerHTML='🏏'" style="width:36px;height:36px;object-fit:contain;border-radius:8px;border:1.5px solid rgba(255,255,255,0.2);">`
          : '';
        const nameHtml = bidTeam
          ? `<div style="min-width:0;">
              <div style="font-size:.95rem;font-weight:700;color:rgba(255,255,255,0.85);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${bidTeam.name}</div>
              <div style="font-size:.62rem;font-weight:700;letter-spacing:1.2px;text-transform:uppercase;color:rgba(255,255,255,0.38);margin-top:2px;">Leading Bid</div>
            </div>`
          : '';
        bidSection = `<div style="margin-top:14px;display:flex;align-items:center;gap:12px;flex-wrap:wrap;">
          <div style="font-size:2.2rem;font-weight:900;color:#60a5fa;font-variant-numeric:tabular-nums;line-height:1;letter-spacing:-1px;">${fmtPts(bid)}</div>
          ${bidTeam ? `<div style="display:flex;align-items:center;gap:10px;flex:1;min-width:0;">${logoHtml}${nameHtml}</div>` : ''}
        </div>`;
      } else {
        bidSection = `<div style="margin-top:18px;display:flex;align-items:center;gap:12px;">
          <div class="lb-auction-spinner" style="width:20px;height:20px;border-width:3px;"></div>
          <div style="font-size:1.05rem;font-weight:700;color:rgba(255,255,255,0.8);">Accepting bids from teams…</div>
        </div>`;
      }
      card.innerHTML = `<div class="lb-status-auction">
        <div class="lb-status-label">🎯 In Auction — Bidding in Progress</div>
        ${bidSection}
        <div style="margin-top:14px;font-size:.72rem;opacity:.6;">
          Base price: <strong>${fmtPts(currentPlayer.base_price)}</strong>
        </div>
      </div>`;
    } else {
      card.innerHTML = `<div class="lb-status-waiting">
        <div class="lb-auction-spinner"></div>
        <div class="lb-auction-pulse">Loading…</div>
      </div>`;
    }
  }

  // ── Render History ─────────────────────────────────────────────
  function formatHistoryMsg(msg) {
    if (!msg) return '';
    return String(msg).toUpperCase()
      .replace(/\bSOLD TO\b/g, 'sold to')
      .replace(/\bFOR\b/g, 'for');
  }

  function renderHistory(history) {
    const list = document.getElementById('lb-history-list');
    if (!history || !history.length) {
      list.innerHTML = '<div class="lb-history-empty">No transactions yet</div>';
      return;
    }
    list.innerHTML = history.map(h => {
      const avatarHtml = h.team_logo_url
        ? `<img src="${h.team_logo_url}" alt="" onerror="this.parentElement.innerHTML='🏅'">`
        : '<span class="lb-history-avatar-ph">🏅</span>';
      return `<div class="lb-history-item">
        <div class="lb-history-avatar">${avatarHtml}</div>
        <div class="lb-history-msg">${formatHistoryMsg(h.message)}</div>
        <div class="lb-history-time">${h.timestamp || ''}</div>
      </div>`;
    }).join('');
  }

  // ── Render MVP / Top Buys ──────────────────────────────────────
  function renderTopPlayers(topPlayers) {
    const list = document.getElementById('lb-mvp-list');
    if (!topPlayers || !topPlayers.length) {
      list.innerHTML = '<div class="lb-mvp-empty">No sold players yet</div>';
      return;
    }
    const medals = ['👑', '🥈', '🥉'];
    const rankClasses = ['lb-mvp-rank-gold', 'lb-mvp-rank-silver', 'lb-mvp-rank-bronze', 'lb-mvp-rank-num', 'lb-mvp-rank-num'];
    list.innerHTML = topPlayers.map((p, idx) => {
      const isMvp = idx === 0;
      const rankIcon  = medals[idx] !== undefined ? medals[idx] : String(p.rank);
      const rankClass = rankClasses[idx] || 'lb-mvp-rank-num';
      const avatarHtml = p.player_photo_url
        ? `<img src="${p.player_photo_url}" alt="" onerror="this.style.display='none';this.parentElement.innerHTML='<span class=lb-mvp-avatar-ph>👤</span>'">`
        : '<span class="lb-mvp-avatar-ph">👤</span>';
      const teamLogoHtml = p.team_logo_url
        ? `<div class="lb-mvp-team-logo"><img src="${p.team_logo_url}" alt=""></div>`
        : '';
      return `<div class="lb-mvp-item${isMvp ? ' lb-mvp-item-1' : ''}">
        <div class="lb-mvp-rank ${rankClass}">${rankIcon}</div>
        <div class="lb-mvp-avatar">
          <div class="lb-mvp-avatar-inner">${avatarHtml}</div>
          ${isMvp ? '<div class="lb-mvp-crown">👑</div>' : ''}
        </div>
        <div class="lb-mvp-info">
          <div class="lb-mvp-name">${p.player_name || '—'}</div>
          <div class="lb-mvp-sub">${p.role || ''}</div>
        </div>
        <div class="lb-mvp-right">
          <div class="lb-mvp-pts">${fmtPts(p.points)}</div>
          <div class="lb-mvp-team">
            ${teamLogoHtml}
            <span class="lb-mvp-team-name">${p.team_name || ''}</span>
          </div>
        </div>
      </div>`;
    }).join('');
  }

  // ── Render Teams Strip ─────────────────────────────────────────
  function renderTeams(teams) {
    const card = document.getElementById('lb-teams-card');
    const grid = document.getElementById('lb-teams-grid');
    if (!teams || !teams.length) { card.style.display = 'none'; return; }
    card.style.display = '';
    _teamsData = teams;
    grid.innerHTML = teams.map(function(t, idx) {
      const logoHtml = t.logo_url
        ? `<img src="${t.logo_url}" alt="" onerror="this.parentElement.innerHTML='🛡'">`
        : '<span class="lb-team-chip-logo-ph">🛡</span>';
      return `<div class="lb-team-chip" data-team-idx="${idx}" tabindex="0" role="button" aria-label="View ${t.name} squad">
        <div class="lb-team-chip-logo">${logoHtml}</div>
        <div>
          <div class="lb-team-chip-name">${t.name}</div>
          <div class="lb-team-chip-pts">${fmtPts(t.remaining_points)} left</div>
        </div>
      </div>`;
    }).join('');

    // Attach interaction events to each chip
    const isTouch = window.matchMedia('(pointer: coarse)').matches;
    Array.from(grid.querySelectorAll('.lb-team-chip')).forEach(function(chip) {
      var idx = parseInt(chip.dataset.teamIdx, 10);
      if (!isTouch) {
        // Desktop: hover shows floating popup
        chip.addEventListener('mouseenter', function() { showTeamPopup(idx, chip); });
        chip.addEventListener('mouseleave', hideTeamPopup);
        chip.addEventListener('focus',      function() { showTeamPopup(idx, chip); });
        chip.addEventListener('blur',       hideTeamPopup);
      }
      // Click / tap always opens the modal (works on both touch and desktop)
      chip.addEventListener('click', function() { openTeamModal(idx); });
    });
  }

  // ── Build squad table rows (used in popup) ─────────────────────
  function buildSquadPopupHtml(players) {
    if (!players || !players.length) {
      return '<div class="lb-tp-empty">No players signed yet</div>';
    }
    const rows = players.map(function(p) {
      const photoHtml = p.photo_url
        ? `<span class="lb-tp-photo"><img src="${p.photo_url}" alt="" onerror="this.style.display='none';this.parentElement.innerHTML='<span class=lb-tp-ph>&#128100;</span>'"/></span>`
        : '<span class="lb-tp-photo"><span class="lb-tp-ph">&#128100;</span></span>';
      const meta = playerRoleMetaHtml(p);
      return `<tr>
        <td>${photoHtml}</td>
        <td>
          <div style="font-weight:700;line-height:1.25;">${lbEsc(p.name) || '&#8212;'}</div>
          ${meta ? `<div class="lb-tm-rrole" style="margin-top:2px;">${meta}</div>` : ''}
        </td>
        <td class="lb-tp-pts">${fmtPts(p.points)}</td>
      </tr>`;
    }).join('');
    return `<table class="lb-tp-table">
      <thead><tr><th colspan="2">Player</th><th>Points</th></tr></thead>
      <tbody>${rows}</tbody>
    </table>`;
  }

  // ── Show / hide floating popup (desktop hover) ─────────────────
  function showTeamPopup(teamIdx, chipEl) {
    var team = _teamsData[teamIdx];
    if (!team) return;
    var popup   = document.getElementById('lb-team-floating-popup');
    var titleEl = document.getElementById('lb-tp-title');
    var contentEl = document.getElementById('lb-tp-content');
    titleEl.textContent = team.name + ' \u2014 Squad (' + (team.players ? team.players.length : 0) + ')';
    contentEl.innerHTML = buildSquadPopupHtml(team.players);
    popup.style.display = 'block';
    // Position above the chip, centred
    var rect = chipEl.getBoundingClientRect();
    var popW = popup.offsetWidth || 280;
    var left = rect.left + rect.width / 2 - popW / 2;
    var top  = rect.top + window.scrollY - popup.offsetHeight - 10;
    left = Math.max(8, Math.min(left, window.innerWidth - popW - 8));
    if (top < window.scrollY + 8) { top = rect.bottom + window.scrollY + 10; }
    popup.style.left = left + 'px';
    popup.style.top  = top  + 'px';
  }

  function hideTeamPopup() {
    var popup = document.getElementById('lb-team-floating-popup');
    if (popup) popup.style.display = 'none';
  }

  // ── Open team modal (mobile / click) ──────────────────────────
  function openTeamModal(teamIdx) {
    var team = _teamsData[teamIdx];
    if (!team) return;
    hideTeamPopup();
    var logoEl = document.getElementById('lb-tm-hlogo');
    logoEl.innerHTML = team.logo_url
      ? `<img src="${team.logo_url}" alt="" onerror="this.parentElement.innerHTML='&#128737;'">`
      : '&#128737;';
    document.getElementById('lb-tm-hname').textContent = team.name;
    document.getElementById('lb-tm-hpts').textContent  = fmtPts(team.remaining_points) + ' remaining';
    var body = document.getElementById('lb-tm-body');
    if (!team.players || !team.players.length) {
      body.innerHTML = '<div class="lb-tm-empty">No players signed yet</div>';
    } else {
      body.innerHTML = team.players.map(function(p) {
        const photoHtml = p.photo_url
          ? `<img src="${p.photo_url}" alt="" onerror="this.style.display='none';this.parentElement.innerHTML='<span class=lb-tm-rph>&#128100;</span>'">`
          : '<span class="lb-tm-rph">&#128100;</span>';
        const meta = playerRoleMetaHtml(p);
        return `<div class="lb-tm-row">
          <div class="lb-tm-rphoto">${photoHtml}</div>
          <div class="lb-tm-rinfo">
            <div class="lb-tm-rname">${lbEsc(p.name) || '&#8212;'}</div>
            ${meta ? `<div class="lb-tm-rrole">${meta}</div>` : ''}
          </div>
          <div class="lb-tm-rpts">${fmtPts(p.points)}</div>
        </div>`;
      }).join('');
    }
    document.getElementById('lb-team-modal-overlay').classList.add('open');
  }

  // ── Modal close handlers (event delegation — elements render after script) ──
  document.addEventListener('click', function(e) {
    if (e.target.id === 'lb-tm-hclose') {
      document.getElementById('lb-team-modal-overlay').classList.remove('open');
    } else if (e.target.id === 'lb-team-modal-overlay') {
      e.target.classList.remove('open');
    }
  });

  // ── Render Tournament Stats ────────────────────────────────────
  function setStat(id, val) {
    var el = document.getElementById(id);
    if (el) el.textContent = (val == null ? 0 : val);
  }
  function renderStats(stats) {
    var card = document.getElementById('lb-stats-card');
    if (!card) return;
    if (!stats || stats.total == null) { card.style.display = 'none'; return; }
    card.style.display = '';
    setStat('lb-stat-inauction', stats.in_auction);
    setStat('lb-stat-sold',      stats.sold);
    setStat('lb-stat-unsold',    stats.unsold);
    setStat('lb-stat-total',     stats.total);
  }

  // ── Polling ────────────────────────────────────────────────────
  function poll() {
    fetch(document.documentElement.dataset.liveDataUrl, { cache: 'no-store' })
      .then(r => r.json())
      .then(data => {
        // Apply theme + sport
        if (data.theme) {
          document.documentElement.setAttribute('data-theme', data.theme);
        }
        if (data.tournament_type || (data.tournament && data.tournament.tournament_type)) {
          _sport = (data.tournament_type || data.tournament.tournament_type || 'cricket').toLowerCase();
          document.documentElement.setAttribute('data-sport', _sport);
        }

        const p = data.current_player;
        const newId    = p ? p.id    : null;
        const newState = p ? p.state : null;
        const newMysteryRevealed = p ? !!p.mystery_revealed : null;

        // If player changed or state changed (e.g. auction→sold), flash the player panel
        const playerChanged = (newId !== lastPlayerId) || (newState !== lastState)
          || (newMysteryRevealed !== lastMysteryRevealed);
        const playerSwitched = (newId !== lastPlayerId);
        if (playerSwitched) {
          // New player: show loader immediately and render attributes now (no old face linger)
          lbShowPhotoLoader();
          renderPlayer(p, data.sold_info);
          renderStatus(p, data.sold_info);
        } else if (playerChanged && (lastPlayerId !== null || newId !== null)) {
          const panel = document.getElementById('lb-player-panel');
          panel.style.transition = 'opacity 0.3s';
          panel.style.opacity = '0';
          setTimeout(function() {
            renderPlayer(p, data.sold_info);
            renderStatus(p, data.sold_info);
            panel.style.opacity = '1';
          }, 300);
        } else {
          renderPlayer(p, data.sold_info);
          renderStatus(p, data.sold_info);
        }

        lastPlayerId = newId;
        lastState    = newState;
        lastMysteryRevealed = newMysteryRevealed;

        renderHistory(data.recent_history);
        renderTopPlayers(data.top_players || []);
        renderTeams(data.teams);
        renderStats(data.stats);
        handleBreakTime(!!data.break_time, data.tournament || {});
        updateAds(data.advertisers || []);
      })
      .catch(function() {});
  }

  // Initial load + periodic refresh with Page Visibility pause
  var _pollTimer = null;
  function startPolling() { if (!_pollTimer) _pollTimer = setInterval(poll, 2000); }
  function stopPolling()  { if (_pollTimer) { clearInterval(_pollTimer); _pollTimer = null; } }
  document.addEventListener('visibilitychange', function() {
    if (document.hidden) { stopPolling(); }
    else { poll(); startPolling(); }
  });
  window.addEventListener('beforeunload', function() { stopPolling(); clearInterval(_clockTimer); });
  poll();
  startPolling();
  window._auctionLbPoll = poll;
})();
]]></script>

<!-- BREAK TIME OVERLAY (IPL-style) -->
<div id="lb-break-overlay">
  <div class="lb-brk-glow"></div>
  <div class="lb-brk-scan"></div>
  <div class="lb-brk-bar lb-brk-bar-top"></div>
  <div class="lb-brk-bar lb-brk-bar-bot"></div>
  <div class="lb-brk-content">
    <div id="lb-brk-logo"><span class="lb-brk-logo-ph">&#127942;</span></div>
    <div class="lb-brk-badge"><span class="lb-brk-dot"></span>Break Time</div>
    <div class="lb-brk-headline">
      <span class="lb-brk-hl1">We&#x2019;ll Be</span>
      <span class="lb-brk-hl2">Right Back</span>
    </div>
    <div class="lb-brk-divider"></div>
    <div class="lb-brk-sub">The auction resumes shortly</div>
    <div class="lb-brk-rings">
      <div class="lb-brk-ring"></div>
      <div class="lb-brk-ring"></div>
      <div class="lb-brk-ring"></div>
      <div class="lb-brk-icon">&#127955;</div>
    </div>
    <div class="lb-brk-timer" id="lb-brk-timer">&#9679; Break in progress</div>
    <div class="lb-brk-ads" id="lb-brk-ads" style="display:none;">
      <div class="lb-brk-ads-label">Our Sponsors</div>
      <div class="lb-brk-ads-viewport">
        <div class="lb-brk-ads-track" id="lb-brk-ads-track"></div>
      </div>
      <div class="lb-brk-ad-dots" id="lb-brk-ad-dots"></div>
    </div>
  </div>
</div>

<!-- SPONSOR STRIP — only when tournament has advertisers configured -->
<t t-set="lb_ads" t-value="tournament.advertiser_ids if tournament else []"/>
<t t-if="lb_ads">
<div class="lb-sponsor-strip" id="lb-sponsor-strip">
  <div class="lb-spon-head"><span class="lb-spon-label">Our Sponsors</span></div>
  <div class="lb-spon-row">
    <div class="lb-spon-viewport">
      <div class="lb-spon-track" id="lb-sponsor-track">
        <t t-foreach="lb_ads" t-as="ad">
          <div class="lb-spon-item">
            <div class="lb-spon-card">
              <t t-if="ad.image">
                <img class="lb-spon-logo"
                     t-att-src="'/%s/auction/public/image/auction.advertiser/%d/image' % (db_name, ad.id)"
                     t-att-alt="ad.name" t-att-title="ad.name"/>
              </t>
              <t t-else="">
                <span class="lb-spon-name"><t t-esc="ad.name"/></span>
              </t>
            </div>
          </div>
        </t>
      </div>
    </div>
  </div>
</div>
<script>document.body.classList.add('lb-has-sponsors');</script>
</t>

<!-- FLOATING SQUAD POPUP (desktop hover) -->
<div id="lb-team-floating-popup">
  <div class="lb-tp-title" id="lb-tp-title"></div>
  <div id="lb-tp-content"></div>
</div>

<!-- TEAM PLAYERS MODAL (mobile / tap) -->
<div id="lb-team-modal-overlay">
  <div class="lb-tm-sheet">
    <div class="lb-tm-header">
      <div class="lb-tm-hlogo" id="lb-tm-hlogo"></div>
      <div style="min-width:0;flex:1;">
        <div class="lb-tm-hname" id="lb-tm-hname"></div>
        <div class="lb-tm-hpts"  id="lb-tm-hpts"></div>
      </div>
      <button class="lb-tm-hclose" id="lb-tm-hclose">&#x2715;</button>
    </div>
    <div class="lb-tm-body" id="lb-tm-body"></div>
  </div>
</div>

</body>
</html>
</template>


<!-- ══════════════════════════════════════════════════════════════════════
     AUCTION COMPLETE — THANK YOU PAGE  (/auction/display_auction/)
     Shown when no auction-state players remain AND at least one non-icon
     player has been sold, indicating the auction has officially concluded.
     ══════════════════════════════════════════════════════════════════════ -->
<!-- thank_you_template moved to auction_thank_you_template.xml -->



<!-- ══════════════════════════════════════════════════════════════════════
     LIVE BOARD OFFLINE TEMPLATE  (/auction/live-board — board not active)
     Shown when the tournament admin has stopped the live board.
     ══════════════════════════════════════════════════════════════════════ -->
<template id="live_board_offline_template" name="Live Board Offline">
<t t-set="theme_val" t-value="theme or 'vanilla'"/>
<html t-att-data-theme="theme_val">
<head>
  <meta charset="UTF-8"/>
  <link rel="shortcut icon" t-att-href="'/web/image/res.company/%d/favicon' % res_company.id" type="image/x-icon"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Live Board — Offline</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;900&amp;family=Rajdhani:wght@400;600;700&amp;display=swap" rel="stylesheet"/>
  <style>
    /* ── Theme variables (mirrors the live board palette) ──────────── */
    :root {
      --off-bg:        #070c18;
      --off-surface:   #0d1526;
      --off-border:    rgba(255,255,255,0.07);
      --off-accent:    #3b72e8;
      --off-accent2:   #5d93ff;
      --off-glow:      rgba(59,114,232,0.30);
      --off-text:      #e8edf8;
      --off-sub:       #8896b3;
      --off-dim:       rgba(255,255,255,0.04);
    }
    [data-theme="cherry"] {
      --off-bg:       #110206;
      --off-surface:  #180306;
      --off-border:   rgba(220,38,38,0.15);
      --off-accent:   #e02626;
      --off-accent2:  #f87171;
      --off-glow:     rgba(220,38,38,0.32);
      --off-text:     #fef0f0;
      --off-sub:      #f0948a;
    }
    [data-theme="butterscotch"] {
      --off-bg:       #0e0800;
      --off-surface:  #150b00;
      --off-border:   rgba(217,119,6,0.18);
      --off-accent:   #d97706;
      --off-accent2:  #fbbf24;
      --off-glow:     rgba(217,119,6,0.32);
      --off-text:     #fef3c7;
      --off-sub:      #d4a843;
    }
    [data-theme="strawberry"] {
      --off-bg:       #0d0010;
      --off-surface:  #160018;
      --off-border:   rgba(219,39,119,0.18);
      --off-accent:   #db2777;
      --off-accent2:  #f472b6;
      --off-glow:     rgba(219,39,119,0.32);
      --off-text:     #fdf2f8;
      --off-sub:      #f0abca;
    }
    
    [data-theme="blackberry"] {
      --off-bg:       #050a14;
      --off-surface:  #0a1220;
      --off-border:   rgba(59,130,246,0.18);
      --off-accent:   #3B82F6;
      --off-accent2:  #93C5FD;
      --off-glow:     rgba(59,130,246,0.35);
      --off-text:     #ffffff;
      --off-sub:      #93c5fd;
    }

    [data-theme="pistah"] {
      --off-bg:       #050d05;
      --off-surface:  #0b1a0b;
      --off-border:   rgba(34,197,94,0.15);
      --off-accent:   #16a34a;
      --off-accent2:  #4ade80;
      --off-glow:     rgba(34,197,94,0.30);
      --off-text:     #f0fdf4;
      --off-sub:      #86efac;
    }

    [data-theme="lemon"] {
      --off-bg:       #12100a;
      --off-surface:  #1a1608;
      --off-border:   rgba(232,194,0,0.18);
      --off-accent:   #E8C200;
      --off-accent2:  #F5D84A;
      --off-glow:     rgba(232,194,0,0.32);
      --off-text:     #fffdf0;
      --off-sub:      #c9b86a;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Montserrat', sans-serif;
      background: var(--off-bg);
      color: var(--off-text);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    /* subtle animated grid */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
      background-size: 48px 48px;
      pointer-events: none;
      z-index: 0;
    }

    /* radial glow behind card */
    body::after {
      content: '';
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 700px;
      height: 700px;
      background: radial-gradient(circle, var(--off-glow) 0%, transparent 70%);
      pointer-events: none;
      z-index: 0;
    }

    .off-card {
      position: relative;
      z-index: 1;
      background: var(--off-surface);
      border: 1px solid var(--off-border);
      border-radius: 20px;
      padding: 56px 64px;
      max-width: 600px;
      width: 90%;
      text-align: center;
      box-shadow: 0 8px 48px rgba(0,0,0,0.55);
    }

    /* tournament logo */
    .off-logo {
      width: 88px;
      height: 88px;
      object-fit: contain;
      border-radius: 12px;
      margin-bottom: 16px;
      filter: drop-shadow(0 4px 16px var(--off-glow));
    }
    .off-logo-placeholder {
      width: 88px;
      height: 88px;
      border-radius: 12px;
      margin: 0 auto 16px;
      background: var(--off-dim);
      border: 1px solid var(--off-border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.4rem;
    }
    .off-tournament-name {
      font-family: 'Rajdhani', 'Montserrat', sans-serif;
      font-size: 1.1rem;
      font-weight: 700;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--off-accent2);
      margin-bottom: 28px;
    }

    /* status pill */
    .off-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(239,68,68,0.12);
      border: 1px solid rgba(239,68,68,0.30);
      border-radius: 100px;
      padding: 6px 18px;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.14em;
      color: #f87171;
      text-transform: uppercase;
      margin-bottom: 32px;
    }
    .off-badge-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #ef4444;
    }

    .off-headline {
      font-family: 'Rajdhani', 'Montserrat', sans-serif;
      font-size: 2.2rem;
      font-weight: 700;
      letter-spacing: 0.03em;
      line-height: 1.15;
      margin-bottom: 16px;
      color: var(--off-text);
    }

    .off-sub {
      font-size: 0.95rem;
      color: var(--off-sub);
      line-height: 1.7;
      max-width: 420px;
      margin: 0 auto 40px;
    }

    /* divider */
    .off-divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--off-accent), transparent);
      opacity: 0.35;
      margin-bottom: 36px;
    }

    /* info pills row */
    .off-info-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
      margin-bottom: 40px;
    }
    .off-info-item {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 0.82rem;
      color: var(--off-sub);
    }
    .off-info-item i {
      color: var(--off-accent2);
      font-size: 0.9rem;
    }

    /* refresh hint */
    .off-refresh {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: var(--off-dim);
      border: 1px solid var(--off-border);
      border-radius: 10px;
      padding: 12px 24px;
      font-size: 0.82rem;
      color: var(--off-sub);
    }
    .off-refresh i {
      color: var(--off-accent2);
      animation: spin 3s linear infinite;
    }
    @keyframes spin { to { transform: rotate(360deg); } }

    /* footer */
    .off-footer {
      position: fixed;
      bottom: 24px;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 0.72rem;
      color: rgba(255,255,255,0.18);
      letter-spacing: 0.08em;
      z-index: 1;
    }
  </style>
</head>
<body>
  <div class="off-card">

    <!-- Logo + Tournament name -->
    <t t-if="tournament.logo">
      <img class="off-logo"
           t-att-src="'data:image/png;base64,' + tournament.logo.decode('utf-8')"
           t-att-alt="tournament.name"/>
    </t>
    <t t-else="">
      <div class="off-logo-placeholder">&#127942;</div>
    </t>
    <div class="off-tournament-name"><t t-esc="tournament.name"/></div>

    <!-- Status badge -->
    <div class="off-badge">
      <span class="off-badge-dot"/>
      Broadcast Offline
    </div>

    <!-- Headline -->
    <div class="off-headline">Live Updates Are Not Active</div>

    <!-- Subtitle -->
    <p class="off-sub">
      The live auction board for <strong><t t-esc="tournament.name"/></strong> is currently
      offline. The broadcast will resume when the auction administrator activates the live feed.
    </p>

    <div class="off-divider"/>

    <!-- Info row -->
    <div class="off-info-row">
      <span class="off-info-item">
        <i class="fa fa-shield-alt"/>
        <span>Official Auction Platform</span>
      </span>
      <span class="off-info-item">
        <i class="fa fa-clock"/>
        <span>Updates stream in real-time when live</span>
      </span>
      <span class="off-info-item">
        <i class="fa fa-lock"/>
        <span>Secure &amp; Verified Feed</span>
      </span>
    </div>

    <!-- Refresh hint -->
    <div class="off-refresh">
      <i class="fa fa-sync-alt"/>
      This page will automatically refresh when the board goes live.
    </div>
  </div>

  <div class="off-footer">
    <t t-esc="tournament.name"/> &#160;&#183;&#160; Powered by Auction Champ
  </div>

  <script>
    // Poll every 15 seconds; reload the page when the board goes live
    (function () {
      var dataUrl = window.location.href.replace('/auction/live-board', '/auction/live-board/data');
      function check() {
        fetch(dataUrl)
          .then(function(r){ return r.json(); })
          .then(function(d){
            if (d.live_board_active !== false) {
              window.location.reload();
            }
          })
          .catch(function(){});
      }
      setInterval(check, 15000);
    })();
  </script>
</body>
</html>
</template>



</odoo>
