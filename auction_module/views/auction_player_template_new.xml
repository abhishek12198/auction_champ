<?xml version="1.0" encoding="utf-8"?>
<odoo>
<template id="player_template_new" name="Player Auction">
    <html lang="en" t-att-data-balance-url="('/' + db_name + '/' + tournament.slug + '/auction/show/team/balance') if db_name and tournament and tournament.slug else ''" t-att-data-rp-url="('/' + db_name + '/' + tournament.slug + '/auction/display_auction/remaining-players?theme=vanilla') if db_name and tournament and tournament.slug else ''">
  <head>
    <meta charset="UTF-8" />
  <link rel="shortcut icon" t-att-href="'/web/image/res.company/%d/favicon' % res_company.id" type="image/x-icon"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <!-- google font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="1" />
    <link
      href="https://fonts.googleapis.com/css2?family=Teko:wght@600&amp;display=swap"
      rel="stylesheet"
    />
    <!-- Athletic / broadcast typography (EPSOR fallbacks) -->
    <link
      href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;600;700&amp;family=Oswald:wght@500;600;700&amp;family=Bebas+Neue&amp;family=Montserrat:wght@600;700&amp;display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/auction_module/static/src/assets/css/style.css" />
    <title>Player Showcase</title>

    <style>
/* ── Vanilla Theme Toggle ── */
#vn-theme-btn{position:fixed;top:14px;right:62px;z-index:9999;background:rgba(40,0,125,0.08);border:1px solid rgba(40,0,125,0.25);border-radius:20px;padding:5px 14px;cursor:pointer;font-family:'Poppins',sans-serif;font-size:11px;font-weight:600;color:#28007d;letter-spacing:0.04em;transition:all 0.2s}
#vn-theme-btn:hover{background:rgba(40,0,125,0.16);box-shadow:0 0 8px rgba(40,0,125,0.20)}
/* Dark mode overrides */
body[data-theme="dark"]{background-image:none!important;background-color:#0d1117}
body[data-theme="dark"] header{background-image:none!important;background-color:#161b22;border-bottom:1px solid rgba(46,90,234,0.30)}
body[data-theme="dark"] .each-club-list,body[data-theme="dark"] .player-profession-body{background-color:#1f2937}
body[data-theme="dark"] h3{color:#e6edf3}
body[data-theme="dark"] h2{color:#79b8ff}
body[data-theme="dark"] .player-name{color:#e6edf3}
body[data-theme="dark"] .player-possition{color:#79b8ff}
body[data-theme="dark"] .league-name,body[data-theme="dark"] .league-name-logo{background-color:#1f2937}
body[data-theme="dark"] .team-name h3,body[data-theme="dark"] .team-name p{color:#c9d1d9}
body[data-theme="dark"] .team-name .bg-white{background-color:#1f2937!important}
body[data-theme="dark"] #vn-theme-btn{background:rgba(121,184,255,0.10);border-color:rgba(121,184,255,0.30);color:#79b8ff}
    
    .sp-image, .sp-image img, #loadImage {
      object-fit: cover !important; object-position: center top;
      border-radius: 14px; box-shadow: 0 16px 40px rgba(0,0,0,.35);
    }
    .player-name {
      text-transform: uppercase; letter-spacing: .06em;
      text-shadow: 0 0 24px rgba(59,114,232,.25);
    }
    .player-attr-grid .player-attr-label { opacity: .7; letter-spacing: .12em; }

  </style>

    <!-- ══════════════════════════════════════════════════════════════
         VANILLA · PREMIUM IPL-STYLE BROADCAST PRESENTATION (visual only)
         Royal-blue + white + silver · stadium LED look. Scoped to this
         template only (no shared style.css changes, no HTML/JS changes).
         ══════════════════════════════════════════════════════════════ -->
    <style>
@font-face{font-family:'Varsity Team';src:url('/auction_module/static/src/assets/fonts/VarsityTeam-Bold.otf') format('opentype');font-weight:normal;font-style:normal;font-display:swap;}
:root{
  --vn-royal:#5b7eb8;
  --vn-royal-lt:#8aa6d4;
  --vn-navy:#1a3560;
  --vn-navy-deep:#142846;
  --vn-silver:#c8d4e8;
  --vn-head:'Varsity Team','EPSOR','Rajdhani','Oswald','Bebas Neue','Montserrat',sans-serif;
}

/* ── Stadium environment (layered, soft pale blue — easier on eyes) ── */
html{
  background:radial-gradient(120% 82% at 50% -12%,
              #7a9bc4 0%, #5b7eb8 28%, #3d5f94 55%, #2a4a78 78%, #1a3560 100%) fixed;
}
body{
  background:transparent !important;
  color:#eaf1ff;
  position:relative;
  z-index:0;
  overflow-x:hidden;
}
/* Floodlights · LED panel lines · stadium depth */
body::before{
  content:"";position:fixed;inset:0;z-index:-1;pointer-events:none;
  background:
    radial-gradient(closest-side at 12% -8%, rgba(190,215,255,.34), rgba(190,215,255,0) 60%),
    radial-gradient(closest-side at 88% -8%, rgba(190,215,255,.34), rgba(190,215,255,0) 60%),
    radial-gradient(closest-side at 50% -15%, rgba(255,255,255,.20), rgba(255,255,255,0) 55%),
    repeating-linear-gradient(90deg, rgba(255,255,255,.035) 0 2px, rgba(255,255,255,0) 2px 48px);
}
/* Pitch glow · fog · speed lines · seating silhouette */
body::after{
  content:"";position:fixed;inset:0;z-index:-1;pointer-events:none;
  background:
    radial-gradient(92% 55% at 50% 120%, rgba(96,152,255,.30), rgba(96,152,255,0) 60%),
    radial-gradient(58% 30% at 50% 130%, rgba(212,231,255,.20), rgba(212,231,255,0) 70%),
    repeating-linear-gradient(114deg, rgba(255,255,255,.028) 0 1px, rgba(255,255,255,0) 1px 92px),
    repeating-linear-gradient(0deg, rgba(0,10,40,.16) 0 3px, rgba(0,10,40,0) 3px 9px) 0 82% / 100% 18% no-repeat,
    linear-gradient(180deg, rgba(7,29,77,0) 58%, rgba(6,22,64,.55) 100%);
}
/* Keep all real content crisply above the background layers */
.single-header,
[data-player-zone],
.container{position:relative; z-index:2;}

/* ── Header ── */
.single-header{
  background-image:none !important;
  background:linear-gradient(180deg, rgba(26,53,96,.5), rgba(26,53,96,0)) !important;
  height:272px;
  border-bottom:1px solid rgba(160,185,215,.28);
  box-shadow:0 1px 0 rgba(140,170,205,.28);
}
.single-header .py-3{border-bottom:1px solid rgba(255,255,255,.14) !important;}
.vn-brand{
  display:inline-flex;align-items:center;gap:12px;
  text-decoration:none;color:inherit;
}
.vn-brand img{max-height:32px;width:auto;display:block;}
.vn-brand-text{display:flex;flex-direction:column;gap:2px;text-align:left;}
.vn-brand-sub{
  font-family:var(--vn-head);
  font-size:.9rem;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  color:#fff;line-height:1;
}
body[data-theme="light"] .vn-brand-sub{color:#1a3560}
body[data-theme="dark"] .vn-brand-sub{color:#fff}
@media (max-width:768px){
  .vn-brand-sub{display:none;}
}
.team-name h3{
  color:#1a3560;
  font-family:var(--vn-head);
  letter-spacing:.6px;
  text-transform:uppercase;
}
.team-name p{color:#4a6fa5;}
.team-name .bg-white{
  background:linear-gradient(180deg,#ffffff,#eef4ff) !important;
  border-radius:8px 0 0 8px;
  box-shadow:0 8px 22px rgba(0,0,0,.28);
}
.team-single-logo{
  box-shadow:0 0 0 3px rgba(255,255,255,.92), 0 0 20px rgba(91,155,255,.7);
}

/* ── Serial number: hollow, glowing, athletic ── */
.stroke-only{
  font-family:var(--vn-head) !important;
  color:transparent;
  line-height:1 !important;
  margin:0 0 12px !important;
  -webkit-text-stroke:2.5px rgba(255,255,255,.88);
  text-shadow:0 0 28px rgba(91,155,255,.6);
  filter:drop-shadow(0 8px 20px rgba(0,0,0,.45));
}

/* ── Player name + role ── */
.player-name{
  font-family:var(--vn-head) !important;
  color:#ffffff !important;
  text-transform:uppercase;
  letter-spacing:2px;
  font-weight:700;
  font-size:clamp(1.6rem, 3vw, 2.9rem) !important;
  line-height:1.1 !important;
  white-space:normal !important;
  text-shadow:0 2px 12px rgba(0,0,0,.5), 0 0 24px rgba(91,155,255,.35);
}
.player-possition{
  font-family:var(--vn-head) !important;
  color:var(--vn-silver) !important;
  font-style:normal !important;
  font-weight:600;
  letter-spacing:3px;
  text-transform:uppercase;
  font-size:clamp(1.35rem, 2.5vw, 2rem) !important;
  line-height:1.3 !important;
  text-shadow:0 1px 6px rgba(0,0,0,.4);
}
.player-tier-badge{
  box-shadow:0 6px 18px rgba(0,0,0,.35), 0 0 16px rgba(91,155,255,.4);
  font-size:clamp(1.15rem, 2.2vw, 1.7rem) !important;
  padding:10px 22px !important;
  border-radius:12px !important;
  display:inline-flex !important;
  align-items:center;
  font-weight:700;
  letter-spacing:1px;
  text-transform:uppercase;
  margin-top:0 !important;
}
.player-highlight-badges{
  display:flex; flex-wrap:wrap; align-items:stretch; gap:12px;
  margin-top:14px;
}
.player-base-badge{
  display:inline-flex; flex-direction:column;
  align-items:flex-start; justify-content:center; gap:3px;
  padding:10px 18px 12px; border-radius:12px;
  background:linear-gradient(135deg, #c9a227, #e8c547 50%, #fff4c2);
  color:#0a1628;
  border:2px solid rgba(255,255,255,.35);
  box-shadow:0 6px 20px rgba(232,197,71,.4);
}
.player-base-badge-lbl{
  font-family:'Rajdhani',sans-serif;
  font-size:.65rem; font-weight:700; letter-spacing:1.5px;
  text-transform:uppercase; opacity:.8; line-height:1;
}
.player-base-badge-val{
  font-family:var(--vn-head),sans-serif;
  font-size:clamp(1.3rem, 2.5vw, 1.95rem); font-weight:700;
  letter-spacing:.5px; line-height:1;
}
/* Powered-by footer */
.vn-footer{
  position:fixed;bottom:0;left:0;right:0;height:42px;z-index:40;
  display:flex;align-items:center;justify-content:center;gap:10px;
  background:rgba(7,18,40,.92);
  border-top:1px solid rgba(180,205,255,.18);
}
.vn-footer-lbl{
  font-family:'Rajdhani',sans-serif;font-size:.62rem;font-weight:700;
  letter-spacing:2.2px;text-transform:uppercase;color:rgba(234,241,255,.5);
}
.vn-footer-logo{
  height:22px;width:auto;max-width:140px;object-fit:contain;display:block;
  filter:drop-shadow(0 0 8px rgba(91,155,255,.4));
}
body[data-theme="light"] .vn-footer{
  background:#f4f7fc;border-top-color:rgba(91,126,184,.16);
}
body[data-theme="light"] .vn-footer-lbl{color:rgba(20,40,80,.45)}
body[data-theme="light"] .vn-footer-logo{
  filter:brightness(0) sepia(1) saturate(6) hue-rotate(190deg) brightness(.55);
}
body{padding-bottom:42px;}

/* ── Player image: broadcast cut-out with soft blue glow frame ── */
.single-player-image .sp-image{
  border-radius:16px;
  background:linear-gradient(180deg, rgba(91,126,184,.2), rgba(20,40,70,.45));
  animation:vnFrameGlow 6s ease-in-out infinite;
}
@keyframes vnFrameGlow{
  0%,100%{box-shadow:0 0 0 3px rgba(255,255,255,.9),0 0 0 8px rgba(91,126,184,.35),
                     0 0 40px rgba(91,126,184,.28),0 30px 70px rgba(0,0,0,.45);}
  50%    {box-shadow:0 0 0 3px rgba(255,255,255,.96),0 0 0 8px rgba(91,126,184,.48),
                     0 0 58px rgba(138,166,212,.4),0 30px 70px rgba(0,0,0,.45);}
}
.single-player-image img{border-radius:14px;}
.single-player-image .sp-image-before{
  background-color:var(--tier-color, #5b7eb8) !important;
  border-radius:16px;
  opacity:.9;
}

/* ── Attribute panel: frosted-glass sports card ── */
.player-profession{
  border-radius:16px !important;
  overflow:hidden;
  box-shadow:0 24px 55px rgba(0,0,0,.42), inset 0 0 0 1px rgba(255,255,255,.14);
  -webkit-backdrop-filter:blur(10px) saturate(1.1);
  backdrop-filter:blur(10px) saturate(1.1);
}
.player-profession-header{
  background:linear-gradient(90deg,#3d5f94,#5b7eb8) !important;
  font-family:var(--vn-head) !important;
  font-style:normal !important;
  letter-spacing:3px;
  color:#fff !important;
  border-bottom:1px solid rgba(255,255,255,.2);
  text-shadow:0 1px 4px rgba(0,0,0,.35);
}
.player-profession-body{
  background:linear-gradient(180deg, rgba(18,52,120,.55), rgba(9,30,80,.55)) !important;
  display:block !important;
  grid-template-columns:none !important;
  gap:0 !important;
}
.player-profession-body > .vn-fb-grid,
.player-profession-body > .player-attr-grid{
  display:grid !important;
}
.player-profession-body .vn-stat-row{
  display:flex !important;
  flex-direction:column !important;
  gap:4px !important;
}
.player-attr-label{
  color:var(--vn-silver) !important;
  font-family:'Rajdhani', sans-serif;
  font-weight:600;
  letter-spacing:.6px;
  text-transform:uppercase;
  font-size:clamp(.65rem, 1vw, .78rem) !important;
}
.player-attr-value{
  color:#ffffff !important;
  font-family:var(--vn-head), 'Rajdhani', sans-serif;
  font-weight:700;
  font-size:clamp(1.3rem, 2.3vw, 1.85rem) !important;
  line-height:1.1 !important;
  letter-spacing:.5px;
  text-shadow:0 1px 4px rgba(0,0,0,.35);
  text-transform:uppercase;
}

/* Football attrs: lemon-style tiles (2-col cards), not flat label:value list */
.vn-fb-grid{
  display:grid;
  grid-template-columns:repeat(2, minmax(0, 1fr));
  gap:10px 12px;
  width:100%;
}
.vn-fb-grid > .vn-fb-span-2,
.vn-fb-grid > .player-tag-chips{
  grid-column:1 / -1;
}
.vn-stat-row{
  display:flex;
  flex-direction:column;
  gap:4px;
  margin:0;
  padding:12px 14px 14px;
  border-radius:12px;
  border:1.5px solid rgba(121,184,255,.35);
  background:rgba(255,255,255,.08);
  box-sizing:border-box;
  min-width:0;
  height:100%;
  transition:border-color .2s, box-shadow .2s;
}
.vn-stat-row:hover{
  border-color:rgba(121,184,255,.7);
  box-shadow:0 4px 14px rgba(0,0,0,.22);
}
.vn-stat-key{
  font-family:'Rajdhani', sans-serif;
  font-size:clamp(.58rem, .85vw, .72rem);
  font-weight:700;
  letter-spacing:2px;
  text-transform:uppercase;
  color:var(--vn-silver, #c5d4f0);
  opacity:.85;
  line-height:1.2;
}
.vn-stat-val{
  font-family:var(--vn-head), 'Rajdhani', sans-serif;
  font-size:clamp(1.05rem, 1.9vw, 1.45rem);
  font-weight:700;
  letter-spacing:.5px;
  line-height:1.15;
  color:#fff;
  text-transform:uppercase;
  text-shadow:0 1px 4px rgba(0,0,0,.35);
  word-break:break-word;
  overflow-wrap:anywhere;
}
.vn-fb-grid .player-tag-chips{
  margin-top:6px;
  display:grid;
  grid-template-columns:repeat(4, minmax(0, 1fr));
  gap:6px;
  width:100%;
  overflow:visible;
}
.vn-fb-grid .player-tag-chip{
  display:inline-flex !important;
  align-items:center;
  justify-content:center;
  width:100%;
  min-width:0;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
  padding:3px 6px !important;
  font-size:clamp(.55rem, .8vw, .66rem) !important;
  font-weight:600;
  letter-spacing:.2px;
  line-height:1.2;
  border-radius:999px;
  box-sizing:border-box;
}
@media (max-width:700px){
  .vn-fb-grid{grid-template-columns:1fr;}
  .vn-fb-grid .player-tag-chips{grid-template-columns:repeat(2, minmax(0, 1fr));}
}

/* ── Action buttons (same actions, premium finish) ── */
.auction-single-buttons button{
  font-family:var(--vn-head) !important;
  letter-spacing:1.5px;
  text-transform:uppercase;
  border-radius:12px !important;
  border:1.5px solid rgba(180,205,255,.6) !important;
  color:#eaf1ff !important;
  background:rgba(255,255,255,.06) !important;
  -webkit-backdrop-filter:blur(6px);
  backdrop-filter:blur(6px);
  box-shadow:0 6px 18px rgba(0,0,0,.3);
  transition:.25s ease;
}
.auction-single-buttons button:hover{
  background:rgba(138,166,212,.2) !important;
  border-color:rgba(200,212,230,.85) !important;
  box-shadow:0 0 18px rgba(138,166,212,.35) !important;
  transform:translateY(-2px);
  cursor:pointer !important;
}
.auction-single-buttons button.sell{
  border:none !important;
  color:#1a3560 !important;
  font-weight:700;
  background:linear-gradient(180deg,#eef3f9,#c5d4e8) !important;
  box-shadow:0 8px 20px rgba(91,126,184,.35), inset 0 0 0 1px rgba(255,255,255,.55) !important;
}
.auction-single-buttons button.sell:hover{
  background:linear-gradient(180deg,#ffffff,#d5e0ef) !important;
  color:#142846 !important;
  transform:translateY(-2px);
}

/* ── Next player button ── */
.next-player-btn{
  font-family:var(--vn-head) !important;
  letter-spacing:2px;
  text-transform:uppercase;
  border:1px solid rgba(170,190,215,.5) !important;
  border-radius:12px !important;
  background:linear-gradient(180deg,#5b7eb8,#3d5f94) !important;
  box-shadow:0 10px 24px rgba(61,95,148,.4), inset 0 1px 0 rgba(255,255,255,.3) !important;
}
.next-player-btn:hover{
  background:linear-gradient(180deg,#6b8fc4,#4a6fa5) !important;
  box-shadow:0 0 24px rgba(138,166,212,.4) !important;
}

/* ── Premium dark-mode variant (toggle preserved) ── */
body[data-theme="dark"]{
  background:radial-gradient(120% 82% at 50% -12%,
              #16305f 0%, #0d1f45 45%, #071634 80%, #030c22 100%) fixed !important;
  background-color:transparent !important;
}
body[data-theme="dark"] .single-header{
  background:linear-gradient(180deg, rgba(4,16,44,.6), rgba(4,16,44,0)) !important;
}

/* ── Bid Summary + Players Left drawers: match soft vanilla blue ── */
#bidSummaryDrawer, #rpDrawer{
  border-left:3px solid var(--vn-royal) !important;
  box-shadow:-6px 0 40px rgba(26,53,96,.3) !important;
}
/* Fold/unfold tab handles */
#bidSummaryDrawer > div[onclick], #rpDrawer > div[onclick]{
  background:linear-gradient(180deg,#5b7eb8,#3d5f94) !important;
  box-shadow:-4px 2px 14px rgba(26,53,96,.35) !important;
}
/* Header bars (2nd div: title + close) */
#bidSummaryDrawer > div:nth-of-type(2), #rpDrawer > div:nth-of-type(2){
  background:linear-gradient(90deg,#3d5f94,#5b7eb8) !important;
}
/* Athletic font + spacing for all drawer labels, titles and close buttons */
#bidSummaryDrawer span, #rpDrawer span,
#bidSummaryDrawer button, #rpDrawer button{
  font-family:var(--vn-head) !important;
  letter-spacing:1px !important;
}

/* ── Large LED / TV screens: scale hierarchy up ── */
@media (min-width:1600px){
  .player-name{font-size:clamp(2.2rem, 2.6vw, 3.4rem) !important;}
  .player-possition{font-size:1.55rem !important;}
  .stroke-only{font-size:14rem;}
  .player-profession{max-width:560px;}
}

/* ── Mobile: keep the brand logo clear of the top-right controls ── */
@media (max-width:768px){
  .single-header{height:auto !important;}
  .single-header .py-3{padding:.5rem 104px .5rem 14px !important; text-align:left !important;}
  .single-header .py-3 img{max-height:28px; width:auto;}
  .vn-brand{justify-content:flex-start;}
  #vn-theme-btn{top:10px; right:50px; padding:4px 10px; font-size:10px;}
  .team-single-logo{width:70px !important; height:70px !important;}
}
    </style>
    <t t-call="auction_module.player_mystery_critical_css"/>
  </head>
  <body style="min-height: 100vh" t-att-data-player-id="player.id" t-att-data-next-countdown="tournament.next_player_countdown if tournament else 5" t-att-data-is-mystery="'1' if player.tier_id and player.tier_id.mystery else '0'" t-att-data-tournament-id="tournament.id if tournament else ''" t-att-data-break-time="'1' if tournament and tournament.break_time_active else '0'">
    <header class="single-header">
      <div class="py-3 text-center">
        <a href="/web" class="vn-brand">
          <img src="/auction_module/static/src/assets/images/logo.svg" alt="Auction Champ"/>
          <div class="vn-brand-text">
            <div class="vn-brand-sub">Vanilla Edition</div>
          </div>
        </a>
      </div>
      <div class="container">
        <div class="position-relative" style="column-gap: 8px">
          <!--<a
            class="back-btn d-none d-sm-flex justify-content-center align-items-center position-absolute"
            href="#"
          >
            <img
              src="/auction_module/static/src/assets/images/back-btn.svg"
              alt="back button"
              width="43px"i
              height="43px"
              style="background-color: transparent"
            />
          </a>-->
          <div
            class="team-name d-flex justify-content-center justify-content-sm-end align-items-center"
          >
            <div class="d-none d-sm-block bg-white" style="min-height: 58px">
              <div>
                <h3><span t-esc="player.tournament_id.name"/></h3>
                <p><span t-esc="player.tournament_id.description"/></p>
              </div>
            </div>
            <img
              t-att-src="image_data_uri(player.tournament_id.logo)"
              alt="team-logo"
              width="91"
              height="91"
              class="team-single-logo"
            />
          </div>
        </div>
      </div>
      <button id="vn-theme-btn" onclick="toggleVnTheme()">
        <span id="vn-th-dark" style="display:none">&#9728; Light</span>
        <span id="vn-th-light">&#9790; Dark</span>
      </button>
    </header>

    <div class="container" data-player-zone="">
      <div
        class="row auction-single justify-content-center justify-content-lg-between gap-4"
      >
        <div class="col-sm-3 single-player-details">
          <h3 class="stroke-only"><span t-esc="player.sl_no"/></h3>
          <h6 class="player-name"><span data-sm-name="" t-esc="player.name.upper()"/></h6>
          <p class="player-possition"><span data-sm-role="" t-esc="(player.dominant_position_id.name if player.tournament_type == 'football' else player.role) or ''"/></p>
          <div class="player-highlight-badges">
            <t t-if="player.tier_id">
              <span class="player-tier-badge"
                    t-att-style="'background-color: ' + (player.tier_id.color or '#3498db') + '; color: #fff;'">
                <t t-esc="player.tier_id.name"/>
              </span>
            </t>
            <t t-if="player.effective_base_price">
              <span class="player-base-badge">
                <span class="player-base-badge-lbl">Base Price</span>
                <span class="player-base-badge-val"><t t-esc="'{:,}'.format(int(player.effective_base_price or 0))"/> pts</span>
              </span>
            </t>
          </div>
        </div>

        <div class="col-sm-3">
          <div class="position-relative single-player-image">
            <div class="sp-image m-auto">
              <div class="sp-image-before" t-att-style="'--tier-color: ' + (player.tier_id.color if player.tier_id else '#01cfff')"></div>
              <!-- Use an image with dimensions 380x553 for optimal results and to ensure it fits well within the design. -->
              <img
                t-att-src="image_data_uri(player.photo)"
                alt="player-image"
                id="loadImage"
              />
            </div>
          </div>
        </div>

        <div class="col-4 col-lg-5">
          <div class="player-profession m-auto">
            <div class="player-profession-header">
              <p>Player Attributes</p>
            </div>
            <div class="player-profession-body">
              <t t-if="player.tournament_type != 'football'">
                <div class="player-attr-grid">
                  <t t-if="player.batting_style">
                    <span class="player-attr-label">Batting Style</span>
                    <span class="player-attr-value"><t t-esc="player.batting_style"/></span>
                  </t>
                  <t t-if="player.bowling_style">
                    <span class="player-attr-label">Bowling Style</span>
                    <span class="player-attr-value"><t t-esc="player.bowling_style"/></span>
                  </t>
                  <t t-if="player.p_category">
                    <span class="player-attr-label">Player Category</span>
                    <span class="player-attr-value"><t t-esc="player.p_category"/></span>
                  </t>
                  <t t-if="player.address">
                    <span class="player-attr-label">Location</span>
                    <span class="player-attr-value"><t t-esc="player.address"/></span>
                  </t>
                </div>
              </t>
              <t t-else="">
                <div class="vn-fb-grid">
                  <t t-if="player.dominant_position_id">
                    <div class="vn-stat-row">
                      <span class="vn-stat-key">Position</span>
                      <span class="vn-stat-val"><t t-esc="player.dominant_position_id.name"/></span>
                    </div>
                  </t>
                  <t t-if="player.preferred_foot">
                    <div class="vn-stat-row">
                      <span class="vn-stat-key">Foot</span>
                      <span class="vn-stat-val"><t t-esc="player.preferred_foot.capitalize()"/></span>
                    </div>
                  </t>
                  <t t-if="player.use_other_attributes">
                    <t t-foreach="player.other_attribute_ids.filtered('value')" t-as="oattr">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key"><t t-esc="oattr.label"/></span>
                        <span class="vn-stat-val"><t t-esc="oattr.value or '—'"/></span>
                      </div>
                    </t>
                  </t>
                  <t t-else="">
                    <t t-if="player.secondary_position_ids">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key">Secondary</span>
                        <span class="vn-stat-val"><t t-esc="', '.join(player.secondary_position_ids.mapped('code'))"/></span>
                      </div>
                    </t>
                    <t t-if="player.age">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key">Age</span>
                        <span class="vn-stat-val"><t t-esc="player.age"/></span>
                      </div>
                    </t>
                    <t t-if="player.work_rate">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key">Work Rate</span>
                        <span class="vn-stat-val"><t t-esc="player.work_rate.capitalize()"/></span>
                      </div>
                    </t>
                    <t t-if="player.height">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key">Height</span>
                        <span class="vn-stat-val"><t t-esc="player.height"/></span>
                      </div>
                    </t>
                    <t t-if="player.weight">
                      <div class="vn-stat-row">
                        <span class="vn-stat-key">Weight</span>
                        <span class="vn-stat-val"><t t-esc="player.weight"/></span>
                      </div>
                    </t>
                  </t>
                  <t t-if="player.p_category">
                    <div class="vn-stat-row">
                      <span class="vn-stat-key">Category</span>
                      <span class="vn-stat-val"><t t-esc="player.p_category"/></span>
                    </div>
                  </t>
                  <t t-if="player.address">
                    <div class="vn-stat-row vn-fb-span-2">
                      <span class="vn-stat-key">Location</span>
                      <span class="vn-stat-val" style="text-transform:none; letter-spacing:0; font-size:clamp(.9rem,1.4vw,1.15rem);"><t t-esc="player.address"/></span>
                    </div>
                  </t>
                  <t t-if="not player.use_other_attributes and (player.playing_style_ids or player.strength_ids)">
                    <div class="player-tag-chips">
                      <t t-foreach="player.playing_style_ids" t-as="sty">
                        <span class="player-tag-chip" style="color:#fff; border-color:rgba(121,184,255,.5); background:rgba(121,184,255,.18);"><t t-esc="sty.icon"/> <t t-esc="sty.name"/></span>
                      </t>
                      <t t-foreach="player.strength_ids" t-as="strg">
                        <span class="player-tag-chip player-tag-chip--strength" style="color:#fff;"><t t-esc="strg.icon"/> <t t-esc="strg.name"/></span>
                      </t>
                    </div>
                  </t>
                </div>
              </t>
            </div>
          </div>

          <div
            class="auction-single-buttons d-flex align-items-center justify-content-center gap-3 mt-4"
          >
            <button onclick="setPlayerUnsold()">Set as unsold</button>
            <button class="sell" onclick="openSellModal()">Sell to Team</button>
          </div>
        </div>
      </div>

      <div class="next-player m-auto pb-5 pb-sm-5">
        <button class="next-player-btn" onclick="changeImage()">
          Next Player
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="white">
            <path d="M6 18L14.5 12 6 6V18ZM16 6V18H18V6H16Z"/>
          </svg>
        </button>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>

    <script>
      let array = [
        "https://images.pexels.com/photos/27219452/pexels-photo-27219452/free-photo-of-woman-in-white-dress-lying-down-on-couch.jpeg?auto=compress&amp;cs=tinysrgb&amp;w=1260&amp;h=750&amp;dpr=1",
        "https://images.pexels.com/photos/28857430/pexels-photo-28857430/free-photo-of-stack-of-delicious-homemade-cookies-on-plate.jpeg?auto=compress&amp;cs=tinysrgb&amp;w=600&amp;lazy=load",
        "https://images.pexels.com/photos/29248997/pexels-photo-29248997/free-photo-of-contemplative-man-holding-a-large-clock-outdoors.jpeg?auto=compress&amp;cs=tinysrgb&amp;w=600&amp;lazy=load",
        "./assets/images/player-image.png",
      ];

      let image = document.getElementById("loadImage");
      let currentIndex = 0;

      async function changeImage(skipOverlay) {
        var currentPlayerId = document.body.getAttribute('data-player-id');
        var countSecs = Math.max(1, parseInt(document.body.getAttribute('data-next-countdown') || '5'));
        var overlay = document.getElementById('ac-next-overlay');
        var countEl = document.getElementById('ac-next-count');
        var mainDiv = document.getElementById('ac-next-main');
        var lastDiv = document.getElementById('ac-next-last');

        // Fetch the next player first, before showing any overlay
        var html;
        try {
          var fetchUrl = window.location.href.split('?')[0] + '?exclude=' + currentPlayerId + '&amp;preview=1';
          var r = await fetch(fetchUrl, {cache: 'no-store'});
          if (!r.ok) throw new Error('HTTP ' + r.status);
          html = await r.text();
        } catch(e) {
          console.error('Next player load failed:', e);
          window.location.reload();
          return;
        }

        var parser = new DOMParser();
        var newDoc = parser.parseFromString(html, 'text/html');
        if (!newDoc.body || !newDoc.body.getAttribute('data-player-id')) {
          window.location.replace(window.location.href);
          return;
        }
        var newPlayerId = newDoc.body.getAttribute('data-player-id');

        // Last player — show message directly, no countdown
        if (newPlayerId === currentPlayerId) {
          if (mainDiv) mainDiv.style.display = 'none';
          if (lastDiv) lastDiv.style.display = 'block';
          if (overlay) {
            overlay.style.display = 'flex';
            requestAnimationFrame(function() {
              requestAnimationFrame(function() { overlay.style.opacity = '1'; });
            });
          }
          return;
        }

        var zone    = document.querySelector('[data-player-zone]');
        var newZone = newDoc.querySelector('[data-player-zone]');

        // Skip overlay when called after sold — swap player immediately
        if (skipOverlay || !overlay) {
          if (newPlayerId) document.body.setAttribute('data-player-id', newPlayerId);
          var _mf = (newDoc.body &amp;&amp; newDoc.body.getAttribute('data-is-mystery')) || '0';
          document.body.setAttribute('data-is-mystery', _mf);
          document.body.removeAttribute('data-mystery-revealed');
          if (zone &amp;&amp; newZone) { zone.innerHTML = newZone.innerHTML; }
          if (typeof window.acMysteryApplyFromBody === 'function') { window.acMysteryApplyFromBody(); }
          if (typeof window.acCommitPlayerOnStage === 'function') {
            await window.acCommitPlayerOnStage(newPlayerId);
          }
          return;
        }

        // Show countdown overlay, then swap
        if (mainDiv) mainDiv.style.display = 'flex';
        if (lastDiv) lastDiv.style.display = 'none';
        overlay.style.display = 'flex';
        requestAnimationFrame(function() {
          requestAnimationFrame(function() { overlay.style.opacity = '1'; });
        });
        if (countEl) countEl.textContent = countSecs;

        await new Promise(function(resolve) {
          var remaining = countSecs;
          var tick = setInterval(function() {
            remaining--;
            if (remaining &lt;= 0) { clearInterval(tick); resolve(); }
            else {
              if (countEl) {
                countEl.textContent = remaining;
                countEl.classList.remove('ac-count-pop');
                void countEl.offsetWidth;
                countEl.classList.add('ac-count-pop');
              }
            }
          }, 1000);
        });

        if (newPlayerId) document.body.setAttribute('data-player-id', newPlayerId);
        var _mf = (newDoc.body &amp;&amp; newDoc.body.getAttribute('data-is-mystery')) || '0';
        document.body.setAttribute('data-is-mystery', _mf);
        document.body.removeAttribute('data-mystery-revealed');
        if (zone &amp;&amp; newZone) { zone.innerHTML = newZone.innerHTML; }
        if (typeof window.acMysteryApplyFromBody === 'function') { window.acMysteryApplyFromBody(); }
        if (typeof window.acCommitPlayerOnStage === 'function') {
          await window.acCommitPlayerOnStage(newPlayerId);
        }
        overlay.style.opacity = '0';
        setTimeout(function() { overlay.style.display = 'none'; }, 400);
      }

      function closeNextOverlay() {
        var ov = document.getElementById('ac-next-overlay');
        if (!ov) return;
        ov.style.opacity = '0';
        setTimeout(function() { ov.style.display = 'none'; }, 400);
      }

      document.addEventListener("keydown", (event) => {
        // alert("space clicked");
        if (event.code === "Space") {
          event.preventDefault();
          changeImage();
        }
      });

      function setPlayerUnsold() {
        var modal = document.getElementById('unsoldConfirmModal');
        modal.style.display = 'flex';
      }

      (function() {
        document.addEventListener('DOMContentLoaded', function() {
          document.getElementById('unsoldModalCancel').addEventListener('click', function() {
            document.getElementById('unsoldConfirmModal').style.display = 'none';
          });
          document.getElementById('unsoldModalConfirm').addEventListener('click', function() {
            document.getElementById('unsoldConfirmModal').style.display = 'none';
            var playerId = parseInt(document.body.getAttribute('data-player-id'));
            if (!playerId) {
              showUnsoldToast('Error: Player ID not found', true);
              return;
            }
            fetch('/web/dataset/call_kw', {
              method: 'POST',
              headers: {'Content-Type': 'application/json'},
              body: JSON.stringify({
                jsonrpc: '2.0',
                method: 'call',
                params: {
                  model: 'auction.team.player',
                  method: 'action_unsold',
                  args: [[playerId]],
                  kwargs: {}
                }
              })
            }).then(function(response) {
              return response.json();
            }).then(function(result) {
              if (result.error) {
                showUnsoldToast('Failed to mark player as unsold', true);
              } else {
                var secs = (result.result &amp;&amp; result.result.display_seconds > 0) ? result.result.display_seconds : 5;
                showUnsoldStamp(secs, '#b71c1c', '#fff');
              }
            }).catch(function() {
              showUnsoldToast('Failed to mark player as unsold', true);
            });
          });
        });
      })();

      function showUnsoldToast(message, isError) {
        var toast = document.getElementById('unsoldToast');
        toast.textContent = message;
        toast.style.background = isError ? '#c0392b' : '#27ae60';
        toast.style.display = 'block';
        setTimeout(function() { toast.style.display = 'none'; }, 3500);
      }

      function showUnsoldStamp(displaySeconds, stampColor, stampText) {
        if (!document.getElementById('_unsoldStampCSS')) {
          var s = document.createElement('style');
          s.id = '_unsoldStampCSS';
          s.textContent = '@keyframes _unsoldDrop{0%{transform:rotate(-22deg) scale(2.4);opacity:0}100%{transform:rotate(-22deg) scale(1);opacity:.92}}@keyframes _unsoldFadeIn{from{opacity:0}to{opacity:1}}';
          document.head.appendChild(s);
        }
        var img = document.getElementById('loadImage');
        var r = img.getBoundingClientRect();
        var ov = document.createElement('div');
        ov.style.cssText = 'position:fixed;z-index:9500;top:'+r.top+'px;left:'+r.left+'px;width:'+r.width+'px;height:'+r.height+'px;background:rgba(0,0,0,0.48);display:flex;align-items:center;justify-content:center;animation:_unsoldFadeIn .35s ease;pointer-events:none;';
        var stamp = document.createElement('div');
        stamp.style.cssText = 'background:'+stampColor+';color:'+stampText+';font-size:clamp(1.8rem,5vw,3.2rem);font-weight:900;letter-spacing:6px;padding:8px 36px;border:5px solid rgba(255,255,255,0.85);border-radius:6px;box-shadow:0 0 0 3px '+stampColor+',0 8px 40px rgba(0,0,0,.5);opacity:0;animation:_unsoldDrop .5s cubic-bezier(.34,1.3,.64,1) .15s forwards;text-shadow:2px 3px 8px rgba(0,0,0,.4);';
        stamp.textContent = 'UNSOLD';
        var barWrap = document.createElement('div');
        barWrap.style.cssText = 'position:absolute;bottom:12px;left:16px;right:16px;';
        var barTrack = document.createElement('div');
        barTrack.style.cssText = 'background:rgba(255,255,255,0.25);border-radius:4px;height:6px;overflow:hidden;';
        var bar = document.createElement('div');
        bar.style.cssText = 'height:100%;width:100%;background:'+stampColor+';border-radius:4px;transition:width '+displaySeconds+'s linear;';
        barTrack.appendChild(bar);
        barWrap.appendChild(barTrack);
        ov.appendChild(stamp);
        ov.appendChild(barWrap);
        document.body.appendChild(ov);
        requestAnimationFrame(function() { requestAnimationFrame(function() { bar.style.width = '0%'; }); });
        setTimeout(function() { if (ov.parentNode) ov.parentNode.removeChild(ov); changeImage(true); }, displaySeconds * 1000);
      }


      function resolveAuctionPanelUrl(kind) {
        var el = document.documentElement;
        var url = (kind === 'rp' ? el.dataset.rpUrl : el.dataset.balanceUrl) || '';
        // Missing db_name produced "//slug/..." which browsers treat as another host.
        if (url.indexOf('//') === 0) { url = ''; }
        if (url) { return url; }
        var parts = (location.pathname || '').split('/').filter(Boolean);
        var db = parts[0] || '';
        var slug = '';
        if (parts[1] === 'auction' &amp;&amp; (parts[2] === 'display_auction' || parts[2] === 'player_selector') &amp;&amp; parts[3]) {
          slug = parts[3];
        }
        if (!db || !slug) { return ''; }
        if (kind === 'rp') {
          var theme = el.dataset.theme || 'vanilla';
          return '/' + db + '/' + slug + '/auction/display_auction/remaining-players?theme=' + encodeURIComponent(theme);
        }
        return '/' + db + '/' + slug + '/auction/show/team/balance';
      }

      function openBidSummary() {
        var frame = document.getElementById('bidSummaryFrame');
        var url = resolveAuctionPanelUrl('balance');
        if (frame &amp;&amp; url) {
          var cur = frame.getAttribute('src') || '';
          if (!cur || cur === 'about:blank') {
            frame.src = url;
          } else {
            try {
              if (frame.contentWindow &amp;&amp; typeof frame.contentWindow.refreshBalance === 'function') {
                frame.contentWindow.refreshBalance();
              }
            } catch (e) {}
          }
        }
        document.getElementById('bidSummaryDrawer').style.right = '0';
        document.getElementById('bidSummaryTabArrow').innerHTML = '&#x2192;';
        document.getElementById('bidSummaryOverlay').style.display = 'block';
        document.body.style.overflow = 'hidden';
      }
      function closeBidSummary() {
        document.getElementById('bidSummaryDrawer').style.right = '-1150px';
        document.getElementById('bidSummaryTabArrow').innerHTML = '&#x2190;';
        document.getElementById('bidSummaryOverlay').style.display = 'none';
        document.body.style.overflow = '';
      }
      function toggleBidSummary() {
        var r = document.getElementById('bidSummaryDrawer').style.right;
        if (r === '0px' || r === '0') { closeBidSummary(); } else { openBidSummary(); }
      }
      function openRemainingPlayers() {
        document.getElementById('rpFrame').src = resolveAuctionPanelUrl('rp');
        document.getElementById('rpDrawer').style.right = '0';
        document.getElementById('rpTabArrow').innerHTML = '&#x2192;';
        document.getElementById('rpOverlay').style.display = 'block';
        document.body.style.overflow = 'hidden';
      }
      function closeRemainingPlayers() {
        document.getElementById('rpDrawer').style.right = '-900px';
        document.getElementById('rpTabArrow').innerHTML = '&#x2190;';
        document.getElementById('rpOverlay').style.display = 'none';
        document.body.style.overflow = '';
      }
      function toggleRemainingPlayers() {
        var r = document.getElementById('rpDrawer').style.right;
        if (r === '0px' || r === '0') { closeRemainingPlayers(); } else { openRemainingPlayers(); }
      }
    </script>
      <!-- Bid Summary Drawer -->
      <div id="bidSummaryDrawer" style="position:fixed; top:0; right:-1150px; width:1150px; max-width:96vw; height:100vh; z-index:9800; display:flex; flex-direction:column; background:#1b2f4d; border-left:3px solid #5b7eb8; box-shadow:-6px 0 32px rgba(26,53,96,0.35); transition:right .35s cubic-bezier(.4,0,.2,1);">
        <!-- Fold/unfold tab -->
        <div onclick="toggleBidSummary()" style="position:absolute; left:-44px; top:50%; transform:translateY(-50%); width:44px; height:130px; background:linear-gradient(180deg,#3d5f94,#5b7eb8); border-radius:8px 0 0 8px; cursor:pointer; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:6px; box-shadow:-4px 2px 14px rgba(91,126,184,0.35); z-index:1;">
          <span id="bidSummaryTabArrow" style="color:#fff; font-size:1.2rem; line-height:1;">&#x2190;</span>
          <span style="color:#fff; writing-mode:vertical-rl; transform:rotate(180deg); font-size:0.58rem; font-weight:700; letter-spacing:2px; text-transform:uppercase; opacity:0.9;">Summary</span>
        </div>
        <div style="padding:16px 20px; background:linear-gradient(90deg,#3d5f94,#5b7eb8); color:#fff; display:flex; align-items:center; justify-content:space-between; flex-shrink:0;">
          <span style="font-size:1.05rem; font-weight:700; letter-spacing:.3px;">&#x1F4CA; Auction Bid Summary</span>
          <button onclick="closeBidSummary()" style="background:rgba(255,255,255,0.15); border:1px solid rgba(255,255,255,0.3); color:#fff; width:32px; height:32px; border-radius:6px; font-size:1.2rem; cursor:pointer; display:flex; align-items:center; justify-content:center; line-height:1;">&#x2715;</button>
        </div>
        <iframe id="bidSummaryFrame" style="flex:1; border:none; width:100%;" title="Bid Summary"></iframe>
      </div>
      <div id="bidSummaryOverlay" onclick="closeBidSummary()" style="display:none; position:fixed; inset:0; z-index:9700; background:rgba(0,0,0,0.4);"></div>
      <!-- Players Left Drawer -->
      <div id="rpDrawer" style="position:fixed; top:0; right:-900px; width:900px; max-width:96vw; height:100vh; z-index:9750; display:flex; flex-direction:column; background:#1b2f4d; border-left:3px solid #5b7eb8; box-shadow:-6px 0 32px rgba(26,53,96,0.35); transition:right .35s cubic-bezier(.4,0,.2,1);">
        <div onclick="toggleRemainingPlayers()" style="position:absolute; left:-44px; top:65%; transform:translateY(-50%); width:44px; height:130px; background:linear-gradient(180deg,#3d5f94,#5b7eb8); border-radius:8px 0 0 8px; cursor:pointer; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:6px; box-shadow:-4px 2px 14px rgba(91,126,184,0.35); z-index:1;">
          <span id="rpTabArrow" style="color:#fff; font-size:1.2rem; line-height:1;">&#x2190;</span>
          <span style="color:#fff; writing-mode:vertical-rl; transform:rotate(180deg); font-size:0.58rem; font-weight:700; letter-spacing:2px; text-transform:uppercase; opacity:0.9; white-space:nowrap;">Players Left</span>
        </div>
        <div style="padding:14px 20px; background:linear-gradient(90deg,#3d5f94,#5b7eb8); color:#fff; display:flex; align-items:center; justify-content:flex-end; flex-shrink:0;">
          <button onclick="closeRemainingPlayers()" style="background:rgba(255,255,255,0.15); border:1px solid rgba(255,255,255,0.3); color:#fff; width:32px; height:32px; border-radius:6px; font-size:1.2rem; cursor:pointer; display:flex; align-items:center; justify-content:center; line-height:1;">&#x2715;</button>
        </div>
        <iframe id="rpFrame" style="flex:1; border:none; width:100%;" title="Remaining Players"></iframe>
      </div>
      <div id="rpOverlay" onclick="closeRemainingPlayers()" style="display:none; position:fixed; inset:0; z-index:9650; background:rgba(0,0,0,0.55);"></div>
      <!-- Unsold Confirmation Modal -->
      <div id="unsoldConfirmModal" style="display:none; position:fixed; inset:0; z-index:10000; background:rgba(10,20,40,0.72); align-items:center; justify-content:center;">
        <div style="background:#243a5c; border-radius:14px; padding:36px 28px 28px; max-width:400px; width:92%; box-shadow:0 12px 40px rgba(0,0,0,0.45); text-align:center; font-family:inherit; border:1px solid rgba(160,185,215,0.28);">
          <div style="width:56px; height:56px; border-radius:50%; background:rgba(229,57,53,0.15); border:2px solid #e53935; display:flex; align-items:center; justify-content:center; margin:0 auto 16px; font-size:1.6rem;">⚠️</div>
          <h5 style="margin:0 0 10px; font-size:1.15rem; font-weight:700; color:#eef2f8;">Set Player as Unsold?</h5>
          <p style="color:rgba(238,242,248,0.7); margin:0 0 26px; font-size:0.9rem; line-height:1.5;">This will set the player to Unsold. You can bring them back to Auction later.</p>
          <div style="display:flex; gap:12px; justify-content:center;">
            <button id="unsoldModalCancel" style="padding:10px 28px; border-radius:8px; border:1px solid rgba(160,185,215,0.3); background:rgba(255,255,255,0.08); color:#eef2f8; font-size:0.95rem; cursor:pointer; font-weight:500;">Cancel</button>
            <button id="unsoldModalConfirm" style="padding:10px 28px; border-radius:8px; border:none; background:#e53935; color:#fff; font-size:0.95rem; font-weight:700; cursor:pointer;">Confirm</button>
          </div>
        </div>
      </div>
      <!-- Toast notification -->
      <div id="unsoldToast" style="display:none; position:fixed; bottom:28px; left:50%; transform:translateX(-50%); z-index:10001; color:#fff; padding:12px 28px; border-radius:8px; font-size:0.95rem; font-weight:600; box-shadow:0 4px 16px rgba(0,0,0,0.3);"></div>
    <!-- Next-player countdown overlay -->
    <style>
      @keyframes ac-count-pop { 0%{transform:scale(1.35);opacity:.55} 100%{transform:scale(1);opacity:1} }
      .ac-count-pop { animation: ac-count-pop 0.45s cubic-bezier(.22,1,.36,1); }
    </style>
    <div id="ac-next-overlay" style="display:none;opacity:0;position:fixed;inset:0;z-index:11000;background:rgba(0,5,20,0.88);backdrop-filter:blur(6px);transition:opacity .4s;align-items:center;justify-content:center;flex-direction:column;gap:18px;">
      <div id="ac-next-main" style="display:flex;flex-direction:column;align-items:center;gap:18px;text-align:center;">
        <p style="color:rgba(200,220,255,0.75);font-size:1.25rem;font-weight:600;letter-spacing:3px;text-transform:uppercase;margin:0;">Next player out of the deck is</p>
        <div id="ac-next-count" style="font-size:11rem;font-weight:900;color:#5b9bd5;line-height:1;text-shadow:0 0 80px rgba(91,155,213,0.7),0 0 20px rgba(91,155,213,0.5);font-variant-numeric:tabular-nums;">5</div>
        <p style="color:rgba(200,220,255,0.4);font-size:0.85rem;letter-spacing:1.5px;margin:0;">Get ready…</p>
      </div>
      <div id="ac-next-last" style="display:none;text-align:center;padding:20px 32px;">
        <div style="font-size:3.5rem;margin-bottom:20px;">🏁</div>
        <p style="color:rgba(200,220,255,0.55);font-size:0.8rem;letter-spacing:3px;text-transform:uppercase;margin:0 0 12px;">End of Deck</p>
        <p style="color:#e8eaf6;font-size:1.6rem;font-weight:800;margin:0 0 10px;">This is the last player<br/>in the auction!</p>
        <p style="color:rgba(200,220,255,0.4);font-size:0.88rem;margin:0 0 28px;">Sell or set this player as unsold to wrap up.</p>
        <button onclick="closeNextOverlay()" style="padding:10px 28px;border-radius:8px;border:1px solid rgba(91,155,213,0.45);background:rgba(91,155,213,0.15);color:#5b9bd5;font-size:0.95rem;cursor:pointer;font-weight:600;">Got it</button>
      </div>
    </div>
    <!-- Pause (Break) + Fullscreen -->
    <div style="position:fixed;top:14px;right:14px;z-index:9999;display:flex;align-items:center;gap:8px;">
      <button type="button" id="ac-break-btn" class="ac-break-btn ac-break-btn-fixed" onclick="acToggleBreakTime()" title="Break Time">&#9208; Pause</button>
      <button id="fs-btn" onclick="toggleFullscreen()" title="Enter Fullscreen" style="width:38px;height:38px;border-radius:8px;background:rgba(0,20,50,0.75);border:1.5px solid rgba(0,64,128,0.55);color:#5b9bd5;cursor:pointer;display:flex;align-items:center;justify-content:center;backdrop-filter:blur(4px);padding:0;transition:background .2s,box-shadow .2s;" onmouseover="this.style.boxShadow='0 0 12px rgba(0,64,128,0.5)'" onmouseout="this.style.boxShadow='none'"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"/></svg></button>
    </div>
    <script>
      function toggleFullscreen() {
        if (!document.fullscreenElement) {
          document.documentElement.requestFullscreen().catch(function(e) { console.warn(e); });
        } else {
          document.exitFullscreen();
        }
      }
      document.addEventListener('fullscreenchange', function() {
        var btn = document.getElementById('fs-btn');
        if (!btn) return;
        if (document.fullscreenElement) {
          sessionStorage.setItem('auction_fullscreen', '1');
          btn.title = 'Exit Fullscreen';
          btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 3v3a2 2 0 0 1-2 2H3m18 0h-3a2 2 0 0 1-2-2V3m0 18v-3a2 2 0 0 1 2-2h3M3 16h3a2 2 0 0 1 2 2v3"/></svg>';
        } else {
          sessionStorage.removeItem('auction_fullscreen');
          btn.title = 'Enter Fullscreen';
          btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"/></svg>';
        }
      });
      if (sessionStorage.getItem('auction_fullscreen') === '1') {
        document.documentElement.requestFullscreen().catch(function(e) { console.warn(e); });
      }
    </script>
    <t t-call="auction_module.player_break_pause_assets"/>
    <t t-call="auction_module.player_mystery_controls"/>
    <t t-call="auction_module.player_sell_modal"/>
    <footer class="vn-footer">
      <span class="vn-footer-lbl">Powered by</span>
      <img class="vn-footer-logo" src="/auction_module/static/src/assets/images/logo.svg" alt="AuctionChamp"/>
    </footer>
    <script>
(function(){
  var t=localStorage.getItem('auction_vn_theme')||'light';
  document.body.setAttribute('data-theme',t);
  var bd=document.getElementById('vn-th-dark');
  var bl=document.getElementById('vn-th-light');
  if(bd){bd.style.display=t==='dark'?'inline':'none';}
  if(bl){bl.style.display=t==='light'?'inline':'none';}
})();
function toggleVnTheme(){
  var cur=document.body.getAttribute('data-theme');
  var nxt=cur==='light'?'dark':'light';
  document.body.setAttribute('data-theme',nxt);
  localStorage.setItem('auction_vn_theme',nxt);
  var bd=document.getElementById('vn-th-dark');
  var bl=document.getElementById('vn-th-light');
  if(bd){bd.style.display=nxt==='dark'?'inline':'none';}
  if(bl){bl.style.display=nxt==='light'?'inline':'none';}
}
    </script>
  </body>
</html>
</template>

</odoo>
