<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="player_sequence_selector">
    <html lang="en">
        <head>
            <meta charset="utf-8"/>
            <meta name="viewport" content="width=device-width, initial-scale=1"/>
            <title>Auction Player Queue</title>

            <!-- Bootstrap -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
                  rel="stylesheet"
                  integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
                  crossorigin="anonymous"/>
            <!-- Google Fonts: Teko -->
            <link rel="preconnect" href="https://fonts.googleapis.com"/>
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="1"/>
            <link href="https://fonts.googleapis.com/css2?family=Teko:wght@600&amp;display=swap" rel="stylesheet"/>

            <!-- Theme stylesheet -->
            <link rel="stylesheet" href="/auction_module/static/src/assets/css/style.css"/>

            <!-- Odoo assets (CSS only) -->
            <t t-call-assets="web.assets_common" t-js="false"/>
            <t t-call-assets="web.assets_frontend" t-js="false"/>

            <style>
                /* ── Player drawer overlay (opened by openPlayerDrawer()) ── */
                .player-drawer-overlay {
                    position: fixed;
                    top: 0; left: 0;
                    width: 100%; height: 100%;
                    z-index: 9999;
                    display: flex;
                    align-items: flex-start;
                    justify-content: flex-end;
                }
                .player-drawer-backdrop {
                    position: absolute;
                    top: 0; left: 0;
                    width: 100%; height: 100%;
                    background: rgba(0,0,0,0.55);
                    backdrop-filter: blur(3px);
                    animation: _seqFadeIn 0.3s ease-in-out;
                }
                .player-drawer-container {
                    position: relative;
                    width: 90%;
                    height: 100vh;
                    background: #0a0a1a;
                    box-shadow: -5px 0 25px rgba(0,0,0,0.35);
                    overflow-y: auto;
                    animation: _seqSlideIn 0.4s ease-out;
                    z-index: 10000;
                }
                .player-drawer-header {
                    position: sticky;
                    top: 0;
                    background: rgba(0,0,0,0.5);
                    backdrop-filter: blur(6px);
                    padding: 1rem 1.5rem;
                    border-bottom: 1px solid rgba(255,255,255,0.1);
                    z-index: 10001;
                }
                .player-drawer-close {
                    background: none;
                    border: none;
                    font-size: 2.8rem;
                    cursor: pointer;
                    color: #fff;
                    padding: 0;
                    width: 48px; height: 48px;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    transition: transform 0.2s;
                }
                .player-drawer-close:hover { transform: scale(1.2); color: #e53935; }
                .player-drawer-content { min-height: calc(100vh - 70px); }
                body.drawer-open { overflow: hidden; }
                @keyframes _seqFadeIn {
                    from { opacity: 0; } to { opacity: 1; }
                }
                @keyframes _seqSlideIn {
                    from { transform: translateX(100%); } to { transform: translateX(0); }
                }
                @keyframes slideOutRight {
                    from { transform: translateX(0); } to { transform: translateX(100%); }
                }
            </style>
        </head>
        <body style="min-height: 100vh">

            <!-- Banner — same as auction_player_template_new.xml -->
            <header class="single-header">
                <div class="py-3 text-center">
                    <img src="/auction_module/static/src/assets/images/logo.svg" alt="Logo"/>
                </div>
            </header>

            <div class="auction-container">

                <h1 class="auction-title">Auction Player Queue</h1>

                <div id="auction_grid" class="auction-grid">
                </div>

            </div>

            <!-- Odoo assets (JS only) -->
            <t t-call-assets="web.assets_common" t-css="false"/>
            <t t-call-assets="web.assets_frontend" t-css="false"/>
        </body>
    </html>
</template>

</odoo>
